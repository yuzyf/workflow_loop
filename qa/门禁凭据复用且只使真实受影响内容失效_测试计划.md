# 门禁凭据复用且只使真实受影响内容失效测试计划

- 工作流编号：2026-08-10-0817-product_change
- 上游验收计划：[门禁凭据复用且只使真实受影响内容失效验收计划](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：第二道门完整校验一次且第三道门只复用有效凭据](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 第三道门只比较第二道门凭据](#tc-01) | 无 | 自动化测试 | 对同一环节执行 `workflow gate impl`，再执行 `workflow gate impl --confirmed` | `src/workflow_loop/verification.py::compare_validation_credential` | `tests/test_current_workflow_qa.py::test_gate_ac01_third_gate_reuses_second_gate_credential` | 准备可通过第二道门的环节，并为完整校验器、测试执行器和穿刺执行器设置调用计数，责任输入保持不变 | 执行第二道门和第三道门并读取状态、流水和调用计数 | 校验器调用次数、凭据、门禁流水、当前环节和昂贵执行器调用次数 | 完整校验恰好一次并产生一份凭据；第三道门只比较凭据，零测试和零穿刺执行并推进 | 完整校验或日志重复，第三道门重跑校验、测试或穿刺，或无凭据仍推进 | pytest JUnit XML、调用计数、凭据哈希、两道门输出、流水记录和推进前后状态 |
| [AC-02：校验凭据只绑定当前环节真正负责的输入](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 凭据只绑定责任输入](#tc-02) | 无 | 自动化测试 | 执行 `workflow gate acceptance_plan` 生成凭据，再执行 `workflow gate acceptance_plan --confirmed` 或凭据复核 | `src/workflow_loop/verification.py::stage_responsibility_paths` | `tests/test_current_workflow_qa.py::test_gate_ac02_credential_binds_only_stage_responsibility` | 为验收计划环节准备责任文档、正常下游结果链接、未登记文件、缓存和构建输出，并生成当前凭据 | 分别修改下游字段、范围外文件、责任文件、责任状态和规则版本后复核凭据 | `bound_files`（绑定文件摘要）、`bound_state`（绑定状态摘要）和逐项差异文本 | 下游正常更新及范围外变化不失效；责任文件、责任状态或规则版本变化时拒绝并指出具体项；状态不保存文档全文 | 凭据绑定整个项目或下游结果，漏绑责任输入，只报告总哈希变化，或状态复制正式文档正文 | pytest JUnit XML、凭据字段清单和哈希、各变化场景的比较结果及具体文件、状态、版本差异 |
| [AC-03：失效范围只覆盖有具体证据的直接受影响内容](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 变化只失效直接受影响结果](#tc-03) | 无 | 自动化测试 | 修改一个主题的责任输入后执行 `workflow gate qa` | `src/workflow_loop/verification.py::inspect_invalidation` | `tests/test_current_workflow_qa.py::test_gate_ac03_invalidation_clears_only_directly_affected_results` | 构造两个独立主题及共享索引，保存计划、代码、任务、结果、验收和最终全量回归事实 | 分别改变单主题验收计划、代码、测试计划、测试代码、机器记录、结果整理和公共入口，再执行调查与应用 | 诊断清单、受影响主题、QA 内部步骤、保留或删除文件、任务和各验证哈希 | 单主题变化只清所属主题；共享入口才影响全部主题；结果整理错误保留机器记录；真实代码或测试变化使最终全量回归失效 | 整轮清空、因依赖关系扩大范围、边查边删、范围外变化清上游，或遗漏最终全量回归失效 | pytest JUnit XML、逐文件差异、受影响主题、应用前后状态、保留和删除清单及最终全量回归状态 |
| [AC-04：门禁失败一次列全问题并给出唯一下一动作](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md#ac-04) | <a id="tc-04"></a>[TC-04 失败一次列全且动作唯一](#tc-04) | 无 | 自动化测试 | 对不变的多错误输入连续两次执行 `workflow gate impl` | `src/workflow_loop/diagnostics.py::format_validation_report` | `tests/test_current_workflow_qa.py::test_gate_ac04_failure_is_complete_stable_and_has_one_next_action` | 构造两个独立文件错误、一个状态错误和一个被前置错误阻断的检查，输入字节保持不变 | 原样执行同一失败门禁两次，不使用管道、截断或重定向 | 两次原始输出、诊断顺序与哈希、未检查项、下一动作、命令数量和状态副作用 | 每次一次列出全部独立错误；阻断项标为未检查；两次内容顺序和哈希相同；仅一个完整下一动作和命令 | 逐次暴露问题，未检查被猜成通过或失败，输出多个命令，相同输入结果不稳定，或发生副作用 | pytest JUnit XML、两次完整输出、报告哈希、问题计数和顺序、命令计数及执行前后状态哈希 |

## 2. 针对性回归范围

- 回归第二道门和第三道门调用边界、凭据规则版本及责任文件集合比较。
- 回归单主题、共享入口和 QA 内部不同变化源的精确失效范围。

## 3. 测试条件要求

- 使用隔离工作流状态和可计数替身执行器；第三道门场景不得实际重跑正式测试或穿刺命令。
- 相同失败输入必须逐字保持不变，以比较两次完整诊断的稳定顺序和报告哈希。

## 4. 未决测试条件

暂无

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/门禁凭据复用且只使真实受影响内容失效_验收计划.md) | 定义凭据复用、责任输入和精确失效边界 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付关系和状态 |
| 上游 | [实施记录](../impl/门禁凭据复用且只使真实受影响内容失效_实施记录.md) | 测试入口和观察位置来自已确认实施与真实代码 |
| 下游 | `./门禁凭据复用且只使真实受影响内容失效_测试结果.md`（待生成） | 正式执行后记录当前机器事实 |
