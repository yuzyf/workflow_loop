# 删除文件不再阻塞基线准备测试计划

- 工作流编号：2026-09-09-0901-product_change
- 上游验收计划：[删除文件不再阻塞基线准备验收计划](../acceptance/删除文件不再阻塞基线准备_验收计划.md)

<a id="1-验收条件覆盖"></a>
## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-02：删除从未提交文件也放行](../acceptance/删除文件不再阻塞基线准备_验收计划.md#ac-02) | <a id="tc-01"></a>[TC-01 删除从未提交文件放行记无副本](#tc-01) | 无 | 自动化测试 | workflow gate impl --prepare-code（实施前基线准备） | `src/workflow_loop/rollback.py::prepare_impl` | `tests/test_gate_delete_files.py::test_delete_uncommitted_file_passes_prepare` | 进场快照记录 victim.py 存在（哈希为真实内容哈希）、该文件从未提交 Git、实施中删除 | AI 执行实施前基线准备 | 准备输出和回退清单 manifest.json 的 entries | 准备成功；victim.py 登记无副本（original_exists=False 且有 no_copy_reason） | 因 Git HEAD 不包含该路径拒绝准备或要求恢复文件 | pytest JUnit XML 报告含上述断言结果 |
| [AC-01：删除已提交文件直接放行](../acceptance/删除文件不再阻塞基线准备_验收计划.md#ac-01) | <a id="tc-02"></a>[TC-02 删除已提交文件用 HEAD 补副本](#tc-02) | 无 | 自动化测试 | workflow gate impl --prepare-code（实施前基线准备） | `src/workflow_loop/rollback.py::_trusted_git_head_baseline` | `tests/test_gate_delete_files.py::test_deleted_committed_file_uses_git_baseline` | victim.py 已提交、进场快照记录真实内容哈希、实施中删除 | 执行 Git 自证（传进场快照哈希） | 自证返回的字节副本和详情 | 自证放行返回 HEAD 字节；详情含两个哈希完全一致 | 删除已提交文件被拒或拿不到副本 | pytest JUnit XML 报告含上述断言结果 |
| [AC-03：修改文件行为不回归](../acceptance/删除文件不再阻塞基线准备_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 修改文件既有行为不回归](#tc-03) | 无 | 自动化测试 | workflow gate impl --prepare-code（实施前基线准备） | `src/workflow_loop/rollback.py::prepare_impl` | `tests/test_rollback.py::test_adjusted_plan_accepts_unsampled_file_equal_to_clean_git_head 等既有自证测试集` | 进场后修改（未删除）的代码文件，覆盖能自证和不能自证两种状态 | 执行实施前基线准备（跑既有测试集） | test_rollback.py 与 test_git_baseline_exit.py 全量结果 | 既有自证测试全部通过（能自证放行、不能自证按文件拒绝） | 已放行的修改文件场景开始报错或已拒绝场景静默放行 | pytest JUnit XML 报告全绿 |
| [AC-04：作废时无副本文件告知](../acceptance/删除文件不再阻塞基线准备_验收计划.md#ac-04) | <a id="tc-04"></a>[TC-04 作废预检输出无副本清单](#tc-04) | 无 | 自动化测试 | workflow abort（整轮作废恢复预检） | `src/workflow_loop/rollback.py::preflight_abort` | `tests/test_rollback.py::test_full_abort_restores_managed_content_and_project_fields_only` | 回退清单含登记为无副本的删除文件 | 用户确认整轮作废，程序执行恢复预检 | 作废预检输出和 abort_manifest 的 no_copy_paths | 作废成功；无副本文件保持删除；预检输出列名并说明实施前内容无法取得 | 因无副本阻塞作废或输出不提这些文件 | pytest JUnit XML 报告含恢复测试通过 |

<a id="2-针对性回归范围"></a>
## 2. 针对性回归范围

- test_git_baseline_exit.py 全量（既有 Git 自证行为不回归）。

<a id="3-测试条件要求"></a>
## 3. 测试条件要求

- 本地 Git 环境可用；测试用临时目录构造真实 Git 仓库和状态文件，不依赖外部服务。

<a id="4-未决测试条件"></a>
## 4. 未决测试条件

- 暂无

<a id="5-上下游文档"></a>
## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [删除文件不再阻塞基线准备验收计划](../acceptance/删除文件不再阻塞基线准备_验收计划.md) | 本测试计划依据的验收条件 |
| 上游 | [实施记录](../impl/删除文件不再阻塞基线准备_实施记录.md) | 测试入口和观察位置来自已确认实施与真实代码 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付关系和状态 |
| 下游 | [删除文件不再阻塞基线准备测试结果](./删除文件不再阻塞基线准备_测试结果.md) | 记录正式执行的结构化报告事实 |
