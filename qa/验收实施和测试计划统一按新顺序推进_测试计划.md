# 验收实施和测试计划统一按新顺序推进测试计划

- 工作流编号：2026-08-07-0837-product_change
- 上游验收计划：[验收实施和测试计划统一按新顺序推进验收计划](../acceptance/验收实施和测试计划统一按新顺序推进_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：新轮次按完整统一顺序推进](../acceptance/验收实施和测试计划统一按新顺序推进_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 三类完整轮次都先实施后测试计划](#tc-01) | 无 | 自动化测试 | `workflow start --intent from_scratch`、`workflow start --intent product_change` 和 `workflow start --intent bugfix` | `src/workflow_loop/path_composer.py::build_stage_path` | `tests/test_current_workflow_contracts.py::test_complete_research_paths_follow_impl_before_test_plan` | 分别准备从零创建、修改产品和修复缺陷三种意图，并保留无需开发任务作为边界样本 | 调用阶段路径生成入口取得每种意图的完整阶段数组 | 返回的阶段数组及实施前回退门要求 | 三种完整轮次均按验收计划、实施、测试计划、测试代码、测试执行排列；无需开发任务没有研发阶段 | 任一完整轮次仍把测试计划放在实施前、缺少实施或测试阶段、出现重复阶段，或无需开发任务被错误扩展 | pytest JUnit XML 报告、四种意图的实际阶段数组和精确测试入口 |
| [AC-02：进行中旧轮次只迁移一次并废止实施前测试计划](../acceptance/验收实施和测试计划统一按新顺序推进_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 旧轮次只读零写且首次继续只迁移一次](#tc-02) | TC-01 | 自动化测试 | `workflow status` 后执行一条会写状态的继续命令 | `src/workflow_loop/cli.py::ensure_stage_path_current` | `tests/test_current_workflow_contracts.py::test_legacy_stage_order_migrates_once_without_read_side_effects` | 构造测试计划位于实施前的活动状态，写入已确认验收、旧测试任务、验收记录、回归状态、追踪行和正式结果文件，并保存所有文件原字节 | 先执行只读状态预览，再执行一次继续入口，再执行第二次继续；另注入状态保存和日志追加失败 | 状态文件、流水日志、追踪表、结果文件、当前阶段及迁移记录数 | 只读前后逐字一致；首次继续保留验收和代码、清旧测试及后续并回到实施；第二次零重复迁移；任一写入失败全部恢复 | 只读产生写入、旧成功结果仍有效、代码被删除、迁移重复、失败留下半迁移或审计记录缺失 | pytest JUnit XML 报告、迁移前后文件哈希、唯一迁移日志和故障恢复比较结果 |
| [AC-03：上游变化只保留仍有当前依据的结果](../acceptance/验收实施和测试计划统一按新顺序推进_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 三类上游变化按主题精确失效](#tc-03) | TC-02 | 自动化测试 | `workflow return` 后执行对应阶段门禁 | `src/workflow_loop/verification.py::inspect_invalidation` | `tests/test_current_workflow_contracts.py::test_upstream_invalidation_preserves_only_current_evidence` | 构造两个独立主题并确认实施、测试计划、测试执行和验收；分别改变一个主题的验收计划、实施代码或记录、测试计划，再构造共享索引变化；另将 `qa/索引.md` 的测试结果和 `acceptance/索引.md` 的验收结果从待生成路径切换为对应正式链接 | 对每种输入调用只读失效检查；对真实上游变化应用检查结果并比较状态、哈希、任务、结果文件和追踪列，对两种结果链接转换只比较规范化前后快照与失效结果 | `InvalidationInspection`（失效检查事实）中的文件差异与受影响主题、应用后的状态和文件，以及测试计划和验收计划的规范化快照 | 验收计划变化清实施及测试；实施变化清测试计划及后续；仅测试计划变化保留实施；独占变化只清所属主题，共享变化才清全部主题；两种结果链接从待生成变为已生成时均不使上游计划失效 | 失效结果被错误保留、测试计划变化误清实施、独立主题被扩大清除、共享变化漏清、只显示总哈希，或任一正常结果链接转换导致测试计划或验收计划失效 | pytest JUnit XML 报告、逐文件变化类型、受影响主题列表、应用前后状态快照及两类索引的规范化哈希 |

## 2. 针对性回归范围

- 回归返回上游时追踪表只重置指定主题和真实下游列。
- 回归相邻历史工作流章节边界，确保阶段确认不会再次改坏历史记录。

## 3. 测试条件要求

- 在临时项目中构造旧顺序与双主题状态；不得修改当前活动状态作为测试准备。
- 正式执行使用 `pytest-junitxml`，迁移故障通过受控注入写入异常验证整批恢复。

## 4. 未决测试条件

暂无

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/验收实施和测试计划统一按新顺序推进_验收计划.md) | 定义新顺序、旧迁移和失效范围 |
| 上游 | [实施记录](../impl/验收实施和测试计划统一按新顺序推进_实施记录.md) | 当前路径、迁移和追踪实现依据 |
| 前置 | [验收计划覆盖完整且每项结果可判定测试计划](./验收计划覆盖完整且每项结果可判定_测试计划.md) | 先证明验收条件本身可判定 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本主题交付状态 |
| 下游 | `./验收实施和测试计划统一按新顺序推进_测试结果.md`（待生成） | 正式执行后记录结构化报告事实 |
