# 正式测试只在真实目标完整执行后通过测试计划

- 工作流编号：2026-08-07-0837-product_change
- 上游验收计划：[正式测试只在真实目标完整执行后通过验收计划](../acceptance/正式测试只在真实目标完整执行后通过_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：测试计划在实施完成后写清真实验证办法](../acceptance/正式测试只在真实目标完整执行后通过_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 测试计划完整绑定真实实施和入口](#tc-01) | 无 | 自动化测试 | `workflow gate test_plan`（测试计划程序检查） | `src/workflow_loop/artifact_validation.py::validate_test_plan_documents` | `tests/test_current_workflow_contracts.py::test_test_plan_rows_bind_real_entries_actions_and_results` | 构造已确认实施和 13 列完整测试计划，再分别删除产品入口、代码入口、测试入口、准备、动作、观察、预期、失败表现和证据字段，并加入“实施后确认” | 调用测试计划正式校验入口检查索引、实施绑定、AC 覆盖和全部测试项字段 | 校验结果中的主题、TC、字段、代码路径和实施哈希 | 完整计划通过；所有缺失和占位字段同一次结果出现；实施未确认或哈希变化时明确阻断且计划不通过 | 根据文件名猜入口、缺字段仍通过、实施未确认仍制定、占位话术未拒绝或降低 AC 边界 | pytest JUnit XML 报告、完整测试项解析结果和全部缺陷字段清单 |
| [AC-02：测试调用真实产品入口并检查入口产生的结果](../acceptance/正式测试只在真实目标完整执行后通过_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 测试正文必须调用产品入口并验证其结果](#tc-02) | TC-01 | 自动化测试 | `workflow gate test_code`（测试代码程序检查） | `src/workflow_loop/test_mapping.py::validate_workflow_test_markers` | `tests/test_current_workflow_contracts.py::test_test_code_calls_product_entry_and_asserts_observable_result` | 准备一项完整测试计划和真实产品函数，分别写真实调用测试、空函数、仅 pass、固定常量返回、恒真断言、只设置目标属性后原样断言等测试正文 | 通过测试代码门禁使用的标识校验读取测试定义范围、正文摘要和哈希，并核对计划中的入口、准备、动作、断言和证据 | 每个测试定义的正文、产品入口调用、关键断言、定义哈希和错误清单 | 只有实际调用产品入口并断言入口结果的测试通过；所有空测试和自造结果测试一次列出具体文件、定义与原因 | 只检查标识文字、空测试通过、准备阶段制造目标结果、没有产品调用或断言只重复输入值 | pytest JUnit XML 报告、测试正文 SHA-256、入口调用和关键断言证据 |
| [AC-03：结构化报告精确证明登记目标在当前代码上完整执行](../acceptance/正式测试只在真实目标完整执行后通过_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 正式通过同时要求精确目标和零跳过失败错误](#tc-03) | TC-02 | 自动化测试 | `workflow test prepare` 后执行 `workflow test run` | `src/workflow_loop/test_execution.py::run_prepared_tasks` | `tests/test_current_workflow_contracts.py::test_structured_report_requires_exact_non_skipped_current_targets` | 登记一个 pytest 精确入口和受管报告路径，构造真实通过、零执行、跳过、失败、错误、缺目标、多目标、重复目标、旧报告和代码执行中变化样本 | 运行受控测试进程并解析本次新报告，随后复核任务、机器记录和自动验收记录当前性 | 报告中的原始 nodeid、执行数、跳过数、失败数、错误数、匹配入口、报告哈希和代码哈希 | 仅真实目标集合完全相等、执行数大于零、三类异常数为零、进程成功且代码未变时建立当前成功记录；其余全部清旧成功 | 仅凭退出码通过、零执行或跳过被接受、额外测试未拒绝、报告陈旧、目标靠文件名猜测或旧成功继续有效 | pytest JUnit XML 报告本身、报告哈希与大小、精确目标、四项计数、进程退出码和代码身份 |

## 2. 针对性回归范围

- 回归 pytest 与 Vitest 两个已支持报告适配器的严格边界和未知适配器拒绝。
- 回归正式结果文档与机器记录逐字段一致，以及旧成功记录失效。

## 3. 测试条件要求

- 使用项目虚拟环境和受控临时报告目录；执行前删除旧报告，测试结束后读取新普通 XML 文件。
- pytest 报告必须由专用钩子写入原始 `item.nodeid`（pytest 唯一测试入口名）。

## 4. 未决测试条件

暂无

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/正式测试只在真实目标完整执行后通过_验收计划.md) | 定义真实测试和结构化报告边界 |
| 上游 | [实施记录](../impl/正式测试只在真实目标完整执行后通过_实施记录.md) | 测试计划、测试标识和执行器实现依据 |
| 前置 | [验收实施和测试计划统一按新顺序推进测试计划](./验收实施和测试计划统一按新顺序推进_测试计划.md) | 实施确认后才制定本计划 |
| 前置 | [实施记录只能记录真实发生的修改测试计划](./实施记录只能记录真实发生的修改_测试计划.md) | 真实实施记录是测试计划依据 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本主题交付状态 |
| 下游 | `./正式测试只在真实目标完整执行后通过_测试结果.md`（待生成） | 正式执行后记录结构化报告事实 |
