# 首次接入已有项目时功能完整且产品文档面向用户测试计划

- 工作流编号：2026-08-07-0837-product_change
- 上游验收计划：[首次接入已有项目时功能完整且产品文档面向用户验收计划](../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：每个真实入口都有功能归属或明确排除理由](../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 全部真实入口只有一个确认处理结果](#tc-01) | 无 | 自动化测试 + 人工验收 | `workflow gate project_design_init` 提交入口清单，用户核对并确认初始化范围 | `src/workflow_loop/stages/stages.py::ProjectDesignInitStage.code_validate` | `tests/test_workflow_acceptance.py::test_existing_project_entries_have_one_confirmed_scope_decision` | 建立项目设计未初始化的隔离安装项目，真实入口证据包括上传页面、搜索框和已不可达的旧版导出命令共 3 项；正确证据把前两项唯一归入“上传文档”和“搜索文档”，后一项写具体排除理由，3 项均标为已确认。错误副本同时包含未归属入口、无理由排除、重复功能名称和未确认入口，并保存 `project_design_initialized=false`（项目设计尚未初始化）。 | 自动化部分提交正确和错误两份初始化证据检查，读取一次返回的全部问题及项目状态；人工部分按代码、测试、配置和可安全运行入口的调查证据逐项核对 3 项清单，并记录对整份入口归属、排除理由和功能名称的确认。 | 门禁输出、初始化证据的入口清单与功能清单、调查证据路径、`.workflow_loop/project.json` 的初始化状态及用户确认记录。 | 正确清单恰有 3 个入口、2 个唯一功能归属、1 个带具体理由的排除项、0 个未决入口和 2 个不重复功能名称；错误副本一次报告 4 类问题且初始化状态仍为 `false`；人工记录确认调查到的入口与清单逐项相等。 | 真实入口遗漏或重复；一个入口同时归属和排除；排除理由为空；功能名称重复；未确认项被放行；错误只报一部分；检查失败后初始化状态变为 `true`；人工确认没有覆盖整份清单。 | `pytest-junitxml` 必须精确匹配本测试入口，`executed_count=1`、`skipped_count=0`、`failed_count=0`、`error_count=0`、进程退出码为 0；同时保存 3 项入口的调查证据、正确计数、4 类错误完整输出、状态值和用户整表确认记录。 |
| [AC-02：四类初始化产物使用完全相同的功能集合](../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 四类产物匹配唯一功能基准后才完成初始化](#tc-02) | 无 | 自动化测试 | `workflow gate project_design_init` 和用户确认后的 `workflow gate project_design_init --confirmed` | `src/workflow_loop/stages/stages.py::ProjectDesignInitStage.code_validate` | `tests/test_workflow_acceptance.py::test_initialization_outputs_match_one_feature_baseline_before_completion` | 建立含“上传文档”“搜索文档”两个已确认功能的隔离项目；产品总说明、2 份功能文档、代码架构 2 个完整功能段、初始化证据功能清单均使用这两个名称，产出清单含产品总说明、2 份功能文档、代码架构和初始化证据共 5 个路径。另准备分别缺功能、多功能、名称不一致、重复架构段、漏产出和缺代码过程字段的错误副本，所有副本初始状态为 `project_design_initialized=false`。 | 正确样例先执行程序门禁，核对用户确认前状态仍为 `false`，再执行确认门禁；各错误副本分别执行程序门禁并读取全部集合差异、产出差异和代码过程错误。 | 四类功能名称计数、5 项产出清单、架构段的触发和输入、具体处理、输出、失败结果和真实依据字段，门禁输出及项目初始化状态。 | 正确样例四个功能集合均精确等于 `['上传文档', '搜索文档']`，每个名称出现 1 次，产出路径数为 5，每个架构段 6 类过程信息齐全；第二道门禁后状态仍为 `false`，确认门禁后才变为 `true`。全部错误副本门禁失败并保持 `false`，输出指出具体来源、缺少项、多出项、重复项或缺失字段。 | 四类集合任一不相等仍通过；漏文件、重复功能段或不完整代码过程未被指出；错误没有具体来源和差集；第二道门禁后提前写 `true`；确认门禁通过后状态仍为 `false`。 | `pytest-junitxml` 必须精确匹配本测试入口，实际执行 1 项且跳过、失败、错误均为 0；保存四类集合、5 项路径、各错误副本的完整差异、两道门禁输出及确认前后初始化状态。 |
| [AC-03：产品文档只描述用户能够理解和检查的产品内容](../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 拒绝明确内部实现且保留用户可见文件](#tc-03) | 无 | 自动化测试 + 人工验收 | `workflow gate spec` 提交产品文档，用户核对程序无法确定的语义内容 | `src/workflow_loop/artifact_validation.py::validate_product_design_documents` | `tests/test_project_design_init.py::test_product_documents_reject_internal_references_but_allow_user_files` | 在隔离项目准备一份功能文档，先写用户上传 `.md` 格式 Markdown 文档、下载图片和查看导入文件；再加入内部路径 `app/api/chat/route.ts`、接口路由 `GET /api/internal/docs`、数据库对象 `documents.source_path` 和类函数 `SyncService.runClearAll`。另准备一句不含确定内部定位、但需要结合产品语境判断的同步描述供用户核对。 | 自动化部分先提交仅含用户可见文件的文档检查，再提交加入 4 类明确内部引用的文档检查；人工部分读取歧义原句和上下文，记录该句是否属于用户可见产品行为及保留或删除决定。 | 产品文档检查返回值和完整问题清单、被指出的原文片段、用户可见文件段落及人工语义确认记录。 | 用户可见 `.md`、Markdown、图片和导入文件描述全部通过；加入内部引用后检查失败，并在同一次输出中逐项出现 `app/api/chat/route.ts`、`GET /api/internal/docs`、`documents.source_path`、`SyncService.runClearAll`；歧义句不因单个技术词自动失败，最终处理与用户记录一致。 | 任一明确内部引用被放行；正常用户文件类型被关键词误拒绝或删去；多个内部引用只报告第一项；歧义句未经用户判断就自动定性；人工决定与最终文档不一致。 | `pytest-junitxml` 必须精确匹配本测试入口，实际执行 1 项且跳过、失败、错误均为 0；保存用户文件通过事实、4 个内部引用的同次错误清单、歧义原句与上下文及用户处理决定。 |

## 2. 针对性回归范围

- 项目设计初始化证据的入口清单、功能清单、产出文件清单及确认状态必须共同回归，避免只检查功能数量。
- 产品总说明、功能文档、代码架构功能段和初始化证据之间的集合比较必须同时覆盖成功、缺少、额外、重名和重复场景。
- 代码架构每个功能段仍须保留触发和输入、具体处理、输出、失败结果与真实代码或运行依据。
- 产品文档检查必须继续区分明确内部实现定位和用户真实操作的文件类型，不能扩大为宽泛关键词拦截。

## 3. 测试条件要求

- 自动化测试使用项目虚拟环境中的 Python 和 pytest，并由正式测试执行器生成 `pytest-junitxml` 结构化报告；每个测试项只执行本表登记的唯一入口。
- 隔离项目必须从 `project_design_initialized=false` 开始，包含可核实的代码、测试、配置和产品入口证据；测试结束要读取项目配置中的最终布尔状态。
- 初始化一致性样例固定使用 2 个功能和 5 个应有正式路径，使缺少、多出、重复和提前完成都能按数量与名称直接判断。
- TC-01 的人工验收保留整份入口调查证据和用户确认；TC-03 的人工验收保留歧义原句、上下文和处理决定。

## 4. 未决测试条件

- 暂无。两项人工核对的对象、判断问题和证据形式已明确，实际确认结论在主题验收时记录，本阶段不提前填写。

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [首次接入已有项目时功能完整且产品文档面向用户验收计划](../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收计划.md) | 本测试计划逐条覆盖的 AC-01 至 AC-03 |
| 上游 | [实施记录](../impl/首次接入已有项目时功能完整且产品文档面向用户_实施记录.md) | 测试入口、代码入口和可观察状态来自已确认实施 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本轮完整交付关系和状态 |
| 下游 | `./首次接入已有项目时功能完整且产品文档面向用户_测试结果.md`（待生成） | 正式主题测试执行后记录结构化报告事实 |
| 下游 | `../acceptance/首次接入已有项目时功能完整且产品文档面向用户_验收结果.md`（待生成） | 自动化和人工证据齐全后逐条记录主题验收结果 |
