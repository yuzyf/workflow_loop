# 正式文档链接可导航且历史坏链接受控修复测试计划

- 工作流编号：2026-08-07-0837-product_change
- 上游验收计划：[正式文档链接可导航且历史坏链接受控修复验收计划](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：已生成正式文档的本地链接真实可导航](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 两道门禁重新检查全部正式链接](#tc-01) | 无 | 自动化测试 | `workflow gate acceptance_plan` 和 `workflow gate acceptance_plan --confirmed` | `src/workflow_loop/cli.py::validate_stage_output` | `tests/test_workflow_acceptance.py::test_formal_link_gate_rechecks_navigation_and_lists_all_failures` | 建立两个隔离安装项目。有效项目处于 `acceptance_plan`（验收计划）环节，受管文档含 1 个指向现有文件且命中唯一显式定位编号的链接；无效项目含缺失文件、缺失定位、重复定位、项目越界和符号链接共 5 个独立问题，并保存执行前阶段和门禁状态。 | 有效项目依次执行第二道门禁和用户确认后的第三道门禁；无效项目执行第二道门禁。读取每次命令输出及执行后的工作流状态。 | 命令标准输出、`.workflow_loop/state.json` 中当前环节、`code_validated`（程序校验是否通过）和 `user_confirmed`（用户是否确认）字段，以及 5 个问题对应的来源文档、行号、原链接、目标和原因。 | 有效项目第二道和第三道门禁都报告链接问题数为 0，第三道门禁后进入下一环节；无效项目一次列出 5 个问题，当前环节仍为 `acceptance_plan`，`code_validated=false` 且 `user_confirmed=false`。 | 任一有效链接被拒绝；第三道门禁未重新检查；5 个问题少报、分次报告或缺来源、行号、原链接、目标、原因；无效项目仍推进环节或留下通过状态。 | `pytest-junitxml`（pytest 的 JUnit XML 结构化报告）必须精确匹配本测试入口，`executed_count=1`、`skipped_count=0`、`failed_count=0`、`error_count=0`、进程退出码为 0；同时保存两次有效门禁输出、一次无效门禁完整问题清单和执行前后状态字段。 |
| [AC-02：尚未生成的未来产物不形成假链接](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 未来产物只接受待生成普通路径](#tc-02) | 无 | 自动化测试 | `workflow gate acceptance_plan` 提交含未来产物位置的验收索引 | `src/workflow_loop/topic_relations.py::read_topic_index` | `tests/test_workflow_acceptance.py::test_future_artifacts_are_pending_plain_paths_until_created` | 在隔离项目建立真实存在的验收计划，并分别准备 1 个正确索引和 5 个错误索引：可点击的缺失目标、错误固定路径、遗漏“待生成”、把必有验收计划写成待生成、目标已存在仍写待生成。正确索引把尚不存在的验收结果写为反引号普通路径加“待生成”，执行前确认结果文件不存在。 | 对 6 个样例分别提交索引检查；检查解析结果、完整错误输出和项目文件清单，确认程序没有创建未来结果文档。 | `read_topic_index()`（读取主题索引函数）的返回关系、门禁错误清单、对应索引单元格及未来结果文件路径。 | 正确样例返回固定路径 `./安装_验收结果.md` 且结果文件仍不存在；5 个错误样例全部失败并分别指出原单元格、预期路径或已存在状态；全部样例执行后新增占位结果文件数为 0。 | 正确普通路径被拒绝；任一假链接、错误路径、无状态路径、必有上游待生成或已存在目标待生成被放行；程序为通过检查创建空文件；错误没有定位到具体单元格和实际值。 | `pytest-junitxml` 必须精确匹配本测试入口，实际执行 1 项且跳过、失败、错误均为 0；保存 6 个样例的解析或错误事实、执行前后文件清单，以及“未来结果文件不存在”的路径检查。 |
| [AC-03：历史坏链接只按确认过的准确范围修复](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 更新与已确认链接修复范围严格分离](#tc-03) | 无 | 自动化测试 + 人工验收 | `workflow update` 完成项目更新后单独执行 `workflow repair-links`，用户确认预览，再使用该预览的 64 位哈希执行 `workflow repair-links --apply` | `src/workflow_loop/cli.py::cmd_repair_links` | `tests/test_workflow_acceptance.py::test_update_and_confirmed_link_repair_keep_exact_scope` | 建立可离线更新的已安装项目，准备 2 份正式文档：1 个能唯一对应旧标题的标准坏链接和 1 个目标不唯一的手写链接；保存正式文档、项目配置和工作流状态的 SHA-256 内容哈希。另保留一份在预览后修改来源文档的漂移样例。 | 自动化部分依次执行受控项目更新、只读修复预览、取消后内容核对、使用已经漂移的旧预览哈希尝试应用、重新预览并应用新哈希；人工部分逐项核对预览中的来源、原链接、目标位置、可自动修复项和不可自动修复项后记录确认决定。 | 每次命令输出、预览哈希、正式文档和状态文件的 SHA-256、实际修改文件清单、不可自动修复清单及人工核对记录。 | 项目更新、预览和取消均使正式文档修改数为 0；预览列出 1 个确定修复和 1 个不确定项；漂移后的旧哈希应用失败且整批写入数为 0；新哈希只修改确认清单中的 1 个目标，不确定项原文、项目配置、当前环节和门禁状态不变；人工记录逐项确认预览范围。 | 更新过程改写正式文档；预览前发生写入；取消或漂移后任一文件改变；应用修改数不是 1、改动不确定项或超出预览；项目状态被推进；人工确认记录缺少任一来源、原链接、目标或决定。 | `pytest-junitxml` 必须精确匹配本测试入口，实际执行 1 项且跳过、失败、错误均为 0；保存更新前后、取消前后、漂移失败前后的 SHA-256，对应命令完整输出、最终修改清单，以及用户签认的逐项预览记录。 |
| [AC-04：历史链接修复要么全部成功要么恢复原文](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-04) | <a id="tc-04"></a>[TC-04 整批修复失败或中断后恢复全部原文](#tc-04) | 无 | 自动化测试 | 使用已确认预览哈希执行 `workflow repair-links --apply` | `src/workflow_loop/markdown_links.py::apply_legacy_anchor_repairs` | `tests/test_workflow_acceptance.py::test_link_repair_completes_atomically_or_restores_every_original` | 在三个隔离目录各准备 2 个可确定修复目标并保存 2 个原文 SHA-256：成功样例正常写入，失败样例在替换第 2 个文件时注入写入错误，中断样例在写入第 1 个文件后触发进程中断并保留可恢复事务资料。 | 三个样例都从当前预览生成确认哈希并执行整批应用；失败样例捕获错误后核对两份文件；中断样例调用恢复入口两次，核对恢复的幂等结果；最后重新扫描链接并检查事务目录。 | 两个目标文件内容和 SHA-256、`LinkRepairResult`（链接修复结果）、完整错误明细、重新扫描问题数及 `.workflow_loop/link_repair` 事务目录。 | 成功样例修改 2 个目标、扫描问题数为 0 且事务目录不存在；失败样例两份文件哈希都等于执行前值并明确报告“已恢复全部原文”；中断样例首次恢复后两份文件哈希等于执行前值，第二次恢复修改文件数为 0，事务目录不存在。 | 成功后仍有候选或新增坏链接；失败或中断留下任一文件为新旧混合内容；恢复后哈希不等；重复恢复再次改文件；事务目录无说明残留；存在未恢复文件却报告成功。 | `pytest-junitxml` 必须精确匹配本测试入口，实际执行 1 项且跳过、失败、错误均为 0；保存三个子场景的预览哈希、两文件执行前后 SHA-256、修复或恢复结果、扫描计数和事务目录存在性。 |

## 2. 针对性回归范围

- Markdown 正文解析仍须忽略代码块、行内代码和外部网址，避免把示例文字当成本地坏链接。
- 主题索引仍须保持稳定主题文件标识、展示顺序和前置关系，待生成语法不能放宽这些约束。
- 项目更新仍只覆盖静态管理材料并保留正式产物、运行资料和当前工作流状态。
- 历史修复的预览哈希、原子替换、失败恢复和中断恢复必须共同回归，避免只验证成功路径。

## 3. 测试条件要求

- 自动化测试使用项目虚拟环境中的 Python 和 pytest，并由正式测试执行器生成 `pytest-junitxml` 结构化报告；每个测试项只执行本表登记的唯一入口。
- 测试数据全部位于 pytest 的隔离临时目录，不读取或改写当前仓库正式文档，不访问真实 PyPI 或 GitHub；更新场景使用本地受控发布资料。
- 失败注入只替换隔离项目中的文件写入动作，必须仍调用真实预览、应用和恢复代码，并核对操作后的文件哈希和事务目录。
- TC-03 的人工验收需要保留程序输出的完整预览清单，由用户逐项确认来源、原链接、目标、自动修复判断和不可自动修复原因。

## 4. 未决测试条件

- 暂无。TC-03 的人工核对对象、确认内容和自动化证据已明确，实际确认结论在主题验收时记录，本阶段不提前填写。

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [正式文档链接可导航且历史坏链接受控修复验收计划](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md) | 本测试计划逐条覆盖的 AC-01 至 AC-04 |
| 上游 | [实施记录](../impl/正式文档链接可导航且历史坏链接受控修复_实施记录.md) | 测试入口、代码入口和可观察状态来自已确认实施 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本轮完整交付关系和状态 |
| 下游 | `./正式文档链接可导航且历史坏链接受控修复_测试结果.md`（待生成） | 正式主题测试执行后记录结构化报告事实 |
| 下游 | `../acceptance/正式文档链接可导航且历史坏链接受控修复_验收结果.md`（待生成） | 自动化和人工证据齐全后逐条记录主题验收结果 |
