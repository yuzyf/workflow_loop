# 项目长期记忆

## 待办（用户已确认方向，等时机执行）

1. **表模式"AI 只填内容、不碰格式"改造**（2026-09-07 确认，2026-09-08 对抗审查后升为方案主线）
   - 背景：kg-app journal 分析发现，表模式时代反复的主因是 AI 在手抄机器事实（行号 L7、L49-L50 格式被拒、RUN 编号过期、表版本 2/3 不符、update_code_design 38 连败的代码映射格式）。
   - 方向：机器能算的数据由代码直接采集进表——修改文件+行号←git diff、RUN/REG 编号←state、表版本←程序元数据、上下游链接←环节状态、设计同步代码映射←tree-sitter 当库集成（2026-09-08 新增，不用 MCP：无固定 token 税、输出格式程序可控、无 INFERRED 推断错误）。
   - AI 只填自由叙述（修改理由、验收语义、未决问题），不带任何格式要求。
   - 同轮做的还有：程序合成环节任务书（环节+主题+上游结论+表位置，几 KB 封顶）——它同时是缓存和 subagent 两个加速器的输入前提，拱心石地位。
   - 实施方式：起 product_change 轮次，与待办 2 同轮设计。

2. **全量验收增加可跳过步骤**（2026-09-07 用户提出）
   - 用户原话："另外要补一个全量验收要加一个可以跳过步骤在加上面的修改"
   - 具体跳过什么、跳过条件，开工时需要先跟用户确认清楚再设计。

3. **token 根治：主线+加速器结构**（2026-09-08 第一性原理重推+对抗审查后定稿）
   - 用户三条硬诉求：可靠（产物可验证）、便宜（成本不随历史超线性涨）、省心（用户操作习惯不变，硬约束）。
   - 主线（必做）：机器采集事实 + 程序合成任务书——一味药同时治抄写输出、重试浪费、读规范三块，压倒性第一。
   - 加速器（按实测二选一或都用）：前缀缓存（**未验证 glm-5.3 端点是否支持，一条 curl 可测，优先测**）；subagent 拆执行（激活条件=实测显示历史累积占大头）。
   - 外围（降级）：知识图谱 MCP 不装——用户流程计划先行、翻码场景少，MCP 挂着收 1-2k token 固定税，tree-sitter 进主线后更没必要。
   - 执行顺序：① 10 分钟缓存验证；② product_change 同轮做主线+待办2；③ 实测下一轮 token 拆账（四块：历史累积/材料重读/门禁重试/AI 抄写）定 subagent 去留。

## 已完成（2026-09-10 收工，轮次 2026-09-09-0901-product_change，提交 28afddb 未推送）

1. **四个门禁痛点修复**（用户 2026-09-09/10 提出，全部验收通过）：
   - 临时产物不再卡实施门禁：Git 清单白名单化（与观察快照同源）、草稿目录豁免、project.json 的 extra_code_suffixes 项目级扩展、疑似临时产物提示不阻塞（R34/R35）。
   - 删除文件不再阻塞基线准备：已提交文件用进场快照哈希 Git 自证；从未提交文件登记无副本放行；作废预检输出无副本清单（R36/R22）。
   - 退回核对无需修改有合法出口：五张主题级表+product_features 加"核对结论"声明行；有效声明过门 2、缺依据拒绝、产物变化不走声明；生成文档渲染核对记录（R47/R30）。
   - 穿刺跳过与复用分开：--skip 有本轮资产先警告再确认、--reuse 带依据必填、连坐拆除、spike 材料输出决策树（R30-R32）。
2. **质量证据**：17 项主题测试全过；628 项全量回归通过（用户 Terminal 执行；沙箱内 14 个失败经 stash 双向对照证实为 shim 环境问题）；架构文档 6.7/6.9/6.13 同步。
3. **本轮验证的机制**：QA 修产品代码自动退回 impl 补记录（流程自我监督真实生效）；QA 测试抓出 2 个实现缺陷（自证传参、spec 无主题级表）。

**遗留待办（合并更新）**：① 程序合成环节任务书（主线第二半）；② 前缀缓存验证（glm-5.3 curl 一条可测）；③ token 拆账定 subagent 去留；④ ~~核对无需修改出口~~（本轮已完成）；⑤ 编辑归程序管章节的事前拦阻；⑥ ~~Git 自证放行真实场景~~（本轮已真实触发放行分支）；⑦ R47 声明的操作路径提示（spec 退回核对时 AI 怎么知道填 product_features 表）可考虑进决策树提示，未验证。

## 已完成（2026-09-08 收工，轮次 2026-09-08-0228-product_change）

1. **表模式"AI 只填内容、不碰格式"改造**——主题 A 完成：machine_collect.py（差异事实/采集写表/进场基线提交号/验收五列取值/编号校验/标识生成）、表版本 v4（采集指纹键+五列程序回填）、workflow collect 命令、门禁指纹比对、标识生成与解析同源、穿刺列回填。598 项测试全绿。**tree-sitter 代码映射未做**（归 update_code_design 改造，仍是待办）。
2. **全量验收可跳过**——主题 B 完成：should_skip_final_regression（差异全空才跳过、无法确认一律运行、依据写入 state）；跳过轮次设计同步免 REG 编号。
3. **门禁失败输出自修复**——主题 C 完成：退回时序提示、基线四项事实、表文档不一致两路径区分（当天三次真实触发并按指引修复）。
4. **计划外**：test_result 空表守卫（required_at_gate=True，R11 违规修复）；R26 验收记录程序建立首次真实工作。

**遗留待办（下轮）**：① 程序合成环节任务书（原待办 3 主线第二半）；② 前缀缓存验证（glm-5.3 端点 curl 一条可测）；③ 下一轮 token 拆账定 subagent 去留；④ "核对无需修改"在 product_change 门 2 无出口（stages.py:197，R46 只加了报错事实）；⑤ 编辑归程序管章节的事前拦阻；⑥ 实测下一轮 Git 自证放行的真实场景（1207 轮只触发了拒绝分支）。

## 已完成（2026-09-08 第二轮，轮次 2026-09-08-1207-product_change，20:45 收工）

1. **test_result 表三列机器采集**——编号/结论/机器编号由程序回填（回填先于空表守卫，qa 实测生效）。上轮"第五处手抄"根治。
2. **代码符号自动定位**——code_symbols.py（ast，零新依赖）；设计同步门禁自动核对 文件::符号 引用并给相近符号建议。38 连败病根根治。
3. **prepare Git 自证出口**——先写代码后准备基线：快照=HEAD 用 HEAD 字节副本放行、新文件放行、不可自证按文件拒绝。真实触发过拒绝分支（快照中间态）。
4. **中途加主题死循环修复**——current_workflow_topics/sync/确认门三处合并 topic_relations 表主题。
5. **回填顺序教训**：机器采集类回填必须在空表守卫之前（回填后非空，守卫只拦真正无内容表）。
612 项测试全绿。git 六个提交全部落库。

## 项目约定

- 本项目由 workflow_loop 管理，需求一律走 workflow start → 推荐路线 → 用户确认 → 执行。
- 改动 src/workflow_loop/ 产品代码必须走完整研发路线（product_change/bugfix）。
- 上一轮进行中的轮次要先收工（workflow done）或作废，再开新轮。

## 已知机制问题（分析结论，2026-09-07/08）

- 填写说明与门禁校验是两个来源、靠人肉同步（BUG-01 模式的洞没堵完：L 格式、表版本、占位词白名单残留）。
- refresh_references_after_removal 异常被静默吞掉（verification.py:3209），删文件后链接回补失败不吭声，门禁把程序自己写的旧链接报成错误。
- update_code_design 环节 38 连败本质也是"AI 手抄机器事实"（代码位置/验证位置格式），归入机器采集待办一并治。
- 时序约束靠 AI 记性搬运不可靠：约束写在 600 行规范中段，动作发生时 AI 眼前是退回输出——2026-09-08 spec 重走两次踩坑实证。退回时序提示已修（返回上游 R25）；根本治法是程序合成任务书（正确时机推正确约束）。
- 编辑归程序管的章节无事前拦阻（如产品总说明第 7 章）：手改后报错，但提示不区分"只改文档"和"表也改了"。报错质量已修（推进 R46）；事前拦阻涉及编辑器交互，未做，下轮评估。
- "退回后核对无需修改"场景在 product_change 轮次门 2 无出口（代码推演：changed_stage_paths 返回空清单时 product_changed 恒 False 必报错）——与 R25 新规"不要求制造无意义修改"存在张力。2026-09-08 对抗审查核对：R46 只增强了报错事实、未改变判定，该缺口仍在，修复属下轮范围。
- 2026-09-08 对抗审查教训：机器采集的生成格式与门禁校验格式必须同源（新文件位置值、多块行号格式都曾两边各写各的）；_texts_from_manifest 返回三元组 (entry, before, after)，解包时 entry 是 dict，容易解错——已在实施记录留档。
- 全量测试在本 WorkBuddy 环境有 15 项固定失败（沙箱 shim 拦 mkdir exist_ok 和嵌套 pytest 子进程），git stash 对照已证明与代码无关；跑全量用 TMPDIR + --basetemp 绕过临时目录问题，mkdir 类失败只能换环境。
- **2026-09-08 实测升级版结论**：shim 白天行为变化后统一入口在本环境直接 532 errors（连 mkdir(parents=True, exist_ok=True) 都拦）。最终全量回归的可靠做法：让用户在自己的 Terminal（无 shim 注入）执行 `cd <项目根> && TMPDIR=/tmp/wfinherit /Users/yu/.local/bin/workflow gate regression_test`，本轮实测 596 passed in 48.07s。shim bug（mkdir 不认 exist_ok=True）已整理复现材料给用户反馈 WorkBuddy。
- 详见工作日志 2026-09-07.md、2026-09-08.md。
