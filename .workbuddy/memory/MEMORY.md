# 项目长期记忆

## 待办（用户已确认方向，等时机执行）

1. **表模式"AI 只填内容、不碰格式"改造**（2026-09-07 确认方向）
   - 背景：kg-app journal 分析发现，表模式时代反复的主因是 AI 在手抄机器事实（行号 L7、L49-L50 格式被拒、RUN 编号过期、表版本 2/3 不符）。
   - 方向：机器能算的数据（修改文件+行号←git diff、RUN/REG 编号←state、表版本←程序元数据、上下游链接←环节状态）由代码直接采集进表，AI 只填自由叙述（修改理由、验收语义、未决问题），不带任何格式要求。
   - 优先级：git diff 自动算行号 > 链接按状态生成（同时治"删文件后逐环节炸"的回补问题）> 表版本自动写 > RUN 编号通道。
   - 实施方式：起 product_change 轮次。

2. **全量验收增加可跳过步骤**（2026-09-07 用户提出）
   - 用户原话："另外要补一个全量验收要加一个可以跳过步骤在加上面的修改"
   - 具体跳过什么、跳过条件，开工时需要先跟用户确认清楚再设计。

3. **token 根治：先实测再定主刀**（2026-09-07 定，用户质疑后修正）
   - 背景：整轮流程在一个长会话里滚，成本随环节超线性涨；GitHub 调研（OpenViking/Symphony/Anthropic harness/Claude Code/aider）没有单一"最佳方案"，是一组组合拳。
   - 完整手段清单及状态：状态文件化（已有）；subagent 换新上下文（候选）；分级加载/精简任务书（候选）；稳定前缀缓存（**未验证用户模型是否吃缓存**）；模型分档——机械活便宜模型判断活贵模型（漏过，要补）；aider 式仓库地图+diff 读代码（漏过，实施环节可用）；压缩代理（不适用，走 WorkBuddy 不走自建网关）。
   - 关键教训：token 花在哪没实测过，不能拍脑袋定方案。四块拆分：历史累积 / 材料重读 / 门禁重试（历史 319 次失败，嫌疑最大）/ AI 抄写。
   - 执行顺序：① 等当前轮次走完，实测一轮把四块拆出来（会话用量+journal 命令次数+文档体积）；② 按数据定主刀和 subagent 粒度；③ 模型分档做乘数。

## 项目约定

- 本项目由 workflow_loop 管理，需求一律走 workflow start → 推荐路线 → 用户确认 → 执行。
- 改动 src/workflow_loop/ 产品代码必须走完整研发路线（product_change/bugfix）。
- 上一轮进行中的轮次要先收工（workflow done）或作废，再开新轮。

## 已知机制问题（分析结论，2026-09-07）

- 填写说明与门禁校验是两个来源、靠人肉同步（BUG-01 模式的洞没堵完：L 格式、表版本、占位词白名单残留）。
- refresh_references_after_removal 异常被静默吞掉（verification.py:3209），删文件后链接回补失败不吭声，门禁把程序自己写的旧链接报成错误。
- 详见当日工作日志 2026-09-07.md。
