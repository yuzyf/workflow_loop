# 2026-09-02 工作日志

## Run 2026-09-01-1141-bugfix「退回验收计划后新增主题能走通门禁与追踪表」完成收工（15:43）

- 修复 10 个退回重走路径缺陷：BUG-01 验收计划主题合并死锁（topic.acceptance_topics 集中主题源）、BUG-02/07 追踪表按主题/按 AC 补行、BUG-03 scaffold 与表同步主题源、BUG-04 同一 Run 重进 impl 继承首次入场基线（meta.impl_entry_baseline 锚点）、BUG-05 复用既有实现说明豁免、BUG-06 reproduce --confirmed 注册幂等、BUG-08 spike --skip 回补穿刺列、BUG-09 引用回补缺 wf_state 抹掉机器事实、BUG-10 失效检查登记基线×完整观察范围错配致幻影 added 卡死收工。
- 改动 7 个源文件 + tests/test_return_rework_flow.py（21 条带 Workflow-Test 标识）；全量回归 511 通过（REG-20260902T073928+0000-9800a09d）。
- 本 Run 完整走了三遍链：reproduce→spike→acceptance_plan→impl→qa→topic_acceptance→regression_test→overall_acceptance→update_code_design→done。

## 环境要点（重要）

- WorkBuddy 沙盒 shim 经 PYTHONPATH 注入所有子 python 进程，会拦截 pytest 临时目录 mkdir（EEXIST 假 PermissionError）并让 workflow repair-links 卡死在 broker 管道读取。
- 在本项目跑测试/workflow 命令的干净环境：`env -u PYTHONPATH -u CODEBUDDY_SESSION_ID -u CODEBUDDY_BROKERED_FS_HOOK_ENABLED -u CODEBUDDY_SAFE_DELETE_SANDBOX`。qa 测试登记命令已把 env -u 前缀写进参数数组（命令数组直接 execve，合规）。
- 收工前 12 项测试失败（test_test_execution/test_verification/test_current_workflow_*）全部为该 shim 环境问题，stash 基线对照确认零回归。

## 流程内踩坑记录

- impl --confirmed 之后不能再动产品代码：任何微调都触发失效退回（本 Run 因此多走一遍）。对抗审查要在确认前做完。
- bug 表「修复与验收结果」程序追加的结论块在 return 后引用已删文档，repair-links 判不可自动修复——处理方式是清表栏位后按 _bug_defect_documents 重建。
- 编辑 spec/代码架构设计.md 表格时不能在表中间留空行：registered_code_design_paths 按连续 | 行扫描，断行会把后续登记行整体丢掉（曾致 test_entry.py 假 deleted）。
- update_code_design 阶段修改的架构文档「代码位置」列必须是完整相对路径，否则产出校验报映射无效。

## 待办

- git 提交本轮改动（等用户确认完成度）。
