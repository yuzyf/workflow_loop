
## Git 提交（light_task 2026-09-08-0120）

- 将上一轮 product_change（2026-09-07-0700）全部成果提交为本地提交 29aa07b，共 135 个文件：5 个缺陷修复、design_sync 新模块、6 个新测试、配套文档与轮次记录。
- 按用户选择只 commit 未 push；本地领先 origin/main 两个提交（f35d6fb、29aa07b），等后续发布或推送时机。

## 第一性原理重推诉求 + GitHub 方案对抗性审查（用户发起）

用户要求把他的所有诉求用第一性原理重新推一遍，并对 GitHub 所有省 token 方案做对抗性审查。

### 用户三条硬诉求（从历史对话提炼）

1. 可靠：产物可验证、可回溯（workflow_loop 的初衷）
2. 便宜：token 成本不随项目历史超线性涨
3. 省心：用户操作习惯不变（"对使用者太复杂了"的否决是硬约束）

token 账两类：必要的活（写代码/判断叙述）vs 流程开销（懂规则/抄事实/修报错/背历史）。方案裁判标准：砍开销不砍活。

### 对抗性审查结论（逐个质询）

- spec-kit：反面教材，只提供教训（3-5 倍开销被骂）。
- OpenViking：偷思想不装软件——"表填写说明即 L1，不再读 L2 全文规范"一行约定即可。
- Symphony：全无人值守不适用（有三道用户门）；该偷的是"程序渲染任务书"。
- Anthropic harness：思路对；"手动新会话"已被否决，subagent 是自动替身，待实测。
- Claude Code 缓存：零开发大杀器，但 glm-5.3 端点是否支持未验证（全盘最大假设，一条 curl 可测）。
- 知识图谱三件套（codebase-memory-mcp/Graphify/code-review-graph）：**高估了**。用户流程计划先行，AI 大规模翻码场景少；MCP 固定收 1-2k token 税；INFERRED 动态语言会错。降为外围，基本可不装。
- Headroom：不适用（不走自建网关），维持排除。
- 分层记忆派：需求已被状态文件化覆盖。

### 审查挖出的新结论

1. 机器采集是乘法项（一味药治抄写+重试+读规范 2.5 块开销），优先级压倒性第一——之前四味药平行摆是错的。
2. update_code_design 38 连败也是"AI 手抄机器事实"（代码位置/验证位置格式），归入机器采集；用 tree-sitter 当库集成进 workflow_loop，不用 MCP（无固定税、格式可控、无推断错误）。
3. 程序合成任务书是拱心石：subagent 和缓存两个加速器都以它为输入前提。

### 最终结构（已更新 MEMORY.md）

- 主线（product_change 同轮必做）：机器采集事实（含 tree-sitter 代码映射）+ 程序合成任务书 + 全量验收可跳过。
- 加速器（实测二选一或都用）：前缀缓存（先 10 分钟验证 glm-5.3）；subagent 拆执行（实测历史累积占大头才激活）。
- 外围（降级不装）：知识图谱 MCP。
- 执行顺序：① 缓存验证 → ② product_change 主线 → ③ 下一轮实测 token 拆账定 subagent。

当前轮次状态：2026-09-07-0700 已在今日凌晨走完（qa 环节后用户已确认成果并提交 29aa07b）——待办启动的"等当前轮次走完"条件已满足。

## product_change 2026-09-08-0228：表模式机器采集 + 全量回归跳过

### spec 环节（已过三道门，含一次返回）

- 两个功能文档落地 R25-R29（机器采集实施改动/验收结果/编号引用/测试标识/穿刺资产）和 R9/R9a（全量回归跳过），表版本升 4。经三轮对抗审查修订。
- **spike 调查中挖出 R25 缺口并返回 spec 补齐**：R25 只写了计划外"新建"文件，漏了计划外"修改"已有文件——回退基线只备份代码计划登记路径（rollback.py:2046），`_trusted_git_head_baseline` 要求文件无未提交差异、刚改过的文件必然不满足（rollback.py:2009-2016），导致取不到改动前内容算不出行号。
- **实测验证的解法**：进场时记录 Git 基线提交号（只在进场时代码范围无未提交改动时记录），采集时 `git show <基线提交号>:<文件路径>` 取进场内容。用本仓库真实改动文件验证：现有机制返回 None，基线提交号方案拿到内容且行号（L21-L23、L79-L83、L118）与真实改动吻合。
- spike 判定结论：本轮改动所需机器能力（找差异文件、算行号、分产品/测试代码、验收五列回填、测试标识校验）全部能从现有代码确认，**无需穿刺项**。
- 产品总说明补了第 9 章本轮修改记录条目（此前遗漏）。

### 追加最小修（用户拍板：退回时序提示 + 报错带事实）

- 起因：我在 spec 重走时两次踩门禁坑（详见下），用户追问"为啥会犯错、项目本身有没有问题"，确认后追加两处修复进本轮范围。
- 功能_返回上游重新处理.md 新增 R25：退回成功输出必须写明时序——先过第一道门（--discuss-done）再改产物文件；核对无需修改时不要求制造无意义修改。
- 功能_推进并确认工作环节.md 新增 R46（后扩展）：基线对比类失败带基线拍摄时间/基线哈希/当前哈希/对比结论；文档与表不一致类失败写明差异章节块和两个指纹，按事实区分修复路径（表未更新→写回表；表已更新仅文档手改→恢复文档由程序按表重写），不笼统归"写回表"。
- product_features 表补两行（返回上游重新处理、推进并确认工作环节），第 7 章由程序按表生成。

### 我踩的两个坑（分析根因，对后续有参考价值）

- 坑 1：退回后先改文档、后登记 --discuss-done。--discuss-done 会拍产物基线快照，门 2 要求"快照之后文档有变化"，我的改动被拍进快照导致报"产物未更新"。根因：时序约束埋在 600 行规范中段，动作发生时眼前是退回输出（只说"核对、不一致就改"），靠 AI 记性搬运约束不可靠。
- 坑 2：手改产品总说明第 7 章。表模式下第 7 章归程序生成（product_features 表），手改导致指纹对不上报"文档被直接修改"。当时报错说"把改动写回表"但表里已写好——报错区分不了"只改文档"和"两个都改"，修复路径误导，已并入 R46。
- 教训：产品总说明是混合所有权文件（第 1-6、8、9 章 AI 手写，第 7 章程序生成）；表模式下"AI 只填内容不碰格式"原则同样适用于"哪些内容归程序管"。
- 未进本轮的遗留：编辑时事前拦阻（改归程序管的章节时提示）没做，涉及编辑器交互超出最小修范围；记入 MEMORY 已知问题。

## acceptance_plan 环节（第二道门已过，等用户确认第三道门）

- 三主题（全部前置"无"）：A 机器事实由程序采集进工作记录表（AC-01~06 对应 R25-R29+表版本升4）；B 无代码改动轮次跳过全量回归执行（AC-01~03 对应 R9/R9a）；C 门禁失败输出支持自修复（AC-01~03 对应返回上游 R25+推进 R46）。
- 流程小坑（未造成门禁失败）：填完 topic_relations 后 discuss 不会自动生成各主题 acceptance_plan 表（ensure_stage_tables 读索引，索引本轮章节还没写入），需用 `workflow scaffold acceptance_plan --topic <主题名>` 逐主题生成。
- 三张 acceptance_plan 表逐列填写（六列字段+叙述栏），第二道门一次通过，程序生成三份验收计划、索引本轮章节、追踪表 12 条 AC 行。凭据 254cd6fb...849ba2。

## impl 环节（第二道门已过，等用户确认代码结果）

- 代码计划 7+4+4 步（三主题），全部实施完成：新增 machine_collect.py（601 行，R25-R29 五件套核心）、records.py 表版本 v4+五列回填+R46 报错、rollback.py 进场基线提交号+指纹校验、cli.py collect 命令+回归跳过分支+退回时序提示、test_runner.py should_skip_final_regression、state.py 跳过字段、test_mapping.py build_marker_from_plan、traceability.py 穿刺列回填、stages.py 基线事实报错。
- 新测试 30 项（machine_collect 17 + regression_skip 7 + gate_self_heal_output 6）全过；受影响既有测试 112 项全过零回归（3 处报错文案断言更新）。
- 实施中发现并修正：AcceptanceCriterionRecord 的 test_ids（TC）与 test_record_ids（RUN）易混字段——机器测试记录编号列必须用后者；验收方式枚举是"自动化测试"不是"自动化"。
- impl 门禁第一次失败 28 项全是填表格式问题（多块行号顿号连接不被 v3 单块格式接受→拆动作行/收敛实际修改行；tests/ 文件不登记实际代码修改是项目惯例；未决问题空→写暂无；架构文档 6.18/6.3 缺显式锚点→repair-links 自动修复）。这正是 R25"AI 手抄机器事实"的现场重演——v4 机器采集上线后这类问题消失。
- 第二次门禁通过，凭据 36d98b7b...c03587。
- 开发检查环境：WorkBuddy 沙箱 shim 拦 pytest 默认临时目录，TMPDIR + --basetemp=/tmp/pytest-basetemp 绕过。

## 对抗性审查（用户要求，impl 第二道门后做）

### 审查发现并修复的真问题（2 个）

1. **解包顺序错误**（严重）：machine_collect._entry_text_or_none 把三元组 (entry, before, after) 的 entry 字典解给了 before，导致门禁指纹校验链路崩溃、全量测试 35 项失败。修正后 test_rollback.py 41 项恢复全过。
2. **采集位置值与门禁行号格式不同源**（会导致 v4 轮次采集结果被门禁拒绝）：新文件原先写文字"新增文件，全部行均为新增"、多块差异用顿号连接，均不被 _parse_recorded_line_range 单块 L 范围接受。改为：新文件 L1-L行数、多块写覆盖全部差异的连续范围（模板允许）。补 2 项同源回归测试（test_machine_collect 19 项全过）。这正是"生成与校验必须同一套格式逻辑"的教训——两边各写各的就会打架。

### 审查确认无遗漏的项

- MEMORY 待办 1（机器采集主线）：R25-R29 全落地，tree-sitter 代码映射除外（当时就定在 update_code_design 改造做，本轮范围外）。
- 待办 2（全量验收可跳过）：R9/R9a 落地。
- 待办 3（任务书/缓存/subagent）：按既定执行顺序属后续轮次，本轮不涉及。
- 已知机制问题四条：时序提示（已修）、报错质量（已修）、事前拦阻（下轮）、stages.py:197 无出口（注意——本次审查核对了现状代码：R46 只给报错加了事实，"核对无需修改"仍无出口，但该张力点的修复在验收 C 主题范围外，已记 MEMORY）。
- 环境判定：全量测试 15 项失败经 git stash 对照证明全部为 WorkBuddy 沙箱 shim 环境问题（mkdir exist_ok 误报 EEXIST + 嵌套 pytest 子进程被拦），与代码改动无关。相关测试在干净环境可跑。

### 审查后状态

- impl 第二道门重新通过，凭据 d44e0e51...ca162f1。等待用户确认代码结果后进 qa。

## qa 环节（第二道门已过，等用户结束确认）

- 三张 test_plan 表填 14 个 TC（A 8 项、B 3 项、C 3 项，全自动化），正式目标名称用 JSON 数组支持一 TC 多测试入口。
- 测试执行踩了三个环境坑（都属临时环境问题，按规范留在 QA 内修正后重试，未返回上游）：
  1. 裸 pytest 不在 PATH → 命令改 .venv/bin/python -m pytest
  2. WorkBuddy shim 拦受控子进程的 /private/var/folders 默认临时目录 → 命令加 --basetemp
  3. 多 TC 共用同一 basetemp，第二个 TC 启动时清空目录被 safe-delete 拦截 → 每个 TC 独立 /tmp/wf-test-tmp/<TC编号>
- 最终 `workflow test run`：14 个测试项全部 passed（真实机器执行，pytest-junitxml 报告）。RUN 编号存 state 可查。
- qa 第二道门通过，凭据 59cb624e...6690d0。R28 标识输出在登记时正常工作（每个 TC 的标识文本由程序生成输出）。

## topic_acceptance 环节（第二道门已过，等用户确认）

- 程序自动建立全部 12 条验收记录（3 主题 × 4/3/3 AC 全 passed，RUN 编号绑定）——R26 机制首次真实工作。
- 踩一个小坑：本轮是 v3 表，五列程序回填是 v4 机制，v3 路径机器编号仍需按记录填写（我初填留空被门禁拦）；从 state 读 test_record_ids 填入后通过。这本身就是 v4 要消灭的"AI 手抄机器事实"最后一次现场表演。
- 第二道门通过，凭据 7e8064cb...ef1b0561。下一步：用户确认 → regression_test（本轮有代码改动，跳过判定会返回不跳过，真实跑全量回归）。

## regression_test 环节（第二道门已过，等用户确认）

- 本轮有代码改动，R9 跳过判定返回"不跳过"——判定函数的反例验证一次（B 主题 AC-01 附带实证）。
- WorkBuddy shim bug 大爆发：统一入口在本环境 532 errors（mkdir exist_ok 不认 + 嵌套 pytest 拦截 + pytest-of-unknown 断链残留）。git stash 对照再次确认与代码无关；上轮 564 passed in 38.73s（凌晨环境还正常，白天 shim 行为变化）。
- 解法：用户在自己的 Terminal（无 shim 注入）执行 `TMPDIR=/tmp/wfinherit workflow gate regression_test`，**596 passed in 48.07s，退出码 0**，记录编号 REG-20260908T112345+0000-fdb92c4d。凭据 652c0e41...f644077。
- shim bug 的完整复现和描述已写给用户用于反馈（mkdir 不认 exist_ok=True 违反 Python 语义）。
- 教训（记 MEMORY）：全量回归在这台 WorkBuddy 环境可能被 shim 拦截，最终回归建议直接在干净终端执行。

## 收尾（19:00-20:00：用户 Terminal 回归 + 计划外缺陷修复 + 整轮收工）

- 用户 Terminal 跑回归：第一次 596 passed（REG-...fdb92c4d）→ 我在整体验收发现 test_result 表全空 → 程序正确退回 qa → 我补填表时机错误（确认后填）触发哈希失效 → 深挖出真 bug：test_result 行清单 required_at_gate=False 让空表静默过三道门（违反 R11）。
- 缺陷修复：两处 schema required_at_gate=True + tests/test_empty_result_table_guard.py 2 项守卫测试。修复改了产品代码 → impl 绑定失效 → 重走 impl→qa→主题验收（test run 复用有效记录 14 passed；B 主题两项因 basetemp 残留 failed 一次，清目录重跑恢复）。
- 第二次用户 Terminal 回归：598 passed（REG-20260908T115435+0000-78d41283，多出的 2 项就是守卫测试）。
- 整体验收 → 设计同步：架构变化，四处正文变更（4.6 加 machine_collect、6.3 加采集命令、6.11 加跳过判定、6.18 加 v4 与空表守卫）；正文变更不能改表格结构（不能加行），只能在单元格内追加事实；机器记录编号必须纯编号不能带说明文字。
- **20:00 整轮收工**（done 2026-09-08T11:59:48+00:00）。全程凭据链完整：impl ec6746f5→qa 6fada31c→验收 019a26f0→回归 82b50b80→整体 234851cc→设计同步 1a81f487。

## 本轮关键数字

- 交付：10 个产品文件改动 + 1 个新模块（machine_collect.py 601 行）+ 4 个新测试文件（32 项测试）+ 2 处 schema 修复
- 质量门：12 AC 全 passed、14 TC 全 passed、最终回归 598 passed 零失败
- v4 机制首验：验收记录程序建立、五列回填、空表守卫全部在本轮真实生效
- 教训留档：五处"AI 手抄机器事实"现场（行号/RUN/表版本/机器编号/test_result 三列）——前四类已由 v4 治，第五类（test_result 的编号/结论/机器编号三列）记为下轮机器采集候选

## 第二轮（2026-09-08-1207-product_change，20:07 开始，进行中）

用户拍板把"治 AI 手抄机器事实残余两类"当轮做：② test_result 三列机器采集、⑤ 代码符号定位。过程中又追加主题三（两个新 bug 用户确认当轮修）。

### 主题一：测试结果表机器采集三列
- machine_collect.py 新增 test_result_columns（与验收五列同源）；records 的 _fill_machine_record_ids 扩展为三列回填（更新/追加/降级/幂等）。

### 主题二：代码符号自动定位
- tree-sitter 改用 Python 标准库 ast（零新依赖）；新模块 code_symbols.py（145 行）：索引 92 文件、相近符号建议、引用四分支核对。
- 挂载 _sync_design_tables 的 architecture_changes 分支：正文变更的新文与依据列 文件::符号 引用自动核对。

### 主题三：先改代码后准备基线的 Git 自证出口（实施中发现的两 bug 用户确认当轮修）
- bug1：prepare 首次准备对"进场后已改代码"无出口——修复：快照差异逐文件 Git 自证（快照=HEAD 用 HEAD 字节副本；新文件放行；不可自证按文件列原因拒绝）。本轮自身实证：进场快照拍到中间态的 records.py 被正确拒绝（AC-02），自证放行由 4 项自动化测试覆盖。
- bug2：中途加主题死循环（索引要文档、文档要主题解析、主题解析要索引合法）——修复：current_workflow_topics/sync_stage_tables/确认门三处合并 topic_relations 表主题。实测打破：第三主题文档生成、登记、门禁全通。
- git 操作教训：git add -A 会把进行中轮次的文件卷进上轮提交——用 reset --soft 拆分；stash push 对未跟踪文件要先 git add -N，且部分失败会吃掉已暂存文件（本轮 records.py 虚惊一场）。
- 本轮期间 git 提交：1641be0（上轮成果）、另两次（1207 主题一二、主题三）。

### 状态
qa 门禁已过（凭据 fce0f701...cca7f16a6）。9 TC 全 passed。**主题一成果首次真实工作**：test_result 三列由程序自动回填（我只填了实际结果说明叙述列）。中途暴露并修复第三个真 bug：回填挂在空表守卫之后永不触发——修正为回填先于守卫（机器采集类回填必须在空表检查前，回填后非空，守卫只拦真正无内容的表）。等用户确认测试结果后进主题验收。
