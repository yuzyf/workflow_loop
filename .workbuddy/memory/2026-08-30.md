# 2026-08-30 工作日志

## 表模式质量修复轮（2026-08-29-2338-product_change）

- spec 环节三门通过（R18-R21 四条新规则入产品规格）；spike 跳过。
- acceptance_plan 门 2 曾被 36 项错误卡死：表模式门禁仍用文档模式校验器逐字解析 v1 生成文档（缺"1.~6."章节、AC-01 小节）。定性为 R6/R14 已确认缺陷的现场复现。
- 用户选择"提前实施生成器渲染"路线解锁：
  - records.py：acceptance_plan 表加"验收条件名称"列 + 需求来源/产品设计依据/本主题验收/本主题不验收/完成判定 5 叙述栏；TABLE_FORMAT_VERSION 升 "2"；generate_document 新增 acceptance_plan 模板六节渲染分支（锚点、字段同行、上下游表）。
  - traceability.py::_source_link 修双重路径 bug（功能_spec/功能_X.md.md），支持表内 Markdown 链接（../ 改 ./）。
  - 5 张 acceptance_plan 表 + topic_relations + product_features 数据修补：版本 2、15 AC 补名称与链接式依据、尖括号改「」、补 5 叙述栏；追踪表本轮错误章节删除后由程序重建。
- 门 2 一次通过（凭据 fcb69147…），用户审阅文档中，待 --confirmed 后进 impl。
- 待实施遗留（impl 施工单）：其余六类表栏位补齐、stages.py 三处 has_any_table 退回口子、缺表 continue 改报错、R19 实质内容校验、R20 填写说明三要素、R21 模板规范同步、_sync_product_features 哈希语义 bug、追踪表"本次需求"文字来源绑表（现取到"修改"两字）。
- 实施完成后的验证路线：workflow return --to acceptance_plan，用 v2 表重做全部验收文档对比质量（T5 主题）。

## 经验

- 全局 workflow 命令实际加载 src/workflow_loop（editable 安装），改 src 立即生效，无需重装。
- 表模式下任何"读回生成文档做校验"的代码都是 R14 违例候选，遇到门禁解析 md 的报错先查这一点。

## AC 字段结构演变查证（用户问"为什么以前有预期结果"）

- 三段式（条件与触发/预期结果/依据）是 08-03 旧模板；70c4f6c（08-09）改六字段+兼容摘要两行；62282b8（08-12）删兼容摘要，定型现行六字段。
- "一次安装"等旧文档质量感来自三段式的一句话预期结果；用户知情后决定**保持六字段现状，不加摘要行**（决策 2026-08-30）。
- 内容质量重写：15 条 AC 六字段全部改为可独立读懂的验收事实（删 T1/stages.py 内部代号、禁空词），门 2 凭据 cc32581d…；COLUMN_HINTS 验收条件 8 列说明已提前升级为三要素口径。

## 验收计划"（待生成）"下游链接永不回补缺陷（用户发现）

- 根因：表模式文档被哈希锁定、只有本环节门禁渲染，下游文件生成后无人重刷验收计划文档（文档模式靠 AI 手改，表模式丢了该行为）。
- 修复（records.py）：① generate_document 的 acceptance_plan 分支按下游文件真实存在输出链接或待生成；② sync_stage_tables 在 impl/qa/topic_acceptance/update_code_design 环节对全部主题重刷 acceptance_plan 文档（仅写文档+程序专用键，表内容不变→不触发失效，R7）。
- 实证：造假下游文件→链接升级→删除→回退→凭据恢复 cc32581d…；475 项测试全过。

## 回补方向纠正（用户两次指出）

- 否决①"只修验收计划一家"：每个环境都有下游引用问题。②否决"每次门全刷本轮所有文档"：下游没生成时全刷无意义。
- 正确机制：**被生成文档 → 待回补引用方**定向映射（_document_reference_map）；sync_stage_tables 循环记录本轮首次真实生成的文档（existed_before 判据），循环后 _backfill_referencing_documents 只重刷映射命中的上游文档（表内容不变不触发失效 R7，手改跳过）。新环节模板加带存在性的下游链接时只需在映射表加一行。
- 全局侧（追踪表 ensure/按列更新、索引 link_or_pending 按存在性重写）本就是"下游出现→全局刷新"，无需改。
- 新增集成测试 test_downstream_generation_backfills_upstream_reference：实施门生成实施记录→同一次门禁验收计划链接升级、测试计划保持待生成、表内容未动；全量 476 通过。

## 规则锚点体系落地（spec return 轮续）
- Standardized/Template spec.md（src+镜像共4份）补规则锚点条目与示例；18份功能文档 407 锚点；发现并修复 R17 行漏加锚点、r-19 曾因正文引用出现"重复"（实为规则行+叙述文字，编号单元格唯一）。
- spec 三门通过（凭据 35aa4cf1）；spike skip；acceptance_plan 表 15 AC 依据链接升级为 #r-N 规则级，追踪表本轮章节删后由程序按新链接重建（14行全带 #r-N），门2过（09a65de2）。
- 用户投诉验收计划文档第2章叙述栏仍是合并 #4-规则 链接"找不到标头"——5张表的产品设计依据叙述栏逐规则拆链；表栏位补齐主题需求来源误指 #4-规则 改指 R18/R16/R21；修后 sync_stage_tables 无问题。
- 教训：升级链接时只改了 AC 字段列，漏了第2章叙述栏——同义信息出现在多处时必须全栏位排查。

## 表模式彻底调教 T2 完成（行迁移 + 版本冻结 + 渲染器 v1/v2 分发）
- records.py：`_SUPPORTED_TABLE_VERSIONS={"1","2"}`；v1 schema/hints 从 git HEAD 逐字节导出为 `_LEGACY_KIND_SCHEMAS`/`_LEGACY_COLUMN_HINTS` 快照，历史两轮 93+ 张 v1 表零扰动（不迁移、按冻结版本校验、v1 渲染器保文档哈希稳定）。
- 开工冻结：`WorkflowState.table_format_version` 新字段，`workflow start` 写入；`_workflow_table_version` 三级判定（state 字段 → 扫描本轮表 max → 当前版本）；sync/校验调用点全部传冻结版本；当前轮 state.json 手工置 "2"。
- `migrate_rows`：仅 v2，按新列补空/剔未知列，已填值保留；`workflow scaffold` 实测迁移本轮 5 张 impl_record 成功（代码修改计划 3列→8列）。
- 渲染器：`generate_document` 按 `_table_version_of` 分发 v1/v2；v2 按环节模板全章节渲染、零占位句（三个禁用句 not in 断言入测试）；`人工验收步骤` 清单从 acceptance_result 移到 acceptance_plan schema；`generate_test_result_document` 拆 `_test_result_machine_lines`（27 机器事实标签与 artifact_validation 逐字段一致）+ v1/v2；test_result 调用点补传 project_root（修 file_key 回退 bug）。
- 测试：新增 4 个 v2 行为测试（v1 冻结渲染、行迁移保值、人工验收机器列可选、start 冻结版本）；全量 481 passed。
- 下一步：任务#5 填全 5 张 impl_record 栏位过门2 → AC-04/R21 模板规范同步 → T3（R19 实质校验 + _sync_product_features 哈希误报）→ T5 return acceptance_plan 重做实证。

## 修复轮（2026-08-29-2338-product_change）进度快照 14:15
- 已完成：spec 三门（R19⑥+R33+产品总说明登记）、spike 跳过、acceptance_plan 三门（门禁主题新增 AC-04 纯流程豁免+追踪表穿刺列修复+AC-04 行追加）、impl --discuss-done 已过。
- 豁免代码已落地：records.py FLOW_ONLY_MARKER/is_flow_only_plan_row/impl_table_exempts_code_result_lists + validate_table 混表报错/required_at_gate 豁免；rollback.py _planned_code_changes 跳过标记行 + _recorded_code_changes table_empty 豁免。全量 488 passed（新增 7 用例）。
- 5 张 impl_record 已填全（实施依据/最低实现设计/计划/动作/实际修改/检查/叙述栏；修复轮验收重做验证=标记行豁免主题）。
- 待办（卡点）：表内行号覆盖预检发现问题需修正后跑 workflow gate impl：
  1) records.py：D 表"实际代码修改"L1990-L2020 无效（真实块在 1980 附近 has_any_table_file 与 2461-2497）；E 表 _sync_product_features 写成 L2279-L2305 应为 L2206-L2250；未覆盖块 782-783/963-1119/1332-1397/1866/2506-2699 需要补记录行或并入现有范围。
  2) 单行值格式错误：数据镜像"实际代码修改"里 L137、L22、L87、L86 等要写成 L137-L137 形式。
  3) cli.py L3127-L3130、state.py L738 未被覆盖（C/D/A 表补行）。
  4) tests/test_records.py (34,140)/(479,484)/(611,618)/(660,726) 未覆盖（E 表 L168-L256/L259-L455 范围改准或补行）。
- 核对脚本方法：git diff -U0 取 hunk 新行号区间，与表"代码位置（最终文件）"逐块求交集。
- 之后链条：gate impl → 用户确认 --confirmed → T5（workflow return --to acceptance_plan 用 v2 表重做验收文档实证）→ qa/topic_acceptance/regression/overall/update_code_design。

## 修复轮收工记录（16:45）
- 2026-08-29-2338-product_change 已 workflow done（2026-08-30T08:45:23+00:00），全链门禁通过，回归 490 passed。
- 表修复实际内容：39 行栏名"数据状态或输出的实际变化"缺顿号（曾使门禁 KeyError 崩溃）；实际代码修改改为一文件一行全跨度（门禁按 key_column 拒绝重复登记）；实施动作记录多区间值并成单跨度；新增 traceability.py 行；tests 行从实际代码修改移除（门禁把测试文件排除在实际改动外）。
- 快照里"行号覆盖核对必拦"的判断不成立：现行门禁不按 git diff 逐 hunk 核覆盖，真正拦人的是表结构规则与最小信息量。
- 过程中发现并修复 4 个本轮新产品缺陷（每次均 return --to impl 修复+回归用例+重走门）：表模式测试项字段退化（test_mapping）、命令参数数组 JSON 字符串未解析+人工行被当自动化（test_execution/cli/stages 四处）、test_result 渲染分发缺失（records.py）、验收结果对纯人工主题生成坏链（records.py）。新增 2 个回归用例（490=488+2）。
- 未提交 git；用户要求时再提交。

## bugfix 轮收工（19:15）
- 2026-08-30-1025-bugfix 已 workflow done。修复用户报告的两处生成文档缺陷：覆盖表对齐模板 13 列（移除独立直白测试名称列，测试项列承载锚点+编号+名称）；测试结果文档生成后实施记录引用回补（引用映射增加 qa/<主题>_测试结果.md → impl_record）。
- 顺带修复表模式 bug_record 生成缺口：缺陷记录文档+索引现按模板落 bug/ 目录（此前只写 records 内部组合文档，环节门禁查不到）。
- 已按修复后的渲染器重新生成 2338 轮 19 份文档（覆盖表 13 列 ✓、实施记录结果引用回补 ✓、验收结果无坏链 ✓）；历史轮次旧模板文档按约定保留。
- 用户预览器不支持 GFM 管道表格与内联 HTML 锚点：文档本身规范，<a id="ac-xx"></a> 是 R17 要求的 AC 导航锚点，正常 Markdown 渲染不可见。
- 提交：7f6f404（修复轮）、9d1e10c（bugfix 轮）。
