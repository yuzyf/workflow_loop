# Bug 索引

| Bug 记录 | 现象 | 根因 | 状态 |
|---|---|---|---|
| [维护测试固定旧版本导致新版本发布流水线失败](./缺陷_维护测试固定旧版本导致新版本发布流水线失败.md) | 发布 0.3.0 后维护版本判断测试失败，流水线停止 | 测试夹具固定使用发布前的 0.2.0 | 已修复并验收 |
| [门禁把真实格式错误说成没有主题或缺少内容](./缺陷_门禁把真实格式错误说成没有主题或缺少内容.md) | 格式写错、基线对不上或位置列写泛称时，门禁不一次给出真实原因和改法 | 上层换成另一句结论，基线只存总哈希且不覆盖 | 已修复并验收 |
| [生成文档列序与结果引用符合模板规则](./缺陷_生成文档列序与结果引用符合模板规则.md) | 上一修复轮生成的实施记录文档“上下游文档”表中，指向本主题测试结果文档的下游行仍写“（待生成）”，而该结果文档在 qa 门禁时已经真实生成 | 根因说明：sync_stage_tables 的下游引用回补只登记了部分文档引用类型；根因位置：src/workflow_loop/records.py 的 _document_reference_map 与 _backfill_referencing_documents；根因证据：qa/测试计划表并入设计语义列_测试结果.md 已生成而实施记录第 93 行仍写（待生成），对照引用映射可知该引用类型未登记 | 已修复并验收 |
| [生成表格列数一致且结果引用全环节回补](./缺陷_生成表格列数一致且结果引用全环节回补.md) | 表模式生成的测试计划“验收条件覆盖”表在所有 Markdown 预览里都显示为竖线文字流，不渲染成表格 | 根因说明：test_plan 渲染分支的分隔行写成“ | --- | ”再加“--- | ”乘列数，比表头多出一列；根因位置：src/workflow_loop/records.py 的 _generate_document_v2 test_plan 分支；根因证据：六份表模式测试计划实测表头 13/分隔行 14，而 _render_rows 生成的其它表分隔行列数与表头一致 | 已修复并验收 |
| [退回验收计划后新增主题能走通门禁与追踪表](./缺陷_退回验收计划后新增主题能走通门禁与追踪表_2.md) | 轮次已推进过验收计划后执行 workflow return 退回，并向 acceptance/索引.md 新增验收主题，第二道门 workflow gate acceptance_plan 报「索引的主题必须覆盖当前工作流全部主题」拒绝放行；第三道门 --confirmed 又因第二道门未通过被拦，而唯一能把新主题写入 state.topics 的注册代码位于第三道门内部，新主题永远进不了流程。 | 根因说明：第二道门取校验集时 or 短路——state 已有主题时 current_workflow_topics 非空，candidate_topics()（实现为 topic in current or topic not in history，本就为收纳索引新主题而写）永不执行，校验集被钉死在旧主题，与索引 3 个主题比对必失败；而注册新主题写入 state 的代码只在第三道门 --confirmed 内部，第三道门又要求第二道门先通过，形成互相等待。根因位置：src/workflow_loop/stages/stages.py 第 333 行 topics = current_workflow_topics(project_root) or candidate_topics(project_root)。根因证据：沙盒 /tmp/wf_repro_deadlock 跑真实命令 workflow gate acceptance_plan，报错「当前主题 [2 个旧主题]，索引主题 [3 个]」；再跑 workflow gate acceptance_plan --confirmed 报「第二道程序校验还没有通过」，state.topics 未变，死锁两端闭合。 | 已修复并验收 |
| [退回重走后门禁不再无故反复失败](./缺陷_退回重走后门禁不再无故反复失败.md) | 第二道门第一次报一屏“目标不是现有普通文件”的链接错误；不修改任何文件原样再执行同一条门禁命令，第二次直接通过。历史 journal 中只含链接错误的 22 次失败里有 18 次紧接着被原样重跑通过，多数发生在同一分钟内。 | 根因说明：第二道门的执行顺序与产品规则相反。cli.py::validate_stage_output 先调用 markdown_links.validate_managed_markdown_links 做链接检查，之后才调用 stage.code_validate，而按当前表重新生成正式文档发生在 code_validate 内部的 stages.py::_stage_table_sync 里。于是第一次执行检查的是尚未重新生成的旧文档，这一次执行顺带把文档生成好，第二次执行才检查到新文档。根因位置：src/workflow_loop/cli.py::validate_stage_output 第 2146 至 2165 行，链接检查调用在 stage.code_validate 调用之前。根因证据：沙盒真实运行显示同一输入下两次链接检查结论从 False 翻转为 True；仓库 journal.jsonl 统计到 18 次“只有链接错误的失败紧接着被原样重跑通过”。 | 已修复并验收 |
| [计划路径非法时门禁一次说清而不是抛异常](./缺陷_计划路径非法时门禁一次说清而不是抛异常.md) | 实施记录工作记录表的代码修改计划行把工作流过程文档路径填进文件列时，执行代码实施第二道门不输出问题清单，只打印一行“错误：命令执行失败（ValueError: ...）”。同一次检查发现的其它问题不再报出，AI 只能改完这一条再跑一次才能看到剩下的问题。 | 根因说明：解析计划路径的函数已经把每个非法路径构造成完整的结构化诊断对象，但对外入口只取其中的证据字段拼成一个字符串抛出异常，其余字段（检查标识、位置、预期、影响、下一动作）全部丢弃；异常沿调用链向上传播，门禁调用链里的两处调用不捕获，最终由命令层兜底处理打印成一行错误。根因位置：src/workflow_loop/rollback.py 第 1447 至 1451 行的 planned_code_paths，函数体为 changes, diagnostics = _planned_code_changes(...) 后 if diagnostics: raise ValueError(把每条诊断的证据字段换行拼接)；不捕获该异常的调用方是同文件第 1808 行的 validate_prepared 与第 2016 行的 prepare_impl，另有 verification.py 第 822 至 828 行的 _active_registered_paths 在实施记录文档已存在时按设计重新抛出。根因证据：沙盒对同一非法路径分别调用两条链路，计划一侧抛 ValueError 且栈顶为 rollback.py:1450，实际改动一侧返回 2 条结构化诊断；读源码确认 _planned_code_changes 第 1419 至 1434 行已生成带七个字段的 Diagnostic 对象。 | 已修复并验收 |
