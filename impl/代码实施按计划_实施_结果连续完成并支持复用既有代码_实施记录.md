# 【实施】代码实施按计划、实施、结果连续完成并支持复用既有代码

- 工作流编号：2026-08-10-0817-product_change
- 验收主题：代码实施按计划、实施、结果连续完成并支持复用既有代码

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品设计 | 一个 `impl`（代码实施）环节内部固定完成代码计划、代码实施和代码结果；用户只确认开始前计划和结束时结果；正式修改前保存真实回退内容；符合计划的既有代码可以复用 | [实施并保护项目修改](../spec/功能_实施并保护项目修改.md#4-规则)、[产品总说明](../spec/产品总说明.md#6-产品通用规则) |
| 验收条件 | AC-01 至 AC-04：计划确认前不改代码、回退基线后连续实施、既有代码可复用、计划与真实差异及结果完全一致 | [验收条件](../acceptance/代码实施按计划_实施_结果连续完成并支持复用既有代码_验收计划.md#ac-01) |
| 代码设计 | 继续复用逐文件首次副本和计划、真实差异、结果三方核对；`impl` 保存内部步骤、计划哈希、复用决定和代码结果事实 | [实施、测试与验收层](../spec/代码架构设计.md#46-实施测试与验收层)、[代码计划与实施记录](../spec/代码架构设计.md#69-功能实施并保护项目修改) |
| 穿刺结论 | 本轮已经确认跳过穿刺，没有本轮穿刺结论或可复用资产 | 暂无 |

## 2. 实施前计划（代码计划）
<a id="2-实施前计划"></a>

### 2.1 预期产品结果

用户确认一份覆盖全部主题的详细代码计划后，程序保存计划文件的首次真实内容，AI 连续完成代码修改和开发检查，再用代码结果逐文件说明真实变化。计划改变时先停止并重新确认，不覆盖首次副本；现有代码已经满足计划时登记复用，不制造空修改，但仍进入后续测试验证。

### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 模块与职责 | `ImplStage`（代码实施阶段策略）校验代码计划和代码结果；`rollback.py` 保存第一次修改前内容并比较计划、真实差异和结果；`cli.py` 只编排计划确认、基线准备、实施和结果确认；`state.py` 保存内部步骤与复用决定。 | 计划真实性、恢复依据、命令顺序和持久状态分别只有一个负责位置。 | AC-01、AC-02、AC-03、AC-04 |
| 接口与调用顺序 | `workflow gate impl --discuss-done` 确认计划，`--prepare-code` 保存回退基线，AI 连续实施并填写动作和结果，`workflow gate impl` 完整校验，用户确认后用 `--confirmed` 进入 QA；零修改复用走 `--accept-existing-code`。 | 用户只在代码计划和代码结果做决定，程序保护动作不增加用户环节。 | AC-01、AC-02、AC-03 |
| 数据、状态与副作用 | 回退清单保存每个计划文件原内容或原本不存在事实；`internal_step`（内部步骤）、`plan_confirmed_hash`（计划确认摘要）、`reuse_decision_hash`（复用决定摘要）和 `result_hash`（结果摘要）绑定当前实施。 | 计划变化、代码变化和复用决定都能被精确识别，不靠模糊布尔值猜测。 | AC-02、AC-03、AC-04 |
| 错误与边界 | 计划外路径、已修改后才补入计划、首次副本不可信、计划与差异或结果集合不一致时一次列全并停止；既有代码复用不能跳过后续正式测试。 | 防止事后补计划、覆盖真实原内容或用零修改冒充已验证交付。 | AC-02、AC-03、AC-04 |

### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 | 共享代码归属 | 风险 | 完成标准 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/state.py` | `StageState` 中实施内部状态和复用字段 | 只有通用三道门、代码基线、计划确认哈希和既有代码哈希，不能明确区分代码计划、实施中和代码结果 | 增加可序列化的实施内部步骤及结果摘要；保留现有 `plan_confirmed_hash`（计划确认哈希）、回退清单和既有代码确认哈希，旧状态缺字段时回到代码计划而不是猜测完成 | 状态能回答当前处于代码计划、代码实施还是代码结果，能绑定计划、首次副本、复用决定和结果 | AC-01、AC-02、AC-03、AC-04 | 无 | 与主题一、三、四共用同一状态模型 | 多个布尔值组合可能产生非法状态 | 内部步骤只有合法顺序，旧状态安全迁移，复用或计划变化后相应结果失效 |
| 2 | `src/workflow_loop/stages/stages.py` | `ImplStage._validate_topic_documents()`、`discussion_validate()`、`code_validate()`、`instruction()` | 第一道门只查章节和未决问题；第二道门已有回退与三方核对，但模板字段不够细，状态没有三段式语义 | 第一道门校验具体文件、位置、当前逻辑、目标逻辑、状态或输出、顺序、依赖、共享归属、风险、完成标准和开发检查；从零创建额外校验项目入口、模块职责、共享状态、依赖和测试入口；第二道门检查真实代码结果并写内部结果状态 | 缺字段、计划外变化、计划内未修改、未记录变化或未完成内容一次列出；通过时得到真实代码结果 | AC-01、AC-02、AC-04 | 1 | 实施阶段规则只在 `ImplStage` 维护，回退算法调用 `rollback.py` | 过度依赖文档措辞会形成脆弱门禁 | 每项计划都可定位和执行，结果表与真实差异逐项对应，没有未完成内容 |
| 3 | `src/workflow_loop/rollback.py` | `planned_code_paths()`、`compute_plan_hash()`、`prepare_impl()`、`validate_prepared()`、`validate_implementation_changes()`、`validate_existing_implementation_paths()` | 已能保存首次原内容和核对四类差异，但阶段语义仍依赖旧测试阶段，复用结果没有新的内部步骤绑定 | 保留核心逐文件算法；把实施内部步骤、当前计划哈希和复用决定纳入校验；计划新增路径只在尚未修改时补副本，已保存路径永不覆盖；复用后代码变化立即拒绝继续 | 回退清单保持首次事实；计划变更可补新路径；计划、实际差异、代码结果三组路径相等或进入明确的零修改复用分支 | AC-02、AC-03、AC-04 | 1、2 | 本文件是实施回退和三方核对的唯一实现 | 错误补基线会把已修改内容当作原始内容 | 首次副本不可覆盖，任一不一致均一次报告，复用分支不伪造差异 |
| 4 | `src/workflow_loop/cli.py` | `cmd_gate()` 的 `impl` 分支、`--discuss-done`、`--prepare-code`、`--accept-existing-code`、阶段完成更新 | 已有计划确认、准备回退和复用命令，但输出仍把它们当独立门；实施完成后进入旧 `test_plan`，第三道门还会重复完整校验 | 把这些命令解释为 `impl` 内部动作：用户确认计划后程序准备回退基线，随后连续实施；既有代码确认写复用决定；第二道门生成代码结果校验凭据，第三道门只确认结果并进入 `qa` | 用户只需两次确认；程序动作不会增加新用户阶段；输出清楚说明当前内部步骤、是否修改代码和唯一下一命令 | AC-01、AC-02、AC-03、AC-04 | 1、2、3 | CLI 编排供主题一和主题四共用 | 当前工作流自举时路径更新可能在实施中触发迁移 | 当前轮能安全完成自举，后续轮次只显示代码计划和代码结果两次确认 |
| 5 | `src/workflow_loop/data/Template_Repository/impl/impl.md` | 实施记录模板 | 模板有实施前计划和实施后记录，但没有固定表达代码计划、代码实施、代码结果、共享归属、风险、完成标准和复用依据；开头把两类产物误写成三类 | 在保持可解析章节锚点的前提下，明确三段式；扩展计划字段；增加代码实施过程、代码结果和既有代码复用事实的固定位置；从零创建增加最低限度实现设计字段；产物数量按实际两类表述 | 新生成文档能完整表达计划、执行和结果，且不会把未来设计写成现状事实 | AC-01、AC-03、AC-04 | 2 | 模板供所有项目安装后生成实施文档 | 改章节名会破坏现有解析器 | 新模板与解析器一致，历史实施文档仍能读取，产物说明与清单一致 |
| 6 | `src/workflow_loop/data/Standardized_Repository/impl/impl.md` | 实施阶段工作规范 | 仍写实施后进入 `test_plan`、再进入 `test_code` 和 `test_execution`，并把回退准备描述成额外门 | 改为进入单一 `qa`；明确用户只确认代码计划和代码结果，`--prepare-code` 是程序保护动作；计划变化先停下，复用既有代码也必须后续正式测试 | 后续安装项目的讨论材料与真实新流程一致，不再引导重复用户确认 | AC-01、AC-02、AC-03 | 4、5 | 规范由 `workflow discuss` 加载 | 规范与 CLI 不一致会继续造成流程反复 | 所有下一步说明只指向 `impl` 内部动作或 `qa` |
| 7 | `src/workflow_loop/data/Standardized_Repository/impl/code_implementation.md` | 代码开发规范 | 规定了代码来源、接口、依赖和自检，但没有明确三段式内部边界和复用代码的记录要求 | 补充代码计划确认后才实施、实施中改变改法先停下、代码结果只写真实变化、复用既有代码必须记录核对依据且不能跳过测试验证 | 代码开发规范能直接约束实施过程和结果真实性 | AC-01、AC-02、AC-03、AC-04 | 5、6 | 所有实施主题共用 | 把流程细节写得过多会与阶段规范重复 | 文件只补代码实施边界，不复制 CLI 门禁实现 |
实施顺序固定为：先落状态字段，再增强 `ImplStage` 计划和结果校验，随后接入现有回退与复用算法，再调整 CLI 内部步骤，最后同步模板和规范。实施中若文件、函数或处理顺序需要超出本表，先停止并更新本计划，不能先改后补记录。

包内实施资源完成后，另执行一次运行材料同步：将 `src/workflow_loop/data/Template_Repository/impl/impl.md`、`src/workflow_loop/data/Standardized_Repository/impl/impl.md` 和 `src/workflow_loop/data/Standardized_Repository/impl/code_implementation.md` 分别原样写入当前项目对应的 `.workflow_loop` 活动副本。活动副本属于工作流过程材料，不属于业务代码修改计划，因此不纳入回退基线的代码文件集合；同步动作必须在代码结果记录前完成并逐字节核对。

#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| 解析所有主题的代码计划文件集合 | `planned_code_paths()` 和计划表 | 每个路径具体、项目内、无通配符，重复路径只保存一次首次副本 |
| 准备回退基线后重复准备 | `prepare_impl()` 和回退清单 | 第一次保存原内容；第二次只补新增且未修改路径，不覆盖首次副本 |
| 制造四类计划与真实差异不一致 | `validate_implementation_changes()` | 一次列出计划未改、改而未计划、改而未记录、记录但未改四类问题 |
| 既有代码零修改复用 | `--accept-existing-code` 路径 | 只在基线后无变化且计划与记录可定位时通过，随后仍进入测试验证 |
| 实施内部步骤状态往返 | 状态保存与 CLI 下一步输出 | 代码计划、代码实施、代码结果顺序稳定，不出现第三次用户确认 |

### 2.4 未决问题

暂无

## 3. 实施后记录
<a id="3-实施后记录"></a>

### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 类、函数或配置项 | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | `src/workflow_loop/state.py` | `StageState` 的内部步骤、结果和复用字段 | 增加代码计划、代码实施、代码结果的内部步骤及其摘要字段，并为旧状态保留安全空值。 | 状态序列化往返和旧状态读取检查通过。 | 已完成 |
| 2 | 3 | `src/workflow_loop/rollback.py` | `_clean_git_head_baseline()`、`prepare_impl()`、`core_registered_paths` | 保存第一次真实代码基线；再次准备时只补尚未修改的新计划路径，不覆盖首次副本。 | 当前回退清单完整，计划路径与第一次基线可比较。 | 已完成 |
| 3 | 2、4 | `src/workflow_loop/stages/stages.py`、`src/workflow_loop/cli.py` | `ImplStage`（代码实施阶段策略）、`ensure_impl_recovery_baseline()`（确保实施入场基线函数）、`cmd_gate()` 的实施分支 | 复用已有计划与三方结果校验，补当前记录结构检查；让正常进入或恢复进入 `impl` 时都立即建立入场代码基线，再把计划确认、回退准备、零修改复用和代码结果凭据接入同一实施阶段。 | 首次代码计划确认不再因“基线要等门通过才生成、门又先要求基线”形成死循环；用户确认点仍保持在代码计划和代码结果两处。 | 已完成 |
| 4 | 5 至 7 | 实施模板与两份实施规范 | 代码计划、实施动作、代码结果和开发检查边界 | 更新包内资源并同步当前项目活动副本，明确最低实现设计、动作记录和真实结果字段。 | 三组资源逐字节比较一致。 | 已完成 |
| 5 | 开发检查计划 | 当前计划、回退清单、真实差异和代码结果 | 三方核对、语法检查、全量开发检查和分发物检查 | 三方文件集合一致，395 项开发检查通过。 | 已完成 |

### 3.2 实施中问题与处理

`src/workflow_loop/stages/stages.py` 中计划核对的 `ImplStage`（代码实施阶段策略）原有计划确认前代码基线检查、实施中计划重新确认、实施记录核对和三方文件集合校验入口，本轮先直接复用，没有为了制造差异重复实现。重新读取更新后的实施材料后，确认原校验器没有检查新模板的最低实现设计、实施动作和代码结果子表，因此在同一计划文件内补当前结构检查；同一文件的 `QaStage`（测试验证阶段策略）变化仍归“完整研发流程”和“测试验证”主题。

重新读取更新后的实施材料时，发现运行解析器仍只读取旧实施记录标题。本轮继续在已确认的 `rollback.py` 和 `stages.py` 计划范围内补当前标题解析与结构检查，并把五份当前实施记录迁到新模板；文件集合和产品结果没有扩大。

验收计划重新确认后再次进入 `impl` 时，第一道门报告“缺少进入 impl 时的代码基线”。日志和状态核对确认：下游失效会清空旧基线，验收计划完成时又先清除恢复原因；原 `ensure_impl_recovery_baseline()`（确保实施恢复基线函数）只在存在恢复原因时执行，导致正常进入实施没有基线，而第一道门通过后才会写基线，形成循环依赖。修复为正常进入或恢复进入 `impl` 都立即记录当前非测试代码快照；门禁仍比较入场快照和计划确认时快照，不能把修改后的代码伪装成实施前状态。隔离命令场景已证明无恢复原因时执行 `workflow discuss` 后也会建立入场基线。

### 3.3 未完成内容

暂无

### 3.4 代码结果

`impl` 已形成代码计划、代码实施、代码结果三段式内部状态。代码结果通过第二道门后保存结果摘要和校验凭据，用户确认后直接进入 `qa`（测试验证）；若基线后没有产品代码变化，只能记录明确的既有代码复用决定，不能伪造一次代码差异。当前项目实际读取的实施模板、实施阶段规范和代码开发规范活动副本也已与包内新规则逐字节同步。

#### 3.4.1 实际代码修改

| 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件 |
|---|---|---|---|---|
| `src/workflow_loop/state.py` | `StageState.internal_step`、`StageState.internal_step_hash`、`StageState.result_hash`、`StageState.reuse_decision_hash` | 给用户可见的 `impl`（代码实施）阶段增加 `code_plan`（代码计划）、`code_implementation`（代码实施）、`code_result`（代码结果）内部步骤字段，以及复用决定和结果摘要字段；旧状态缺字段时保持空值。 | 状态可以区分“计划已确认、正在实施、结果待确认”，并能绑定计划、真实代码结果和既有代码复用决定，不靠多个布尔值猜测当前步骤。 | AC-01、AC-02、AC-03、AC-04 |
| `src/workflow_loop/cli.py` | `ensure_impl_recovery_baseline()`（确保实施入场基线函数）、`confirmation_next_step()`、`current_stage_next_instruction()`、`cmd_gate()` 的 `impl` 分支 | 正常进入或恢复进入 `impl` 时都立即记录非测试代码入场快照；`--prepare-code`（准备代码回退基线）作为程序保护动作，成功后把内部步骤置为代码实施；`--accept-existing-code`（确认复用既有代码）只在基线后零修改时记录复用哈希；第二道门通过后把 `impl` 内部步骤置为代码结果，并把下一步指向 `qa`。 | 首次代码计划门不再循环失败，计划确认前代码变化仍会被入场快照发现；用户只确认代码计划和代码结果，输出明确显示回退基线、复用决定、真实差异和唯一下一命令。 | AC-01、AC-02、AC-03 |
| `src/workflow_loop/rollback.py` | `_section()`（读取实施章节函数）、`_code_plan_section()`（读取代码计划函数）、`_code_result_section()`（读取代码结果函数）、`compute_plan_hash()`（计算代码计划摘要函数）、`prepare_impl()`（准备实施回退函数） | 章节解析按真实标题层级截取，优先读取当前 `2.3` 代码计划和 `3.4.1` 代码结果并兼容旧标题；计划摘要绑定整个“实施前计划”；首次准备保存真实 Git 基线，再次准备只补仍未修改的新计划路径。 | 最低实现设计、代码文件表或开发检查发生变化都会使旧计划确认失效；新旧轮次都能核对三方集合，首次副本不会被再次准备覆盖。 | AC-01、AC-02、AC-04 |
| `src/workflow_loop/stages/stages.py` | `ImplStage._validate_topic_documents()`（校验主题实施记录函数） | 当前阶段路径版本要求每份记录包含最低实现设计、当前代码修改计划、实施动作、实施中问题、实际代码修改和开发检查结果；旧路径版本继续读取原结构供迁移。 | 模板、规范和门禁使用同一三段式产物结构，缺少任一当前章节会一次列入门禁问题。 | AC-01、AC-04 |
| `src/workflow_loop/data/Template_Repository/impl/impl.md` | `## 3. 实施后记录（代码实施与代码结果）`、`### 3.1 实施动作记录`、`### 3.4 代码结果` | 模板增加实施动作记录、实施中问题、代码结果实际修改表和开发检查表，并要求结果写到文件、精确位置、逻辑和可观察变化。 | 新生成实施记录能连续表达代码实施和代码结果，计划外偏差必须返回第 2 章重新确认，不再把过程描述当成最终实现事实。 | AC-01、AC-03、AC-04 |
| `src/workflow_loop/data/Standardized_Repository/impl/impl.md` | `## 1. 阶段目标和固定顺序`、`## 3. 代码计划`、`## 4. 代码实施`、`## 5. 代码结果` | 规范明确从零开发的最低实现设计写进代码计划，修改产品不建立设计期代码修订；实施前保存回退基线，实施中按计划连续完成，复用既有代码也必须经过正式测试验证。 | `workflow discuss`（读取阶段材料命令）和 `workflow gate impl`（代码实施门禁命令）使用同一三段式规则，代码结果确认后进入 `qa`。 | AC-01、AC-02、AC-03 |
| `src/workflow_loop/data/Standardized_Repository/impl/code_implementation.md` | `## 8. 开发检查怎么选`、`## 9. 写完代码后的自检` | 补充开发检查只作为实施反馈、不能冒充正式测试；要求代码结果记录真实文件和精确位置，并检查公共入口、依赖、副作用和错误路径。 | 实施记录和代码结果的完成标准从“写过代码”改为可定位、可复核的真实行为，未生成测试计划时不提前写测试结论。 | AC-01、AC-02、AC-04 |

#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `.venv/bin/python -m compileall -q src` | `state.py`、`cli.py`、实施门禁和回退调用的语法 | 命令退出码为 `0`，没有语法错误输出。 | 否 |
| 已执行 `workflow gate impl --discuss-done` 和 `workflow gate impl --prepare-code` | 当前工作流五个主题的代码计划、计划哈希和回退清单 | 计划确认和修改前文件基线已写入当前工作流状态与 `.workflow_loop/rollback/2026-08-10-0817-product_change/impl/manifest.json`；`--prepare-code` 未修改业务代码。 | 否 |
| `cmp -s`（逐字节比较命令）核对三份包内实施资源与活动副本 | 实施记录模板、实施阶段工作规范和代码开发规范 | 三组比较退出码均为 `0`；当前项目实际读取的三份活动副本与对应包内源资源完全一致。 | 否 |
| `git diff --check` | 实施记录、规范模板和源代码文本差异 | 命令退出码为 `0`，没有尾随空格或补丁格式错误。 | 否 |
| `.venv/bin/python -m pytest -q tests/test_rollback.py tests/test_stages.py` | 当前实施标题解析、当前路径结构要求和旧路径读取边界 | 33 项测试全部通过；当前路径必须使用新结构，路径版本早于 2 的旧轮次仍可读取旧标题。 | 否 |
| `.venv/bin/python -m pytest -q` | 代码计划、回退基线、既有代码复用、门禁推进及全部项目回归 | 395 项测试全部通过；没有失败、错误或跳过。该结果是实施开发检查，正式测试仍在后续 `qa`（测试验证）环节重新登记和执行。 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 前置 | [完整研发流程按最少用户环节推进并兼容旧轮次实施记录](./完整研发流程按最少用户环节推进并兼容旧轮次_实施记录.md) | 新路径和兼容迁移先建立 `impl` 的外部位置 |
| 上游 | [验收计划](../acceptance/代码实施按计划_实施_结果连续完成并支持复用既有代码_验收计划.md) | 本主题用户结果和 AC-01 至 AC-04 |
| 上游 | [代码架构设计](../spec/代码架构设计.md#69-功能实施并保护项目修改) | 实施三段式、回退和复用设计 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 当前工作流全部交付关系 |
| 下游 | `../qa/代码实施按计划_实施_结果连续完成并支持复用既有代码_测试计划.md`（待生成） | 测试验证内部形成测试计划 |
| 下游 | `../qa/代码实施按计划_实施_结果连续完成并支持复用既有代码_测试结果.md`（待生成） | 正式执行后生成机器结果 |
| 下游 | `../acceptance/代码实施按计划_实施_结果连续完成并支持复用既有代码_验收结果.md`（待生成） | 主题验收结果 |
