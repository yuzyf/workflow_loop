# 【实施】一次安装后可在三种操作系统开始使用工作流

- 工作流编号：2026-07-30-0740-product_change
- 验收主题：一次安装后可在三种操作系统开始使用工作流

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品设计 | 安装器在 macOS、Linux 和原生 Windows 上先完成无写入预检和一次确认，再把固定版本的全局命令与当前项目作为一次事务安装 | [安装到项目](../spec/功能_安装到项目.md) |
| 验收条件 | AC-01 至 AC-05：安装前可审查、一条命令完成三平台安装、产品身份固定、异常完整恢复、重复安装只补缺少部分 | [验收条件](../acceptance/一次安装后可在三种操作系统开始使用工作流_验收计划.md#4-验收条件) |
| 测试项 | TC-01 至 TC-05：预检取消、三平台安装、固定身份、失败恢复和重复安装 | [测试项](../qa/一次安装后可在三种操作系统开始使用工作流_测试计划.md#1-验收条件覆盖) |
| 代码设计 | Bash 和 PowerShell 只处理终端差异，Python 负责项目写入事务；版本、安装工具、摘要、项目标记和身份输出固定 | [终端与发布适配层](../spec/代码架构设计.md#42-终端与发布适配层) |
| 穿刺结论 | 固定 uv 0.11.33 可以使用现有 Python 安装 0.1.0，禁止托管或下载 Python，并从实际安装目录运行命令 | [固定版本 uv 隔离安装穿刺](../spec/穿刺_固定版本uv隔离安装.md#7-结论) |

## 2. 实施前计划

### 2.1 预期产品结果

用户在目标项目根执行当前操作系统的一条官方命令，可以在一次确认后得到身份和版本严格为 `workflow-loop 0.1.0` 的全局命令与完整项目骨架。取消、预检失败或安装中途失败时，本次下载、全局工具、命令搜索路径和项目文件不会留下半套修改。重复安装不会覆盖同版本项目已经定制的内容。

### 2.2 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件和测试项 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/__init__.py` | 产品身份与版本常量 | 只定义 `__version__ = "0.1.0"` | 增加固定产品标识并让命令和安装模块读取同一身份常量，不增加其它版本分支 | 程序内部只有一组 `workflow-loop` 和 `0.1.0` 身份值 | AC-03；TC-03 | 无 |
| 2 | `install.sh` | macOS/Linux 官方安装入口 | 依赖用户已有 pipx 或 uv；公开调用 `install-project`；没有固定摘要、完整预检和失败恢复 | 改为从当前终端读取确认；写入前检查 Python、同名命令、项目骨架、工具目录和命令搜索路径；确认后下载并校验固定 uv 0.11.33，显式使用现有 Python安装固定包；调用实际命令路径和一次性事务；失败时恢复本次修改 | 取消保持零写入；安装和恢复范围在确认前可见；不自动安装 Python；不下发通用测试脚本 | AC-01、AC-02、AC-03、AC-04、AC-05；TC-01 至 TC-05 | 1 |
| 3 | `install.ps1` | 新增 Windows PowerShell 官方安装入口 | 暂无现有逻辑 | 使用 Windows PowerShell 5.1 和 PowerShell 7 原生能力完成与 Bash 入口相同的预检、一次确认、固定 uv 下载校验、全局命令安装、命令搜索路径处理、项目写入和失败恢复；不调用 Bash，不永久修改执行策略 | 具备 Windows PowerShell 5.1 或 PowerShell 7 和 Python 3.11 或更高版本的原生 Windows 不依赖 WSL（Windows Linux 子系统）；用户 PATH（命令搜索路径）修改可以恢复；脚本失败时不留下半套安装 | AC-01、AC-02、AC-03、AC-04、AC-05；TC-01 至 TC-05 | 1 |
| 4 | `src/workflow_loop/installer.py` | `install_project()`、资源准备、事务校验和恢复 | 直接删除旧模板目录、覆盖 `AGENTS.md` 并写 `project.json`；没有一次性事务、完整性检查和失败恢复 | 校验安装脚本生成的一次性事务文件、固定版本、项目绝对路径和允许写入路径；先在临时目录准备并核对骨架；保存原文件或“原本不存在”记录后原子替换；失败时逐项恢复；下次安装先处理遗留事务；完整同版本项目保持零修改 | 项目只在事务成功时出现完整骨架；残缺或异版本骨架在写入前停止；事务不能重复用于另一项目 | AC-01、AC-02、AC-03、AC-04、AC-05；TC-01 至 TC-05 | 1 |
| 5 | `src/workflow_loop/project.py` | `ProjectState`、`is_installed()`、`create_project()` 和新增骨架检查 | 只要 `project.json` 存在且版本匹配就视为已安装；安装版本在本文件重复写死 | 读取统一版本常量；检查项目标记、模板仓库、规范仓库和 `AGENTS.md` 是否共同构成完整骨架；创建项目时只写精简项目级状态，不创建正式产物或全量测试脚本 | 完整、未安装和残缺三种状态可以区分；残缺状态不会被误当成安装成功 | AC-02、AC-03、AC-04、AC-05；TC-02 至 TC-05 | 1 |
| 6 | `src/workflow_loop/cli.py` | `main()`、版本入口和内部项目写入入口 | 没有 `--version`；普通帮助公开 `install-project`，任何调用者都能直接写项目 | 增加固定版本身份输出；删除公开 `install-project`；新增不显示在普通帮助中的 `_install-project`，只把已校验的一次性事务交给安装模块；错误输出说明失败原因和正确入口 | `workflow --version` 固定返回 `workflow-loop 0.1.0`；用户和 AI 不能通过公开命令绕过安装确认 | AC-02、AC-03、AC-04；TC-02、TC-03、TC-04 | 1、4、5 |
| 7 | `.github/workflows/release.yml` | 固定版本发布任务、手动验证入口和三平台作业依赖 | 当前只接受 `v0.1.0` 标签；构建后先上传不可覆盖的 PyPI 0.1.0，再从 PyPI 执行三平台安装检查；不能在发布前单独触发真实三平台作业 | 增加不发布的手动触发入口；构建后下载同一份分发包，在 macOS、Linux、Windows PowerShell 5.1 和 7 中直接运行官方安装脚本，并通过 uv 的本地包查找配置安装该分发包；PowerShell 两种版本使用独立工具目录；三平台全部成功后，标签任务才可信发布同一份分发包并创建 GitHub Release | 手动作业只产生三平台验证记录，不上传公开内容；未经三平台验证的构建包不能进入 PyPI；PyPI、GitHub Release、安装脚本和验证记录绑定同一份 0.1.0 构建包 | AC-01 至 AC-05；TC-01 至 TC-05 | 2、3、6 |
| 8 | `pyproject.toml` | `[project.optional-dependencies].dev`（pip 使用的开发附加依赖） | `pytest` 已声明，但发布工作流测试直接导入的 `yaml` 模块只存在于 uv 的开发依赖组；全新环境执行 `python -m pip install -e ".[dev]"` 后无法收集测试 | 在 pip 的 `dev` 附加依赖中增加提供 `yaml` 模块的 `PyYAML>=6.0`；保留 uv 开发依赖组中的同一声明，使两种项目开发安装入口都具备完整测试依赖 | 全新 Python 环境按发布工作流安装项目后可以导入 `yaml` 并收集全部测试；正式产品运行依赖不增加 `PyYAML` | AC-02、AC-03；TC-02、TC-03 | 7 |
| 9 | `uv.lock` | `workflow-loop` 包的可选开发依赖元数据 | 已锁定 `PyYAML 6.0.3`，但 `dev` 附加依赖元数据只包含 `pytest` | 用项目锁文件工具根据 `pyproject.toml` 更新元数据，不手工改包版本或摘要 | `dev` 附加依赖同时解析到 `pytest` 和 `PyYAML`；现有 `PyYAML 6.0.3` 锁定版本保持一致 | AC-02、AC-03；TC-02、TC-03 | 8、项目修改可恢复主题的 `uv.lock` 文件分类修复 |
| 10 | `.github/workflows/release.yml` | 首次远程验证的临时分支触发条件 | 工作流第一次加入仓库时不在默认分支，GitHub 不提供手动触发按钮；验证分支临时允许推送启动同一套任务 | 修复依赖并完成远程验证后，删除只服务于 `codex/three-platform-validation` 分支的临时触发条件，恢复正式的“手动验证或 `v0.1.0` 标签发布”入口；标签发布限制不变 | 最终产品不保留测试分支名称；手动运行仍只验证不发布，标签运行仍须验证通过后发布 | AC-02、AC-03；TC-02、TC-03 | 8、9 |

### 2.3 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| `bash -n install.sh` | Bash 脚本语法 | 脚本语法检查通过 |
| PowerShell 语法解析和静态检查 | `install.ps1` 在 PowerShell 5.1 与 7 的兼容写法 | 不使用 Bash、POSIX 路径或仅 PowerShell 7 支持的语法 |
| `python -m compileall src/workflow_loop` | Python 安装、项目状态和命令入口代码 | 所有修改后的 Python 模块可以编译 |
| 在临时目录手工调用安装事务入口 | 一次性事务、路径限制、重复调用和失败恢复 | 合法事务只使用一次；路径或版本不符时零写入；注入失败后恢复原内容 |
| 从源码入口执行 `workflow --version` | 产品身份和固定版本 | 输出严格等于 `workflow-loop 0.1.0` |
| 比较发布配置中的版本、标签和安装脚本常量 | 固定版本一致性 | 所有位置都严格使用 `0.1.0` 或 `v0.1.0` |
| 检查发布任务触发条件、作业依赖、本地构建包位置和平台隔离配置 | 发布前验证顺序和三平台作业独立性 | 手动触发不会发布；标签发布必须等待三平台作业；安装脚本直接安装同一次构建产物；两种 PowerShell 不复用工具目录 |
| 在全新临时虚拟环境执行 `python -m pip install -e ".[dev]"`，再导入 `yaml` 并运行 `tests/test_release_workflow.py` | pip 开发附加依赖和发布工作流测试的全新环境可运行性 | 安装后可以导入 `yaml`；发布工作流的三项结构测试全部通过 |
| `uv lock --check` | 项目依赖声明与 `uv.lock` 的一致性 | 锁文件与 `pyproject.toml` 一致，`PyYAML` 同时属于 pip 的 `dev` 附加依赖和 uv 开发依赖组 |
| 恢复正式触发条件后运行发布工作流结构测试 | 临时远程启动规则的清理结果 | 不再包含验证分支名称；手动验证和固定标签发布规则仍通过结构测试 |

### 2.4 未决问题

暂无

## 3. 实施后记录

### 3.1 实际代码修改

| 对应计划步骤 | 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件和测试项 |
|---|---|---|---|---|---|
| 1 | `src/workflow_loop/__init__.py` | `__version__`（产品版本号）、`PRODUCT_NAME`（产品名称）、`PRODUCT_IDENTITY`（完整产品身份） | 增加产品名称和完整产品身份，并让安装器与命令入口读取同一组常量 | 产品身份固定为 `workflow-loop 0.1.0`，没有增加其它版本分支 | AC-03；TC-03 |
| 2 | `install.sh` | macOS/Linux 官方安装入口、固定 uv 版本与摘要配置、失败恢复流程 | 写入前检查 Python 3.11 以上版本、已有同名命令、操作系统与处理器架构、摘要工具、项目路径和命令搜索路径；向用户展示实际写入范围并只确认一次；确认后下载并校验固定版本 uv 0.11.33，强制使用现有 Python；调用内部项目安装事务。命令搜索路径只写入预检时已经披露的终端配置文件，不再让 uv 二次猜测终端；失败时恢复旧配置或删除本轮新建配置 | 用户取消或预检失败时不写入；完整同版本项目重复安装时项目内容不变；Linux 和 macOS 的实际写入路径与确认前披露一致；安装过程不下载 Python，也不生成通用测试脚本 | AC-01 至 AC-05；TC-01 至 TC-05 |
| 3 | `install.ps1` | Windows PowerShell 官方安装入口、`Write-Utf8NoBom`（无字节顺序标记的 UTF-8 写入函数）、用户 `PATH`（命令搜索路径）恢复流程 | 使用原生 PowerShell 完成与 Bash 入口相同的预检、确认、固定 uv 下载校验、全局工具安装和失败恢复；项目 JSON 继续由 .NET 公共能力写成无字节顺序标记的 UTF-8；安装脚本文件自身增加 UTF-8 BOM（文件开头的编码标记），供 Windows PowerShell 5.1 正确解析中文源码 | Windows 用户不依赖 Bash 或 WSL；PowerShell 7 和 Windows PowerShell 5.1 均可解析并运行同一脚本；脚本不永久修改执行策略；本次新增的用户命令搜索路径可以随失败恢复 | AC-01 至 AC-05；TC-01 至 TC-05 |
| 4 | `src/workflow_loop/installer.py` | `_validate_transaction_token`（校验一次性事务凭据）、`_prepare_staging`（准备临时完整骨架）、`_record_backups`（记录原内容）、`_validate_recovery_manifest`（恢复前完整校验）、`_restore_from_manifest`（按清单恢复）、`recover_pending_transaction`（处理遗留事务）、`install_project_transaction`（项目安装事务） | 按准备、备份、替换、提交执行安装；在恢复前完整校验产品、版本、项目路径、允许写入路径、恢复副本和摘要；下次安装先处理遗留事务 | 事务只能用于声明的产品、版本、项目绝对路径和两条允许写入路径；恢复副本、摘要或路径异常时先停止，不修改项目；中断后可由下次安装恢复 | AC-01 至 AC-05；TC-01 至 TC-05 |
| 5 | `src/workflow_loop/project.py` | `check_skeleton`（检查项目骨架）、`create_project`（创建项目骨架） | 将骨架检查改为区分完整、未安装和残缺三种状态，并逐项核对项目标记、必需模板、必需规范和 `AGENTS.md`；创建项目时只写精简项目状态 | 不再仅凭 `project.json` 或非空目录判断安装成功；初次安装不创建正式工作产物、工作流运行状态或项目专用全量测试脚本 | AC-02 至 AC-05；TC-02 至 TC-05 |
| 6 | `src/workflow_loop/cli.py` | `main`（命令入口）、`--version`（显示版本）、`_install-project`（内部项目安装命令）、`cmd_internal_install_project`（执行内部项目安装）、UTF-8 输出初始化 | 增加固定身份输出；内部项目安装命令只把一次性事务交给安装模块，并从普通帮助的公开命令列表隐藏；在解析参数和打印中文前把标准输出与标准错误统一设置为 UTF-8 | 用户可核对严格身份 `workflow-loop 0.1.0`，不能通过公开日常命令绕过安装确认；Windows 的非交互输出环境不再因西文编码无法输出中文而中断 | AC-02、AC-03、AC-04；TC-02、TC-03、TC-04 |
| 7、10 | `.github/workflows/release.yml` | `workflow_dispatch`（手动触发）、`prepublish-smoke`（发布前安装检查）、`publish-pypi`（发布 PyPI）和 `github-release`（创建 GitHub Release） | 增加只构建和验证、不发布的手动入口；发布前作业下载同一次构建产物，并让三平台官方安装脚本通过 `UV_FIND_LINKS`（uv 本地包查找目录）安装该产物；Linux 和 macOS 使用 Python 标准库创建伪终端，在出现确认提示后输入答案；PowerShell 7 和 Windows PowerShell 5.1 使用隔离工具目录；三个系统独立跑完，不因一个平台失败取消其它平台；远程验证完成后删除临时验证分支触发，只保留手动验证和 `v0.1.0` 标签入口 | 手动触发不会上传公开内容；标签发布依赖顺序为“核对与全量测试 → 构建 → 三平台安装 → PyPI → GitHub Release”；三平台作业覆盖取消、完整安装、身份核对和重复安装，Linux、macOS、PowerShell 7 还覆盖只缺项目侧和只缺电脑侧；最终配置不包含临时分支名 | AC-01 至 AC-05；TC-01 至 TC-05 |
| 8 | `pyproject.toml` | `dev`（pip 开发附加依赖） | 增加 `PyYAML>=6.0`，即提供 `yaml` 模块的开发测试依赖；不加入产品运行依赖 | 全新环境执行 `python -m pip install -e ".[dev]"` 后可以收集发布任务测试 | AC-02、AC-03；TC-02、TC-03 |
| 9 | `uv.lock` | 开发附加依赖元数据 | 使用 uv 锁文件工具同步 `pyproject.toml`，让 `dev` 同时包含 `pytest` 和 `PyYAML`，保持既有 `PyYAML 6.0.3` 锁定版本 | pip 与 uv 两种开发安装入口使用一致的完整测试依赖 | AC-02、AC-03；TC-02、TC-03 |

### 3.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `python3 -m compileall -q src/workflow_loop`（编译检查全部 Python 模块） | Python 安装、项目状态和命令入口代码 | 返回码为 0；只证明当前 Python 源码可以编译，不代表正式测试或主题验收通过 | 否 |
| `bash -n install.sh`（检查 Bash 脚本语法） | macOS/Linux 安装脚本语法 | 返回码为 0；只覆盖 Bash 语法，不代替 macOS/Linux 的正式安装验收 | 否 |
| 在临时目录直接调用 Python 安装事务 | 首次安装、同版本重复安装、合法恢复、篡改副本摘要、越界路径和错误产品身份 | 首次安装后骨架完整；重复安装前后文件摘要和纳秒修改时间不变；合法恢复还原原 `AGENTS.md` 并删除本轮新骨架；后三种异常均在修改目标前停止并保留恢复证据。该检查不是三种操作系统的正式测试 | 否 |
| 从源码读取统一身份常量，并核对安装脚本、发布标签与固定 uv 配置 | 产品身份和固定发布配置 | 产品身份为 `workflow-loop 0.1.0`，发布标签为 `v0.1.0`，两份安装脚本固定 uv 0.11.33；仅核对当前仓库内容，未实际发布 PyPI 或 GitHub Release | 否 |
| 在全新临时虚拟环境安装 `.[dev]`（开发附加依赖），导入 `yaml` 并运行发布任务测试 | pip 开发附加依赖完整性 | `PyYAML 6.0.3` 可以导入，`tests/test_release_workflow.py` 的 3 项测试通过；第一次远程运行出现的测试收集错误已消除 | 否 |
| `uv lock --check`（检查 uv 锁文件） | 依赖声明与锁文件一致性 | 返回码为 0；`dev` 开发附加依赖元数据同时包含 `pytest` 和 `PyYAML` | 否 |
| 强制 `PYTHONIOENCODING=cp1252`（Python 西文输出编码）运行中文帮助 | Windows 非交互输出编码 | 命令入口仍完整输出中文并以状态 0 退出，证明入口会在打印前切换到 UTF-8 | 否 |
| 检查 `install.ps1` 文件首字节并运行远程 Windows 作业 | Windows PowerShell 5.1 与 PowerShell 7 的脚本解析和安装 | 文件以 `EF BB BF` 开头，即 UTF-8 BOM；PowerShell 7 和 Windows PowerShell 5.1 均完成取消、安装、身份核对与重复安装检查 | 否 |
| GitHub Actions 第六次远程验证（提交 `8c72266`） | 完整测试、构建、Linux、macOS、PowerShell 7、Windows PowerShell 5.1、发布边界和同一分发包 | [远程运行 30798718139](https://github.com/yuzyf/workflow_loop_spike/actions/runs/30798718139) 整轮成功；三个系统下载同一 `dist` 分发包并通过，PyPI 与 GitHub Release 作业均跳过 | 否 |
| 恢复正式触发条件后解析 YAML 并运行发布任务结构测试 | 临时分支触发清理和正式作业依赖 | YAML 可解析；最终配置不含 `codex/three-platform-validation`，只保留手动验证和 `v0.1.0` 标签；依赖顺序仍为“核对与完整测试 → 构建 → 三平台安装 → PyPI → GitHub Release” | 否 |
| `.venv/bin/python -m pytest -q`（本地完整测试） | 当前仓库全部自动化测试 | 153 项通过；该结果是实施阶段开发反馈，正式结果仍由后续测试执行阶段登记和运行 | 否 |

### 3.3 未完成内容

暂无

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/一次安装后可在三种操作系统开始使用工作流_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 上游 | [测试计划](../qa/一次安装后可在三种操作系统开始使用工作流_测试计划.md) | 本主题准备覆盖的测试范围 |
| 上游 | [代码设计](../spec/代码架构设计.md#61-功能安装到项目) | 本次实施遵守的安装架构 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [主题测试结果](../qa/一次安装后可在三种操作系统开始使用工作流_测试结果.md) | 实施完成后执行正式测试 |
| 下游 | [主题验收结果](../acceptance/一次安装后可在三种操作系统开始使用工作流_验收结果.md) | 测试通过后执行主题验收 |
