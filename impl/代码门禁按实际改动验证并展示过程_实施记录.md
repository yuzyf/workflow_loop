# 【实施】代码门禁按实际改动验证并展示过程

- 工作流编号：2026-08-27-0318-product_change
- 验收主题：代码门禁按实际改动验证并展示过程

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品设计 | 代码实施和测试验证都把计划当作预期，不把文件清单当作白名单；门禁按实际改动、理由、验收关联和证据判断。 | [产品总说明](../spec/产品总说明.md#6-产品通用规则) |
| 验收条件 | AC-01 至 AC-07：首次展示过程、额外代码或测试文件不返工、实际改动证据检查、定向重查和恢复限制如实展示。 | [验收计划](../acceptance/代码门禁按实际改动验证并展示过程_验收计划.md#ac-01) |
| 现有代码设计 | 门禁由阶段策略、CLI 编排、验证失效和回退模块共同实现；机器测试记录继续绑定真实代码和测试哈希。 | [代码架构设计](../spec/代码架构设计.md#74-验证失效) |
| 穿刺结论 | 本轮跳过穿刺，没有可复用资产。 | 暂无 |

## 2. 实施前计划（代码计划）
<a id="2-实施前计划"></a>

### 2.1 预期产品结果

代码实施和测试验证首次进入时明确展示门禁含义。实施或测试中新增必要文件不触发基线返工、重设基线或重复确认。第二道门展示可取得的实际改动、理由、验收关联和测试证据；缺少事实才失败。恢复依据不足只显示限制，不阻塞代码或测试门禁。

### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 阶段策略 | `ImplStage`（代码实施阶段策略）不再把实施基线、计划文件集合或回退清单作为讨论门和代码门的硬前置；`TestCodeStage`（测试代码策略）不再用测试/产品代码基线阻断 QA；`QaStage` 保留测试计划、任务和机器结果真实性检查。 | 文件范围预测不可靠，最终实际改动和执行证据才是门禁需要验证的事实。 | AC-02、AC-03、AC-06、AC-07 |
| 实际改动读取 | 新增 `rollback.list_actual_working_tree_changes()`（读取 Git 当前提交差异、暂存差异、未暂存差异和未跟踪文件）并过滤工作流管理目录；无 Git 时返回“未能自动完整确认”。 | 计划可能漏文件，Git 状态可直接提供实际改动；无法确认时必须诚实报告。 | AC-02、AC-03、AC-05 |
| 实施结果核对 | `rollback.validate_actual_implementation_changes()`（按实际改动与实施记录核对）逐行读取实际逻辑、可观察变化、修改理由、AC 关联和测试证据；任一字段缺失或是占位值都生成独立诊断，不比较计划文件集合。 | 额外文件可以实现已确认行为，但门禁必须真的检查理由和证据，不能只在提示中声称检查。 | AC-02、AC-03 |
| 测试改动核对 | `verification.list_actual_test_changes()`（按 Git 状态读取实际测试相关改动）列出实际路径；`test_mapping`（测试映射模块）从全部测试源码读取当前主题的 Workflow-Test 标识，把实际测试入口归属到 TC 和 AC。实际入口可偏离最初计划路径，但测试项、验收条件和执行证据不能缺。 | 测试计划也是预期，文件路径可以调整；真实测试项和机器记录仍必须严格。 | AC-06、AC-07 |
| CLI 展示 | `cmd_discuss()` 首次进入 `impl` 或 `qa` 各输出编号 1 至 5 的完整过程，后续加载不重复；`cmd_gate()` 的第二道门显示实际改动和证据缺失项；默认下一步不出现 `--prepare-code` 与 `--rebaseline`。 | 编号步骤可由测试逐项判断，AI 不需要从一段概括文字猜规则。 | AC-01、AC-03、AC-04、AC-06、AC-07 |
| 定向失效 | 测试代码变化时，用变化路径中的 Workflow-Test 标识定位具体 `(主题, TC)`；只删除这些测试项的机器记录。无法证明具体测试项的共享配置或辅助文件才影响该主题全部测试项；QA 范围确认始终保留。 | 变化影响范围必须来自可检查证据，不能因为同属一个主题就清空所有测试项。 | AC-07 |
| 恢复边界 | 保留开工回退清单和作废恢复实现，但门禁失败不再由回退清单缺失触发；恢复时只恢复有真实依据的条目并报告未知范围。 | 恢复和正确性是两个不同问题。 | AC-05 |

### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/rollback.py` | `RecordedCodeChange`、`_recorded_code_changes_with_diagnostics()`、`validate_actual_implementation_changes_report()` | 已能读取实际路径、逻辑、可观察变化和 AC，但没有读取“修改理由”和“测试证据”两列，缺失时仍可能通过。 | 给记录事实增加 `change_reason`（修改理由）和 `test_evidence`（测试证据）；当前路径文档要求两列存在且值具体，旧历史文档继续兼容；缺列、空值和占位值分别输出具体诊断。 | 门禁只有在每个实际文件的理由、AC 和测试证据都完整时通过；计划外路径本身不失败。 | AC-02、AC-03、AC-05 | 无 |
| 2 | `src/workflow_loop/stages/stages.py` | `ImplStage.discussion_validate()`、`ImplStage.code_validate()` | 实施第一道门检查代码基线和完整快照；第二道门检查回退清单、基线后差异和计划集合。 | 第一、第二道门移除基线硬前置，调用实际改动核对；保留实施文档结构、最终位置、逻辑、可观察变化和 AC 检查。 | 缺少回退依据只成为恢复限制说明；额外实现文件可通过。 | AC-02、AC-03、AC-04、AC-05 | 1 |
| 3 | `src/workflow_loop/test_mapping.py`、`src/workflow_loop/stages/stages.py` | `validate_workflow_test_markers()`、实际测试路径关联、`TestCodeStage._validate_current_test_code()` | 标识校验只扫描计划登记路径并要求实际标识路径等于计划路径；额外测试文件虽会显示，但没有成为可用测试入口。 | 扫描全部测试源码；每个 TC 仍必须恰好有一个完整标识，但允许其真实测试入口路径与计划预测不同；实际变化路径有标识时显示对应 TC/AC，没有直接标识的共享文件明确按可证明范围归属。 | 计划外测试文件可直接成为真实测试入口，缺测试项、AC 或机器证据才失败。 | AC-06、AC-07 | 无 |
| 4 | `src/workflow_loop/cli.py` | `_take_impl_code_baseline_notice()`、`cmd_discuss()`、`cmd_gate()` | 当前首次提示是一段概括文字，没有稳定的五步编号；默认下一步已移除旧基线命令。 | `impl` 和 `qa` 首次提示分别输出明确编号 1 至 5；后续加载只显示动态结果，不重复完整过程；继续兼容旧参数但不推荐。 | 自动化测试可逐项判断五步是否完整，AI 能直接理解何时继续、何时返回上游。 | AC-01、AC-03、AC-04、AC-06、AC-07 | 2、3 |
| 5 | `src/workflow_loop/verification.py` | `InvalidationFinding`、测试代码失效分支、`apply_invalidation()` | 已能按路径定位受影响主题，但 `_invalidate_test_execution_outputs()` 会删除该主题全部测试任务。 | 在测试代码变化事实中保存受影响 `(主题, TC)`；应用失效时只删除对应测试任务，主题测试结果文档和验收记录因汇总已过期而删除；同主题其它 TC 的当前机器记录保留。共享入口或无法细分的共享配置才影响全部当前 TC。 | 失效报告显示变化路径、受影响 TC 和保留 TC；QA 范围确认不被清除。 | AC-04、AC-05、AC-07 | 1、2、3 |
| 6 | `src/workflow_loop/test_execution.py`、`src/workflow_loop/artifact_validation.py` | 执行记录和结果校验 | 测试执行前后比较整体产品/测试 hash；结果校验依赖当前测试代码 hash。 | 保留执行期间代码未被并发修改的安全检查；移除进入 QA 的基线要求，确保机器记录仍绑定本次执行时的产品和测试 hash。 | 测试真实执行证据不降级，受影响任务可单独重跑。 | AC-06、AC-07 | 3、5 |
| 7 | `src/workflow_loop/data/Template_Repository/impl/impl.md`、`src/workflow_loop/data/Standardized_Repository/impl/impl.md`、`src/workflow_loop/data/Standardized_Repository/qa/test_plan.md`、`src/workflow_loop/data/Standardized_Repository/qa/test_code.md`、`src/workflow_loop/data/Standardized_Repository/qa/test.md` | 实施结果表和阶段规范 | 实施模板没有“修改理由”和“测试证据”列；规范已说明计划不是白名单。 | 在新实施模板结果表增加两列并同步当前项目活动模板；规范明确实际入口可调整、共享测试文件无法细分时的保守影响范围。 | 新轮次会生成门禁真正能检查的字段，材料与代码一致。 | AC-01、AC-03、AC-06、AC-07 | 1、3、4、5 |
| 8 | `tests/test_current_workflow_contracts.py`、`tests/test_commands.py`、`tests/test_rollback.py`、`tests/test_stages.py`、`tests/test_verification.py` | 七条当前门禁契约测试和底层模块测试 | 当前正式契约仍引用旧三方集合语义，没有覆盖本轮七条 AC。 | 增加 TC-01 至 TC-07 的 Workflow-Test 标识和真实场景；更新旧计划白名单断言，保留回退模块独立测试。 | 正式测试可证明四个补修点和原有真实性边界。 | AC-01 至 AC-07 | 1 至 7 |

#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| `.venv/bin/python -m pytest tests/test_current_workflow_contracts.py tests/test_stages.py tests/test_commands.py tests/test_rollback.py tests/test_verification.py tests/test_current_workflow_qa.py -q` | 七条正式契约、实施/测试门禁和失效行为 | 旧真实回退测试保留；新增实际改动门禁、额外文件、测试项级失效和非阻塞限制用例通过。 |
| `.venv/bin/python -m compileall -q src/workflow_loop` | Python 语法和导入 | 退出码为 0。 |
| `git diff --check` | 文档和代码文本改动 | 无空白错误。 |

### 2.4 未决问题

暂无

## 3. 实施后记录（代码实施与代码结果）
<a id="3-实施后记录"></a>

### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 类、函数或配置项 | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 2、4 | `src/workflow_loop/cli.py` | `confirmation_next_step()`、`current_stage_next_instruction()`、`cmd_discuss()`、`cmd_gate()` | 移除默认的 `--prepare-code`（准备代码回退基线）和 `--rebaseline`（重设基线）门禁路径；首次进入 `impl` 或 `qa` 展示实际改动门禁过程；第二道门按实际改动和证据继续。 | 下一步只要求补齐实际文件记录后执行 `workflow gate impl`。 | 已完成 |
| 2 | 1、2、5 | `src/workflow_loop/rollback.py` | `list_actual_working_tree_changes()`、`actual_implementation_paths_since_entry()`、`validate_actual_implementation_changes_report()` | 从 Git 工作树和入场观察快照取得实际实现改动，过滤流程工作目录；按实际路径核对实施记录，不再按计划文件集合阻断。 | 计划外文件只在缺少实际逻辑、验收关联或测试证据时失败；恢复依据不足单独报告。 | 已完成 |
| 3 | 2、3 | `src/workflow_loop/stages/stages.py` | `ImplStage`、`TestCodeStage`、`QaStage` 的门禁校验 | 删除实施和 QA 的代码/测试基线硬阻断，改为校验实际改动、实施记录、Workflow-Test（工作流测试追踪标识）和真实测试结果。 | 额外代码或测试文件不会仅因路径未列入计划而触发返工。 | 已完成 |
| 4 | 3 | `src/workflow_loop/test_mapping.py` | `all_test_source_paths()`、`collect_all_workflow_test_markers()` | 扫描项目全部测试源码中的 Workflow-Test 标识，用于把计划外测试文件归属到测试主题。 | QA 可读取新增测试文件的测试项和验收关联。 | 已完成 |
| 5 | 5、6 | `src/workflow_loop/verification.py` | `_git_changed_paths()`、`list_actual_test_changes()`、凭据绑定和失效检查 | 增加 Git 实际测试改动读取，绑定实际代码/测试变化指纹，并在失效时保留未受影响主题的有效证据。 | 门禁输出具体测试路径和受影响范围，不把整体变化扩大成整批失效。 | 已完成 |
| 6 | 7 | `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` | 作废恢复预检规则 | 将恢复依据不足改为明确列出不可自动恢复的内容，并声明该限制不阻塞代码或测试门禁。 | 流程规范不再把恢复能力误报成代码门禁失败。 | 已完成 |
| 7 | 7 | `src/workflow_loop/data/Standardized_Repository/impl/impl.md` | 实施阶段代码计划、实施和代码结果说明 | 明确计划只是预期、额外实现文件可直接修改，代码结果必须记录实际文件、理由、验收关联和测试证据。 | 后续 `workflow discuss` 读取到与 CLI 一致的实施规则。 | 已完成 |
| 8 | 7 | `src/workflow_loop/data/Standardized_Repository/qa/test.md` | 测试验证实际改动和定向重查说明 | 明确额外测试文件不构成范围变化，测试代码或配置变化只使直接受影响测试项重新执行。 | QA 规则不再要求测试基线返工或整批重跑。 | 已完成 |
| 9 | 7 | `src/workflow_loop/data/Standardized_Repository/qa/test_code.md` | 测试代码范围说明 | 明确测试计划不是测试文件白名单，允许加入夹具、辅助脚本和配置，并要求变化后只重查受影响测试项。 | 测试代码材料与实际测试改动读取规则一致。 | 已完成 |
| 10 | 7 | `src/workflow_loop/data/Standardized_Repository/qa/test_plan.md` | 测试计划阶段门禁说明 | 明确测试计划是预期，不保存测试基线作为进入 QA 的硬前置；额外测试路径直接记录测试项和验收条件。 | 首次 QA 材料不会引导 AI 重设测试基线。 | 已完成 |
| 11 | 7 | `src/workflow_loop/data/Template_Repository/impl/impl.md` | `3.4.1 实际代码修改` 结果表模板 | 增加“修改理由”和“测试证据”列，要求新轮次逐文件记录为什么修改以及怎样验证。 | 新生成实施记录可被程序实际读取并门禁校验理由和证据。 | 已完成 |

### 3.2 实施中问题与处理

重新读取更新后的流程材料后，确认实施和 QA 原有代码/测试基线硬阻断会把计划外文件误判为门禁失败。本轮将门禁改为读取 Git 实际改动，并保留恢复能力的独立限制说明；没有改变已确认的产品行为或验收条件。

### 3.3 未完成内容

状态：无

### 3.4 代码结果

本轮实际实现和流程材料修改已经完成。计划中的文件只是预期，门禁按 Git 实际改动读取结果；测试源码和测试配置的实际变化由 `qa` 阶段继续核对。

#### 3.4.1 实际代码修改

| 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| `src/workflow_loop/cli.py` | `confirmation_next_step()`、`current_stage_next_instruction()`、`cmd_discuss()`、`cmd_gate()` | 首次 `impl` 或 `qa` 材料加载显示实际改动门禁过程；`impl` 门改为读取实际改动并核对证据；旧基线参数仅兼容历史调用。 | AI 看到单一、可执行的下一步；额外文件不再引发 `--prepare-code` 或 `--rebaseline` 返工。 | 让命令输出直接解释门禁含义，减少重复确认和 token 浪费。 | AC-01、AC-02、AC-03、AC-04、AC-06、AC-07 | `.venv/bin/python -m pytest -q tests/test_commands.py tests/test_current_workflow_qa.py` 通过。 |
| `src/workflow_loop/rollback.py` | `list_actual_working_tree_changes()`、`actual_implementation_paths_since_entry()`、`validate_actual_implementation_changes_report()` | 从 Git 和入场观察快照取得实际实现路径；只核对实际路径是否有完整记录，不比较计划白名单。 | 门禁输出实际文件、缺失字段和恢复限制；计划外文件有完整证据即可通过。 | 解决计划漏文件造成的反复，同时保留事实追踪。 | AC-02、AC-03、AC-05 | `.venv/bin/python -m pytest -q tests/test_rollback.py tests/test_stages.py` 通过；`compileall` 通过。 |
| `src/workflow_loop/stages/stages.py` | `ImplStage`、`TestCodeStage`、`QaStage` 门禁校验 | 实施和 QA 不再以代码/测试基线为硬前置，改按实际变更和真实测试证据校验。 | 计划外实现或测试文件不会自动失败；缺证据仍会一次列全。 | 将阶段策略落实为实际改动门禁。 | AC-02、AC-03、AC-06、AC-07 | `.venv/bin/python -m pytest -q tests/test_stages.py tests/test_current_workflow_qa.py` 通过。 |
| `src/workflow_loop/test_mapping.py` | `all_test_source_paths()`、`collect_all_workflow_test_markers()` | 读取全部测试源码中的 Workflow-Test 标识，不再只读取计划测试路径。 | 新增测试文件可以关联主题、测试项和验收条件。 | 支持计划外测试文件的真实追踪。 | AC-06、AC-07 | Workflow-Test 标识校验测试通过。 |
| `src/workflow_loop/verification.py` | `_git_changed_paths()`、`list_actual_test_changes()`、验证凭据和失效检查 | 读取 Git 实际测试改动，绑定实际变化指纹，按直接影响范围失效旧记录。 | 输出具体测试路径和受影响项，未受影响证据保留。 | 防止测试变更造成整批重复执行。 | AC-04、AC-07 | `.venv/bin/python -m pytest -q tests/test_verification.py` 通过。 |
| `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` | 作废恢复预检规则 | 将恢复依据不足定义为独立限制，禁止把未知恢复范围伪装成门禁失败。 | 作废前能列出无法恢复的内容；代码和测试门禁仍按可确认事实执行。 | 同时满足恢复诚实性和门禁可用性。 | AC-05 | `git diff --check` 通过；流程材料同步核对通过。 |
| `src/workflow_loop/data/Standardized_Repository/impl/impl.md` | 实施阶段固定顺序和代码结果说明 | 将计划定义为预期，允许额外实现文件，并要求结果记录理由、AC 和测试证据。 | AI 不会把计划文件清单当作修改白名单，实施结果有统一字段。 | 使流程材料与实际 `impl` 门禁逻辑一致。 | AC-01、AC-02、AC-03、AC-04 | `cmp -s`（逐字节比较）核对活动副本通过。 |
| `src/workflow_loop/data/Standardized_Repository/qa/test.md` | QA 测试验证规则 | 允许额外测试文件，变化后只重查直接受影响测试项。 | QA 不再因计划外测试路径重设基线或整批返工。 | 让测试阶段与实际工具链文件保持一致。 | AC-06、AC-07 | `cmp -s` 核对活动副本通过；QA 测试通过。 |
| `src/workflow_loop/data/Standardized_Repository/qa/test_code.md` | QA 测试代码规则 | 允许额外测试文件、夹具、辅助脚本和配置，变化后只重查直接受影响测试项。 | 计划外测试路径可被读取和归属。 | 让测试代码规则覆盖真实工具链文件。 | AC-06、AC-07 | `cmp -s` 核对活动副本通过；Workflow-Test 相关测试通过。 |
| `src/workflow_loop/data/Standardized_Repository/qa/test_plan.md` | QA 测试计划规则 | 测试计划是预期，额外路径直接关联测试项和验收条件。 | QA 不再因计划外测试路径重设基线或整批返工。 | 让测试计划与实际测试实现保持一致。 | AC-06、AC-07 | `cmp -s` 核对活动副本通过；QA 测试通过。 |
| `src/workflow_loop/data/Template_Repository/impl/impl.md` | 实施记录模板结果表 | 增加“修改理由”和“测试证据”列，使新生成结果能够被门禁逐项读取。 | 新轮次实施记录不会缺少门禁所需的理由和证据字段。 | 修复 QA 发现的实施门禁事实缺口。 | AC-03、AC-06 | `cmp -s`（逐字节比较）核对模板通过。 |

#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `.venv/bin/python -m compileall -q src/workflow_loop` | Python 语法和导入 | 退出码 `0`，无语法错误。 | 否 |
| `.venv/bin/python -m pytest -q tests/test_stages.py tests/test_commands.py tests/test_rollback.py tests/test_verification.py tests/test_current_workflow_qa.py` | 实施、CLI、回退、验证和 QA 门禁 | 168 个相关测试通过，失败数、错误数和跳过数均为 `0`。 | 否 |
| `.venv/bin/python -m pytest -q` | 全项目回归开发检查 | 全部项目测试通过；正式测试仍在 QA 阶段重新登记和执行。 | 否 |
| `git diff --check` | 源代码和流程材料文本 | 退出码 `0`，没有尾随空格或补丁格式错误。 | 否 |
| `cmp -s`（逐字节比较） | 包内标准流程材料与当前项目活动副本 | 流程材料同步检查通过。 | 否

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/代码门禁按实际改动验证并展示过程_验收计划.md) | 本主题七条验收条件 |
| 上游 | [产品设计](../spec/产品总说明.md#6-产品通用规则) | 实际改动门禁和恢复分离规则 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md#2026-08-27-0318-product_change) | 本轮完整交付关系 |
| 下游 | `qa/代码门禁按实际改动验证并展示过程_测试计划.md`（待生成） | 测试验证阶段制定测试计划 |
| 下游 | `acceptance/代码门禁按实际改动验证并展示过程_验收结果.md`（待生成） | 主题验收结果 |
