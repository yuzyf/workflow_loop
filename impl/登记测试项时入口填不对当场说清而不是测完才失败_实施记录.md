# 【实施】登记测试项时入口填不对当场说清而不是测完才失败

- 工作流编号：2026-09-03-0231-bugfix
- 验收主题：登记测试项时入口填不对当场说清而不是测完才失败

## 1. 实施依据

| 依据类型 | 依据编号 | 具体内容 | 文档位置 |
|---|---|---|---|
| 验收条件 | JU-01 | 测试计划表“正式目标名称”和“测试入口”两列的填写说明要写清必须形状、可照抄的例子、多入口写法，以及哪一列决定登记。 | [AC-01](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收计划.md#ac-01) |
| 验收条件 | JU-02 | 入口形状不对时，按表登记当场失败并写明主题、测试项、列名、实际值和正确形状，状态文件不写入该测试项的登记任务；同一问题在测试验证第二道门的表校验里也报出。 | [AC-02](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收计划.md#ac-02) |
| 验收条件 | JU-03 | 一个测试项由多个测试函数覆盖时，可以在“正式目标名称”一格登记多个入口，真实执行后判定通过。 | [AC-03](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收计划.md#ac-03) |
| 验收条件 | JU-04 | 已经按单个 项目相对路径::标识 填对的表，登记入口和执行判定与修改前相同。 | [AC-04](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收计划.md#ac-04) |
| 产品设计 | JU-05 | 程序写入表的每个填写说明必须写到让不了解背景的填写者一次填对的深度，正式目标名称必须与报告适配器输出的目标名逐字一致。 | [工作记录表与正式文档生成 R20](../spec/功能_工作记录表与正式文档生成.md#r-20) |
| 产品设计 | JU-06 | 表模式必须承接文档模式对固定事实的全部校验，不得因为走表而跳过任何原有检查。 | [工作记录表与正式文档生成 R15](../spec/功能_工作记录表与正式文档生成.md#r-15) |
| 产品设计 | JU-07 | 同一测试项的多个测试函数可以合并为一条命令，实际执行的正式目标名称与登记名称一致才建立通过记录。 | [编写并执行测试 R9](../spec/功能_编写并执行测试.md#r-9) |
| 现有代码设计 | JU-08 | 测试计划解析、测试任务登记与执行、结构化报告校验分别由 test_mapping、test_execution 和 test_report 三个模块负责，工作记录表的栏位定义与校验集中在 records。 | [代码架构设计](../spec/代码架构设计.md) |

<a id="2-实施前计划"></a>
## 2. 实施前计划（代码计划）


### 2.1 预期产品结果

- 填表人打开测试计划工作记录表就能从“正式目标名称”的说明里知道必须写成 项目相对路径::报告里的目标名，能照抄一个例子，也知道一个测试项覆盖多个测试函数时把多个入口写成 JSON 数组；同时知道“测试入口”那一列只进生成文档，不决定登记。
- 填不对时，按表登记这一步就失败，输出直接指出是哪个主题的哪个测试项、哪一列、填的是什么、应该写成什么形状，状态文件里不会留下这个测试项的登记任务；同一问题在测试验证环节第二道门的表校验里也会报出来。
- 已经填对的表登记和执行结论不变，真实通过的测试不会再因为这一格的写法被判失败。

### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 模块与职责 | “正式目标名称”单元格怎样解析成登记入口，只在 test_mapping 新增一个函数里定义；表校验、按表登记和执行前登记校验三处都调用它，不各写一份形状判断。 | 同一条规则只在一个负责模块中定义，三处口径不会随时间漂移；本次缺陷的根因正是校验只存在于文档模式一侧。 | AC-01、AC-02、AC-03 |
| 接口与调用顺序 | 解析函数接受单元格原值（普通字符串或已解析的数组），返回“入口清单”和“问题说明”两项；问题说明非空表示这一格不能用于登记，由调用方决定报成表格式问题还是登记失败。 | 调用方所处环节不同、输出形式不同，但判断标准必须相同；返回问题说明而不是抛异常，能让表校验把它并进同一份问题清单一次报全。 | AC-02、AC-03 |
| 数据、状态与副作用 | 一个测试项只有一个入口时这一格仍写普通字符串；覆盖多个测试函数时写 JSON 数组，与同表“命令参数数组”一列的写法一致。登记任务的入口清单由解析结果去重后写入；登记失败时不把该测试项写进状态文件。 | JSON 数组能容纳含空格和顿号的 vitest 测试标题，用分隔符切分会在标题本身含分隔符时切错。 | AC-03、AC-02 |
| 错误与边界 | 空值、非字符串元素、缺少 :: 、多于一个 :: 、路径为空或含空格、目标名为空、同一格内重复入口都判为不能用于登记，问题说明写出列名、实际值和正确形状。新增的表校验只对表版本 2 生效，表版本 1 的冻结轮次维持原有口径。 | 错误信息要能让填表人直接改对，不能只说“无效”；按冻结版本生效是既有的表版本规则，避免进行中的旧轮次因为程序升级突然报错。 | AC-02、AC-04 |

### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改内容 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | src/workflow_loop/test_mapping.py | 新增 parse_official_target_names 与形状常量 | 本文件只有私有函数 _entry_parts 在文档模式下解析 项目相对文件::可定位标识，表模式没有任何解析或校验。 | 新增一个公开函数，把“正式目标名称”单元格解析成入口清单：普通字符串按单个入口处理，JSON 数组按多个入口处理，逐个检查形状并查重，返回入口清单和问题说明。 | 调用方可以拿到解析后的入口清单，或一句写明列名、实际值和正确形状的问题说明。 | AC-02、AC-03 | 无 |
| 2 | src/workflow_loop/test_mapping.py | TestPlanItem、_test_plan_items_from_table、planned_test_source_paths | TestPlanItem 只有单个 test_entry 字段；表模式把“正式目标名称”原值直接放进该字段；planned_test_source_paths 按单个 test_entry 取测试文件路径。 | 给 TestPlanItem 增加入口清单字段，表模式用第 1 步的解析结果填充它，planned_test_source_paths 遍历清单里的每个入口取测试文件路径。 | 一个测试项覆盖两个测试文件时，两份测试文件都进入测试代码快照，不再只登记第一个。 | AC-03 | 1 |
| 3 | src/workflow_loop/records.py | KIND_HINTS 中 test_plan 测试项的“正式目标名称”与“测试入口”说明 | “正式目标名称”只写“测试报告里的正式目标名，用于精确匹配；人工验收行留空”，没有形状、例子和多入口写法；“测试入口”没有说明它不参与登记。 | 把“正式目标名称”的说明改写成必须形状、可照抄的例子、多入口的 JSON 数组写法和“这一列是登记入口的唯一来源”四件事；在“测试入口”的说明里写明它只进入生成的测试计划文档，不作为登记入口。 | 新生成或补齐的测试计划表里，这两列的填写说明文字带形状、例子和多入口写法。 | AC-01 | 无 |
| 4 | src/workflow_loop/records.py | _TYPE_COLUMNS 与 validate_table 的类型列校验分支 | 类型列只声明了“命令参数数组”是 JSON 数组、“超时秒数”是整数；“正式目标名称”没有任何格式声明，填什么都放行。 | 为“正式目标名称”声明入口类型，在类型列校验里调用第 1 步的解析函数，问题说明作为格式问题写进问题清单，位置带行清单名称、行号和列名；该分支只对表版本 2 生效。 | 测试验证环节第二道门对形状不对的“正式目标名称”报出格式问题，不再放行。 | AC-02 | 1 |
| 5 | src/workflow_loop/test_execution.py | prepare_task 的表模式分支 | 表模式直接把“正式目标名称”原值当测试名和登记入口，登记入口写成 entries = [item.test_entry] if item.test_entry else [test_id]，不校验形状，一格只装一个入口。 | 改为调用第 1 步的解析函数：有问题时抛出带列名、实际值和正确形状的登记失败，不写入登记任务；没问题时把解析出的全部入口作为该测试项的登记入口。 | 按表登记时形状不对当场失败并说清怎么改；一个测试项可以登记多个入口。 | AC-02、AC-03、AC-04 | 1 |
| 6 | src/workflow_loop/test_execution.py | _validate_prepared_tasks_from_tables | 表模式的执行前登记校验只比对命令参数数组，不看登记入口，因此登记入口写错也回“与工作记录表一致”。 | 在逐行比对里加入入口核对：先用第 1 步的解析函数取表内应有的入口，形状有问题时报出该问题，形状正确但与已登记入口集合不一致时报出两边实际值。 | 执行前的登记校验不再对入口错误给出“一致”的结论。 | AC-02、AC-04 | 1、5 |
| 7 | src/workflow_loop/records.py | validate_table 的 test_entries 分支与行键取值 | 入口格式问题的位置只写到“行清单名称 第 N 行的 列名”，没有行键；按表登记打印失败清单时因此看不到是哪个测试项编号。 | 在行循环里取出该行清单的行键列名与取值，入口格式问题的位置改成“行清单名称 第 N 行（测试项编号 TC-xx）的 列名”，行键为空时保持原写法。 | 按表登记失败时输出里能直接看到主题、测试项编号、列名、实际值和正确形状。 | AC-02 | 4 |

#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| uv run python -m pytest tests/test_records.py tests/test_test_mapping.py tests/test_test_execution.py tests/test_test_report.py -q | 工作记录表校验、测试计划解析、测试任务登记与执行、结构化报告匹配四处现有行为在本次修改后是否仍成立。 | 退出码为 0，没有失败项；如果有失败，输出里能直接看到是哪个测试函数和哪一行断言。 |
| uv run python /tmp/wf_entry_repro/repro.py | 单入口填纯标题时的登记行为，以及填对时的登记与执行结论。 | 第 3 步不再是“登记成功”，而是抛出带列名、实际值和正确形状的登记失败；把该格改成 tests/test_upload.py::test_upload 后仍然判定 passed。 |
| uv run python /tmp/wf_entry_repro/multi.py | 一个测试项由同文件两个测试函数覆盖时的登记与执行。 | 把两个入口写成 JSON 数组后登记出两条入口，真实执行判定 passed；只填一个入口时仍然失败并说明报告里有未登记的入口。 |

### 2.4 未决问题

- 暂无

<a id="3-实施后记录"></a>
## 3. 实施后记录（代码实施与代码结果）

### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 代码位置（最终文件） | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | src/workflow_loop/test_mapping.py | L8-L8 | 为解析 JSON 数组写法的入口增加 json 标准库导入。 | uv run python -c "import workflow_loop.test_mapping" 成功，无导入错误。 | 已完成 |
| 2 | 1 | src/workflow_loop/test_mapping.py | L182-L204 | 把原 _entry_parts 内联的形状判断抽成 _entry_shape_problem，按“恰好一个 :: 、左边路径非空且不含空格、右边标识非空”分别返回不带栏目名的原因；_entry_parts 改为先调用它再拆分，文档模式的报错文字保持原有含义。 | uv run python -m pytest tests/test_test_mapping.py -q 通过，文档模式解析行为没有变化。 | 已完成 |
| 3 | 1 | src/workflow_loop/test_mapping.py | L207-L262 | 新增四个常量和 parse_official_target_names：按普通字符串或 JSON 数组取出入口，逐个检查元素类型与形状并查重，返回入口清单和一句带列名、实际值、正确形状与多入口例子的问题说明。 | 对纯标题、顿号串、单入口、JSON 数组四种取值各调用一次，前两种返回空清单加问题说明，后两种返回 1 条和 2 条入口。 | 已完成 |
| 4 | 2 | src/workflow_loop/test_mapping.py | L96-L115 | 给 TestPlanItem 增加 test_entries 字段和 registered_entries 属性：表模式有入口清单时用清单，文档模式回退到单个 test_entry。 | uv run python -m pytest tests/test_test_mapping.py -q 通过，既有构造方式不受影响。 | 已完成 |
| 5 | 2 | src/workflow_loop/test_mapping.py | L327-L342 | 表模式构造计划项时先解析“正式目标名称”，用解析结果填 test_entry 与 test_entries；形状有问题时留空，交给表校验和按表登记报具体问题，不在这里猜入口。 | 沙盒里填 JSON 数组的表构造出的计划项带两条入口。 | 已完成 |
| 6 | 2 | src/workflow_loop/test_mapping.py | L624-L634 | planned_test_source_paths 改成遍历 registered_entries 逐个取测试文件路径，不再只取单个入口。 | uv run python -m pytest tests/test_test_mapping.py tests/test_snapshots.py -q 通过。 | 已完成 |
| 7 | 3 | src/workflow_loop/records.py | L373-L393 | 改写表版本 2 的两条填写说明：“正式目标名称”写明它是登记入口的唯一来源、必须形状、pytest 与 vitest 各写什么、只写标题会被拒绝、多入口的 JSON 数组写法；“测试入口”写明它只进生成文档、不是登记入口。 | 在隔离沙盒重新生成测试计划表，读出的两条说明就是改写后的文字。 | 已完成 |
| 8 | 4 | src/workflow_loop/records.py | L801-L802 | 在类型列声明里给“正式目标名称”加 test_entries 类型，并注明判断标准在 test_mapping 里只定义一次。 | uv run python -m pytest tests/test_records.py -q 通过。 | 已完成 |
| 9 | 4 | src/workflow_loop/records.py | L1291-L1301 | 在 validate_table 的类型列分支里新增 test_entries 分支：调用解析函数，问题说明作为格式问题追加进清单，位置带行清单名称、行号和列名，并用表版本等于 2 限定生效范围。 | 沙盒里对填纯标题的表调用 validate_table，返回一条格式问题，文字含“测试项 第 1 行的 正式目标名称”。 | 已完成 |
| 10 | 5 | src/workflow_loop/test_execution.py | L247-L288 | prepare_task 表模式分支改为先解析“正式目标名称”，有问题说明就抛出 ValueError 结束登记，没问题时用解析结果构造计划项并把全部入口作为登记入口，删掉原来的 entries = [item.test_entry] if item.test_entry else [test_id] 兜底。 | 沙盒里填纯标题时登记抛出带列名和正确形状的错误，状态文件里没有该测试项的登记任务；填 JSON 数组时登记出两条入口。 | 已完成 |
| 11 | 6 | src/workflow_loop/test_execution.py | L380-L432 | _validate_prepared_tasks_from_tables 在收集表内命令时一并解析入口：形状有问题时把问题说明作为一条错误列出；形状正确但与已登记入口集合不同时列出两边实际值。 | 沙盒里两种正确填法都回“与工作记录表一致”，入口集合被真实比对过。 | 已完成 |
| 12 | 7 | src/workflow_loop/records.py | L1230-L1306 | 在行清单循环开始处取出 key_column，并在 test_entries 分支里用它拼出带行键的位置前缀；行键为空时仍写“行清单名称 第 N 行”。 | uv run python -m pytest tests/test_table_entry_registration.py -q 5 条全部通过；uv run python -m pytest tests/ -q 544 条通过。 | 已完成 |

### 3.2 实施中问题与处理

- 测试代码步骤按 AC-02 写断言时发现，按表登记打印的失败清单里只有“测试项 第 1 行”，没有测试项编号，不满足“写明测试项编号”这条标准。这属于产品代码问题，因此按流程退回代码实施登记这处改动，在 validate_table 的入口格式问题里补上行键，再回测试验证重新执行。

### 3.3 未完成内容

状态：无

### 3.4 代码结果

#### 3.4.1 实际代码修改

| 文件 | 代码位置（最终文件） | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| src/workflow_loop/test_mapping.py | L8-L634 | 增加 json 标准库导入，供解析 JSON 数组写法的登记入口使用。 TestPlanItem 增加 test_entries 元组字段；registered_entries 属性在有清单时返回清单，否则把单个 test_entry 包成一元组，没有入口时返回空元组。 _entry_shape_problem 逐条判断“恰好一个 :: ”“左边路径非空”“左边路径不含空格”“右边标识非空”，命中哪条就返回哪条原因，全部满足返回空串；_entry_parts 先调用它，有原因时按原有文字抛出 ValueError，否则拆成路径与目标。 parse_official_target_names 判断值是数组还是字符串；字符串以 [ 开头时用 json.loads 解析并要求结果是数组，解析失败或不是数组各有独立原因；逐个元素要求是非空字符串并通过形状判断；最后检查同一格内是否重复。任一条不成立返回空清单加一句问题说明，说明里含列名、实际值、正确形状、单入口例子和多入口 JSON 数组例子。 表模式构造计划项前先解析“正式目标名称”，把解析结果分别填进 test_entry（取第一条）和 test_entries（全部）；解析失败时两者留空，不用原始字符串顶替。 planned_test_source_paths 改为对每个计划项遍历 registered_entries，逐个入口调用既有的计划入口校验取测试文件路径；没有入口时按空值走原有校验以保留原报错。 | 模块可以解析 JSON 数组形式的单元格内容。 同一个计划项对象可以表达一个测试项的多个登记入口，文档模式的取值方式不变。 文档模式的报错含义不变，表模式可以复用同一套形状判断而不必抄一份。 调用方拿到的要么是规范化后的入口清单，要么是一句能直接照着改的问题说明。 表模式的计划项带规范化入口清单；形状错误不会被当成一个合法入口带进后续环节。 一个测试项覆盖两个测试文件时，两份文件都进入测试代码快照，不再只登记第一个。 | 多入口写法采用 JSON 数组，与同表“命令参数数组”一列一致，需要标准库解析。 一个测试项由多个测试函数覆盖是产品允许的做法，原数据结构只装得下一个入口。 同一条形状规则只在一个地方定义，避免两种模式口径漂移——本次缺陷的根因就是校验只存在于文档模式一侧。 表校验、按表登记和执行前登记校验三处都要用同一判断标准，这是 AC-02 与 AC-03 的共同前提。 原实现把单元格原值直接当入口，是缺陷记录第 6 节确认的根因位置之一。 支持多入口后，测试代码快照必须覆盖全部登记入口所在文件，否则第二个文件的改动不会使结果失效。 | AC-02、AC-03 | uv run python -m pytest tests/test_test_mapping.py -q 无失败。正式测试事实由 qa 环节给出。 开发检查脚本 /tmp/wf_entry_repro/check_fix.py 第四种填法登记出两条入口。 uv run python -m pytest tests/test_test_mapping.py -q 无失败，文档模式既有断言全部保持。 开发检查脚本四种填法的实际输出：前两种返回问题说明，后两种分别返回 1 条和 2 条入口。 开发检查脚本第四种填法的计划项带两条入口并在执行后精确匹配。 uv run python -m pytest tests/test_test_mapping.py tests/test_snapshots.py -q 无失败。 |
| src/workflow_loop/records.py | L373-L1306 | 表版本 2 的“正式目标名称”说明改写成四件事：本列是登记入口唯一来源、必须写成 项目相对路径::报告里的目标名 并与适配器输出逐字一致、pytest 写 nodeid 而 vitest 写 测试文件路径::测试标题、多入口写成 JSON 数组；同时点明只写标题会被登记拒绝。“测试入口”说明补上它只进入生成的测试计划文档、不是登记入口。 类型列声明新增“正式目标名称”对应 test_entries 类型，并在注释里写明与文档模式同一口径、判断标准只在 test_mapping 定义一次。 validate_table 的类型列分支新增 test_entries 处理：延迟导入 test_mapping 调用解析函数，问题说明非空时以“行清单名称 第 N 行的 …”为前缀作为格式问题追加进 problems；整个分支用表版本等于 2 限定，表版本 1 的冻结轮次不执行新校验。 行清单循环开始处取出该行清单的行键列名，test_entries 分支据此把位置前缀拼成“行清单名称 第 N 行（测试项编号 TC-xx）的 列名”，行键为空时退回不带行键的写法。 | 新生成或补齐的测试计划表里，这两列的说明带必须形状、可照抄的例子和多入口写法。 表校验开始把这一列当成有格式要求的列，而不是任意文本。 测试验证环节第二道门对形状不对的这一格报出格式问题并停留；旧版本表的判定口径不变。 按表登记失败时的问题清单能直接定位到具体测试项编号，而不只是行号。 | 填写说明没有写出必须形状，是填表人按字面填错的直接原因。 表 schema 缺少格式声明，使门禁按 R4 逐列校验时放行了任何取值。 问题要在门禁一次列全的清单里出现，而不是等到测试执行后由报告解析报出。 补行键是测试代码步骤真实发现的缺口：只有行号时，按表登记的失败清单不满足“写明测试项编号”这条验收标准。 | AC-01、AC-02 | 开发检查脚本每次都在隔离沙盒重新生成表并打印这两条说明，输出即改写后的文字。 开发检查脚本里纯标题与顿号串两种填法都被表校验报为格式问题。 开发检查脚本输出的表校验结论：两种错误填法各 1 条格式问题，两种正确填法 0 项问题。 tests/test_table_entry_registration.py::test_prepare_from_tables_rejects_bare_title_without_registering 与 ::test_table_gate_reports_bare_title_as_format_problem 覆盖它，本地 5 条全部通过。 |
| src/workflow_loop/test_execution.py | L247-L432 | prepare_task 表模式分支先调用解析函数：问题说明非空时直接抛 ValueError 结束登记，按表登记的调用方会给这句话补上主题和测试项编号再打印；解析成功时用清单构造计划项，并把全部入口去重排序后写入登记任务，原来的单入口兜底表达式删除。 _validate_prepared_tasks_from_tables 增加表内入口收集：逐行解析，形状有问题时把问题说明作为一条独立错误列出且不参与后续比对；形状正确时把表内入口集合与已登记入口集合比对，不同则列出两边实际值。 | 按表登记时形状不对当场失败，状态文件里不写入该测试项的登记任务；一个测试项可以登记多个入口。 执行前的登记校验不再对入口错误给出“与工作记录表一致”的结论。 | 登记入口是判定测试项通过与否的直接输入，这里是唯一能当场拦住错误的位置。 表模式原来只比对命令参数数组，是文档模式已有入口核对在表模式一侧的缺口。 | AC-02、AC-03、AC-04 | 开发检查脚本：错误填法登记被拒且状态文件里登记任务为空字典；两种正确填法登记入口分别为 1 条和 2 条，执行后判定 passed。 开发检查脚本里两种正确填法的执行前登记校验返回“全部 1 个测试项的登记任务与工作记录表一致”，入口集合被真实比对。 |

#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| uv run python -m pytest tests/test_records.py tests/test_test_mapping.py tests/test_test_execution.py tests/test_test_report.py -q | 工作记录表校验、测试计划解析、测试任务登记与执行、结构化报告匹配四处现有行为。 | 78 passed in 2.56s，退出码 0，没有失败项。 | 否 |
| uv run python /tmp/wf_entry_repro/check_fix.py | 纯标题、顿号连接两个入口、单个正确入口、JSON 数组两个入口这四种填法在表校验、按表登记、执行前登记校验和真实执行四处的表现。 | 前两种：表校验各报 1 条格式问题，登记被拒并打印列名、实际值和正确形状，状态文件里的登记任务为空字典。第三种：表校验 0 项问题，登记入口 1 条，执行退出码 0 判定 passed，机器记录精确匹配入口 1 条、实际执行数 1、跳过失败错误均为 0。第四种：表校验 0 项问题，登记入口 2 条，执行判定 passed，精确匹配入口 2 条、实际执行数 2、跳过失败错误均为 0。 | 否 |
| uv run python /tmp/wf_entry_repro/repro.py | 缺陷复现阶段原样的脚本在修复后的表现，用于确认原缺陷路径已经改变。 | 脚本在第 3 步登记处抛出 ValueError 并中止，错误文字为“正式目标名称 里的 '验证上传完成' 必须用一个 :: 连接项目相对文件和可定位标识……”；原先这一步是静默成功、到执行后才判 failed。 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [登记测试项时入口填不对当场说清而不是测完才失败 测试计划](../qa/登记测试项时入口填不对当场说清而不是测完才失败_测试计划.md) | 代码结果确认后，在测试验证开始时确认范围和通过标准 |
| 下游 | [登记测试项时入口填不对当场说清而不是测完才失败 测试结果](../qa/登记测试项时入口填不对当场说清而不是测完才失败_测试结果.md) | 同一测试验证阶段连续完成测试代码、登记、执行和结果 |
| 下游 | [登记测试项时入口填不对当场说清而不是测完才失败 验收结果](../acceptance/登记测试项时入口填不对当场说清而不是测完才失败_验收结果.md) | 正式测试后执行主题验收 |
