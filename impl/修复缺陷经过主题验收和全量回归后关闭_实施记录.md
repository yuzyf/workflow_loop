# 【实施】修复缺陷经过主题验收和全量回归后关闭

- 工作流编号：2026-07-20-0637-from_scratch
- 验收主题：修复缺陷经过主题验收和全量回归后关闭

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品规则 | 缺陷复现阶段确定一个验收主题；主题验收通过后先保留待回归状态；最终全量回归和整体验收都通过后才关闭缺陷。 | [缺陷复现与主题规则](../spec/功能_复现产品缺陷.md#4-规则)<br>[缺陷状态与关闭规则](../spec/功能_验收并完成交付.md#4-规则) |
| 产品设计 | 修 bug 的验收目标、验收范围和 AC-01 至 AC-03。 | [本主题验收计划](../acceptance/修复缺陷经过主题验收和全量回归后关闭_验收计划.md) |
| 测试计划 | TC-01 检查主题对应，TC-02 检查主题验收后的状态，TC-03 检查回归和整体验收共同决定关闭。 | [本主题测试计划](../qa/修复缺陷经过主题验收和全量回归后关闭_测试计划.md) |
| 代码设计 | 缺陷记录模块保存追加结果，阶段门禁模块检查前置结果，命令层在阶段确认时更新缺陷状态。 | [代码架构设计](../spec/代码架构设计.md#53-三道门禁与阶段推进) |
| 追踪记录 | 每条验收条件的实施、测试、验收和代码设计位置由全局追踪表记录。 | [需求交付追踪表](../需求交付追踪表.md) |
| 穿刺结论 | 暂无。本工作流已由用户确认跳过穿刺，本主题没有待验证的技术不确定性。 | 暂无 |

<a id="2-实施前计划"></a>
## 2. 实施前计划

### 2.1 预期产品结果

修 bug 工作流完成后，维护者可以从缺陷记录看到完整的处理状态：缺陷先在复现阶段绑定一个验收主题；该主题通过验收后，状态明确为“主题验收通过，待全量回归”；全量回归失败时明确回到“回归失败，重新处理中”；只有所有主题验收、最终全量回归和整体验收都通过后，缺陷记录和 `bug/索引.md` 才显示“已修复并验收”。原来的复现条件、实际结果、期望结果和根因内容保持不变。

### 2.2 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件和测试项 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/bug_record.py` | `update_status()`（更新缺陷状态）、`_replace_result_update()`（追加或替换阶段结果）、`_update_index_status()`（同步缺陷索引状态） | 按工作流编号和验收主题找到缺陷记录，按阶段追加结果，并同步 `bug/索引.md`；重复执行同一阶段时不重复追加相同区块。 | 复核并补齐状态更新的固定顺序：主题验收只写“主题验收通过，待全量回归”，回归失败只写“回归失败，重新处理中”，整体验收通过才写“已修复并验收”；继续保留原复现事实，只替换同一工作流、同一阶段的结果区块。 | 缺陷记录追加 `## 8. 修复与验收结果` 下的阶段结果；`bug/索引.md` 的状态列随阶段更新；不修改第 1 至第 7 节。 | AC-02、AC-03；TC-02、TC-03 | 已确认验收主题和测试计划；先完成门禁代码校验。 |
| 2 | `src/workflow_loop/artifact_validation.py` | `validate_reproduce_documents()`（校验缺陷复现）、`validate_test_execution_results()`（校验主题测试结果）、`validate_topic_acceptance_results()`（校验主题验收结果）、`validate_final_regression_state()`（校验最终回归状态）、`validate_overall_acceptance_prerequisites()`（校验整体验收前置条件） | 缺陷复现检查真实复现字段、根因和一个验收主题；主题测试和主题验收分别检查各自结果；最终回归检查当前工作流的 `state.regression_test`（最终回归状态）；整体验收复核主题结果和最终回归状态。 | 把“缺陷能否关闭”固定为可判断的门禁条件：缺陷记录必须有一个主题；每个主题的测试和验收结果必须通过；统一测试入口退出码必须为 0；整体验收只能在这些结果齐全后放行。 | 门禁失败时输出缺少的主题、结果或状态；全部前置条件通过时才允许用户确认整体验收。 | AC-01、AC-03；TC-01、TC-03 | 先确认 `bug/`、`qa/`、`acceptance/` 的结果文件格式。 |
| 3 | `src/workflow_loop/stages/stages.py` | `TestCodeStage.code_validate()`（测试代码阶段门禁）、`TestExecutionStage.code_validate()`（主题测试门禁）、`TopicAcceptanceStage.code_validate()`（主题验收门禁）、`RegressionTestStage.code_validate()`（回归门禁）、`OverallAcceptanceStage.code_validate()`（整体验收门禁） | 先确认测试代码，再执行主题测试，再核对主题验收；最终回归阶段自动执行统一测试入口；整体验收阶段检查所有主题结果和回归状态。 | 保持阶段顺序，禁止跳过：测试代码未写完不能执行测试，测试未通过不能主题验收，主题验收未完成不能回归，回归未通过不能整体验收，整体验收前不能关闭缺陷。 | 阶段门禁状态按实际结果推进；失败时留在当前阶段并输出缺失项，不写入“已修复并验收”。 | AC-02、AC-03；TC-02、TC-03 | 先完成实施代码，再按 `test_code → test_execution → topic_acceptance` 运行后续门禁。 |
| 4 | `src/workflow_loop/cli.py` | `apply_stage_completion_updates()`（阶段确认后的固定记录更新）、`cmd_gate()`（门禁总入口） | 阶段确认时更新追踪表；修 bug 在主题执行确认时追加主题验收状态，在整体验收确认时追加已修复状态；门禁失败时不推进。 | 复核固定更新与阶段推进的先后关系：先完成结果校验和缺陷记录更新，成功后才写用户确认、阶段完成和下一阶段状态；更新失败时保持当前阶段，提示补齐记录。 | 写入缺陷记录、`bug/索引.md`、`.workflow_loop/state.json` 和 `需求交付追踪表.md`；命令输出下一步或具体失败原因。 | AC-02、AC-03；TC-02、TC-03 | 先完成 `bug_record.py` 和结果校验，再接入命令层。 |

### 2.3 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| `python3 -m pytest tests/test_bug_record.py -q` | 缺陷状态追加、原始复现事实保护、重复更新幂等性。 | 主题验收、回归失败和整体验收分别得到规定状态；重复执行不重复追加；原复现章节不变化。 |
| `python3 -m pytest tests/test_stages.py -q` | 主题执行、最终回归和整体验收门禁。 | 缺少结果或回归失败时门禁拒绝推进；全部结果通过时才允许整体验收。 |
| `python3 -m pytest tests/test_commands.py -q` | `workflow gate`（工作流门禁命令）确认后的状态和下一步输出。 | 固定记录更新失败时不推进；成功时状态、缺陷记录和下一阶段一致。 |
| `./scripts/test_all.sh` | 全部现有单元测试和集成测试。 | 全量测试通过，且没有破坏其他工作流阶段。 |
| `python3 -m compileall -q src tests` | Python 源码和测试源码的语法。 | 命令无输出并以退出码 0 结束。 |

### 2.4 未决问题

暂无

<a id="3-实施后记录"></a>
## 3. 实施后记录

### 3.1 实际代码修改

| 对应计划步骤 | 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件和测试项 |
|---|---|---|---|---|---|
| 1 | `src/workflow_loop/bug_record.py` | `update_status()`（更新缺陷状态）、`record_topic_acceptance_pass()`（记录主题验收）、`record_regression_failure()`（记录回归失败）、`record_overall_acceptance_pass()`（记录整体验收） | 按当前工作流编号和验收主题找到唯一缺陷记录，向“修复与验收结果”追加或替换当前阶段结果；主题验收、回归失败和整体验收分别使用固定状态。 | 保留缺陷原有复现事实和根因内容；`bug/索引.md` 的状态按阶段更新为“主题验收通过，待全量回归”“回归失败，重新处理中”或“已修复并验收”。 | AC-01、AC-02、AC-03；TC-01、TC-02、TC-03 |
| 2 | `src/workflow_loop/artifact_validation.py` | `validate_test_execution_results()`（校验主题测试结果）、`validate_topic_acceptance_results()`（校验主题验收结果）、`validate_final_regression_state()`（校验最终回归状态）、`validate_overall_acceptance_prerequisites()`（校验整体验收前置条件） | 校验每个主题的测试结果和验收结果必须绑定当前工作流并明确写“通过”；校验 `state.regression_test.status`（最终回归状态）为“passed”；整体验收同时复核全部主题结果和最终回归状态。 | 缺少主题结果、工作流编号不一致、结果不明确或回归未通过时，门禁停在当前阶段并输出具体缺项；前置结果齐全后才允许用户确认整体验收。 | AC-01、AC-03；TC-01、TC-03 |
| 3 | `src/workflow_loop/stages/stages.py` | `TestExecutionStage.code_validate()`、`TopicAcceptanceStage.code_validate()`、`RegressionTestStage.code_validate()`、`OverallAcceptanceStage.code_validate()` | 主题测试阶段检查测试结果；主题验收阶段检查主题验收结果；最终回归阶段自动调用统一测试入口并检查状态；整体验收阶段检查全部主题结果和最终回归状态，阶段不能跳过顺序。 | 阶段门禁只在对应结果齐全且可判断时通过；失败时不推进状态，也不提前关闭缺陷。 | AC-02、AC-03 |
| 4 | `src/workflow_loop/cli.py` | `apply_stage_completion_updates()`（阶段确认后的固定更新）、`cmd_gate()`（门禁总入口） | 在修 bug 工作流中，主题执行确认时追加“主题验收通过，待全量回归”，整体验收确认时追加“已修复并验收”；最终回归明确失败时追加“回归失败，重新处理中”。固定记录更新成功后才写用户确认和推进状态。 | 缺陷记录、`bug/索引.md`、`需求交付追踪表.md`、`.workflow_loop/state.json` 和 Journal 的更新顺序保持一致；更新失败时停在当前阶段。阶段共用的追踪表更新细节记录在[追踪表主题实施记录](./需求交付各阶段通过追踪表双向关联_实施记录.md)。 | AC-02、AC-03；TC-02、TC-03 |

### 3.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `PYTHONPATH=src .venv/bin/python -m pytest tests/test_bug_record.py tests/test_stages.py tests/test_commands.py -q` | 缺陷状态追加、主题执行/回归/整体验收门禁和命令推进。 | 命令退出码为 0；相关测试执行完成。 | 否 |
| `./scripts/test_all.sh` | 全部现有单元测试和集成测试。 | 命令退出码为 0；终端统计 `126 passed in 10.00s`。此记录只作为实施期间开发检查，不作为正式测试结果。 | 否 |
| `PYTHONPATH=src .venv/bin/python -m compileall -q src tests` | Python 源码和测试源码语法。 | 无输出，退出码为 0。 | 否 |

### 3.3 未完成内容

暂无

#### 3.4.2 开发检查记录

| 检查项 | 范围 | 反馈 | 是否还需修改 |
|---|---|---|---|
| pytest 全量 | 467 项测试（含新增 8 项） | 全部通过 | 否 |
| 门禁自查 | workflow gate impl / status / test prepare --from-tables 真实执行 | 输出符合预期 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/修复缺陷经过主题验收和全量回归后关闭_验收计划.md) | 定义缺陷关闭前必须满足的用户结果和验收条件。 |
| 上游 | [测试计划](../qa/修复缺陷经过主题验收和全量回归后关闭_测试计划.md) | 定义本主题对应的 TC-01 至 TC-03 测试范围。 |
| 上游 | [代码架构设计](../spec/代码架构设计.md) | 说明命令层、门禁校验层和缺陷记录层的职责。 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本主题在整个需求交付链路中的位置。 |
| 下游 | [主题测试结果](../qa/修复缺陷经过主题验收和全量回归后关闭_测试结果.md) | 实施完成后记录实际测试结果。 |
| 下游 | [主题验收结果](../acceptance/修复缺陷经过主题验收和全量回归后关闭_验收结果.md) | 测试通过后记录实际用户验收结果。 |
