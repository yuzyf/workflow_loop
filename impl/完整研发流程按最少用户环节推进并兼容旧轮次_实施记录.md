# 【实施】完整研发流程按最少用户环节推进并兼容旧轮次

- 工作流编号：2026-08-10-0817-product_change
- 验收主题：完整研发流程按最少用户环节推进并兼容旧轮次

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品设计 | 三种完整研发意图统一使用“验收计划 → 代码实施 → 测试验证 → 主题验收 → 最终全量回归 → 整体验收 → 最终代码设计同步”，旧五个阶段不再作为用户环节 | [产品总说明](../spec/产品总说明.md#6-产品通用规则)、[开始或继续一轮工作](../spec/功能_开始或继续一轮工作.md#4-规则) |
| 验收条件 | AC-01 至 AC-04：精简新路径、从零最低限度实现设计归入代码计划、旧轮次逐项迁移、项目设计初始化标记按真实架构形成时机写入 | [验收条件](../acceptance/完整研发流程按最少用户环节推进并兼容旧轮次_验收计划.md#ac-01) |
| 代码设计 | 阶段路径只生成 `impl`（代码实施）和 `qa`（测试验证）主干；旧阶段仅供迁移识别；迁移必须先检查后一次写入 | [当前研发路径与活动轮次迁移](../spec/代码架构设计.md#513-当前研发路径与活动轮次迁移)、[阶段与门禁层](../spec/代码架构设计.md#44-阶段与门禁层) |
| 穿刺结论 | 本轮已经确认跳过穿刺，没有本轮穿刺结论或可复用资产 | 暂无 |

## 2. 实施前计划（代码计划）
<a id="2-实施前计划"></a>

### 2.1 预期产品结果

新建或继续完整研发轮次时，用户只看到实际需要的产品设计、项目设计初始化、缺陷复现或穿刺前置，以及统一的“验收计划、代码实施、测试验证、主题验收、最终全量回归、整体验收、最终代码设计同步”主干。旧轮次第一次继续时按当前文件、代码和机器记录迁移，第二次继续不再改变状态；从零创建只在最终代码设计同步确认后写入项目设计已初始化状态。

主题验收结果严格使用当前模板中的“验收记录编号”。符合模板且编号与当前状态一致的结果可以通过门禁；字段缺失或编号不一致时，门禁必须一次指出具体问题，不能要求文档补写已经取消的旧字段。

### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 模块与职责 | `path_composer.py` 只生成当前流程路径；`cli.py` 负责旧活动轮次的一次性事务迁移；`state.py` 只保存路径版本、内部步骤和旧事实摘要；规范、模板、产品文档和架构文档各自只保留当前职责。 | 路径生成、迁移写入、状态事实和文档规则分开维护，删除旧文件后不会再形成第二套可执行流程。 | AC-01、AC-02、AC-03 |
| 接口与调用顺序 | 新轮次由 `build_stage_path()`（生成阶段路径函数）直接得到 `impl -> qa` 主干；旧轮次先由 `ensure_stage_path_current()`（确保当前阶段路径函数）预检并迁移，再执行原命令；从零项目在最终代码设计同步确认后写初始化标记。 | 新旧轮次最终进入同一条主干，同时保证只读命令不暗中迁移，失败时不留下半迁移状态。 | AC-01、AC-03、AC-04 |
| 数据、状态与副作用 | 状态保存 `stage_path_version`（阶段路径版本）、`legacy_stage_facts`（旧阶段事实摘要）和 `internal_step`（阶段内部步骤）；迁移只改当前状态、对应追踪事实和已失效结果，公开文档入口只指向仍存在的产品、架构与 ADR。 | 每项写入都有明确恢复范围，旧事实可解释但不会重新变成用户阶段。 | AC-03、AC-04 |
| 错误与边界 | 迁移前完整核对状态、追踪表和待保留事实；任一写入失败恢复原字节。没有独立职责的旧规范、旧总文档和孤立流程图直接删除，普通历史记录中的已发生事实不改写。 | 防止迁移失败破坏活动轮次，也防止为了兼容历史继续维护冲突规则。 | AC-02、AC-03、AC-04 |
| 主题验收字段契约 | `_validate_topic_acceptance_result()`（校验单个主题验收结果的函数）按当前模板读取“验收记录编号”，并与 State Snapshot（状态快照）中当前有效验收记录的 `record_id`（验收记录唯一编号）逐项比较；不把旧字段“程序记录”作为别名。 | 规范、模板和门禁必须使用同一个字段名；拒绝旧别名可以防止为了迁就实现重新污染当前文档。 | AC-01 |

### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 | 共享代码归属 | 风险 | 完成标准 |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/state.py` | `StageState`、`WorkflowState`、`state_from_dict()` | 状态只认识旧阶段集合，没有代码实施和测试验证的内部步骤，也没有显式迁移版本和旧事实映射 | 增加可向后读取的内部步骤、迁移版本和旧事实映射；旧字段继续反序列化，新状态只使用 `impl` 和 `qa`；缺失新字段时取安全未完成值 | 旧 `state.json` 可读取；新状态能说明迁移是否已执行、当前内部步骤及保留或失效的旧事实 | AC-03、AC-04 | 无 | 状态模型由本文件统一定义，主题二、三、四共用 | 默认值错误可能把旧事实误判为已确认 | 旧状态往返读写不丢字段，新路径状态不再创建五个旧阶段 |
| 2 | `src/workflow_loop/path_composer.py` | `FROM_SCRATCH_PATH`、`PRODUCT_CHANGE_BASE`、`BUGFIX_BASE`、`build_stage_path()` | 三条完整研发路径仍包含 `code_design`、`revise_code_design`、`test_plan`、`test_code`、`test_execution` | 把共享主干改为 `acceptance_plan → impl → qa → topic_acceptance → regression_test → overall_acceptance → update_code_design`；按意图保留 `spec`、`project_design_init`、`reproduce` 和可选 `spike` | 新轮次保存的 `stage_path` 不再出现五个取消阶段，顺序和适用前置保持明确 | AC-01、AC-02 | 1 | 路径组成只在本文件维护 | 路径顺序变化会影响所有命令和旧状态 | 三种意图和项目初始化分支都得到验收计划规定的路径 |
| 3 | `src/workflow_loop/stages/stages.py` | 旧阶段策略注册、`ImplStage`、新增 `QaStage`、`UpdateCodeDesignStage`、`QaStage.additional_doc_paths()` | 五个旧阶段仍是新路径可实例化阶段；从零初始化标记仍可能在早期代码设计完成时写入；当前 QA 没有加载正式测试结果模板 | 让旧类只供迁移读取；用 `ImplStage` 和新增 `QaStage` 承担新主干；将从零初始化写入移动到最终 `UpdateCodeDesignStage` 用户确认之后，已有项目初始化时机不变；让 QA 同时加载测试计划和测试结果模板及各自规范 | 阶段实例和用户提示与新路径一致；从零项目在最终同步前 `project_design_initialized`（项目设计已初始化标记）保持未完成；AI 写测试结果前能看到门禁实际校验的固定字段 | AC-01、AC-02、AC-04 | 1、2 | 阶段策略是主题二和主题三的公共实现 | 旧类完全删除会导致历史状态无法读取；漏加载结果模板会让文档生成和门禁要求不一致 | 新路径可实例化，旧路径可迁移，从零和已有项目的标记时机分别正确；QA 材料清单包含两份模板和四份规范 |
| 4 | `src/workflow_loop/cli.py` | `STAGE_LABELS`、`current_stage_next_instruction()`、`ensure_stage_path_current()`、迁移预检与提交、阶段完成更新 | 用户提示和命令路由仍显示旧阶段；迁移只识别一种旧顺序并成段清空测试事实；重复继续靠偶然路径相等避免再次变化 | 统一显示 `impl` 和 `qa`；第一次会写状态的继续命令先生成迁移计划，逐项判断旧验收、设计草稿、实施、测试计划、测试代码和机器记录是否仍有效，再事务写入；记录迁移版本，重复调用零写；无参数继续保持只读预告 | 输出明确列出保留、失效、当前位置和唯一下一命令；有效旧内容不因改名丢失，冲突内容才失效 | AC-01、AC-03、AC-04 | 1、2、3 | 命令编排由本文件统一处理，门禁凭据细节由主题四承担 | 迁移中途失败可能造成状态和正式文档不一致 | 迁移预检失败零写，成功一次完成，第二次继续状态、文件和日志不变 |
| 5 | `src/workflow_loop/verification.py` | 旧阶段失效顺序、阶段哈希和恢复动作映射 | 失效链按三个拆分测试阶段运行，旧迁移可能整体清除后续事实 | 将阶段依赖改为 `acceptance_plan → impl → qa`；为旧事实迁移提供逐项比较结果；保留正常下游链接、时间和编号；只把不能证明仍有效的内容标为待重新核对 | 新路径的下游失效从最早真实变化开始，迁移可说明每项旧事实的保留或失效原因 | AC-03 | 1、2、3 | 详细凭据和精确主题失效由主题四完整实现 | 只有聚合哈希的历史状态不能补造逐文件证据 | 有证据的旧事实保留；证据不足明确未检查，不静默当作有效或全部删除 |
| 6 | `src/workflow_loop/artifact_validation.py` | 阶段索引、追踪关系和阶段名校验 | 部分校验仍假定测试计划、测试代码和测试执行是独立阶段 | 接受单一 `qa` 阶段继续使用分开的测试计划和测试结果文档；移除新路径对旧阶段的依赖；历史正式文档仍可导航 | 新环节名称变化不破坏现有正式产物路径和主题关系校验 | AC-01、AC-03 | 2、3 | 正式产物结构由本文件集中校验 | 误改列名会破坏历史文档校验 | 当前轮新索引通过，历史索引和文档仍可读取 |
| 7 | `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` | 完整研发流程说明和阶段顺序 | 安装到项目的规范资源仍描述旧阶段和多次确认 | 更新为精简主干、代码实施三段式、单一测试验证、旧轮次迁移和初始化标记真实时机 | 包内规范不再把后续安装项目引回旧流程 | AC-01、AC-02、AC-03、AC-04 | 2、3、4 | 全局生命周期规则供全部阶段共用 | 规范与代码不同步会再次产生错误计划 | 规范、路径、CLI 输出和状态字段使用同一阶段顺序和中文含义 |
| 8 | `src/workflow_loop/role_doc.py` | `DOC_OVERVIEW`（文档概览）、`ROLE_DOC_MAP`（阶段角色映射） | 文档概览和备用角色映射仍把初步代码设计、设计期代码修订和三个拆分测试阶段写成当前用户流程 | 文档概览只说明当前主干；角色映射只保留当前路径会实例化的阶段，旧阶段名称只在迁移映射中识别 | 开始和讨论命令不会再向 AI 输出已经取消的工作顺序 | AC-01、AC-02 | 2、3 | 用户可见阶段说明集中在本文件和 CLI 当前角色表 | 保留两套阶段说明会继续产生相互冲突的下一步 | 概览、阶段路径和 CLI 下一步使用同一当前顺序 |
| 9 | `src/workflow_loop/data/Standardized_Repository/code_design/code_design.md` | 代码架构文档编写规则 | 仍要求讨论计划代码、计划测试位置和未实现内容，与现状初始化和最终同步职责冲突 | 只规定怎样根据产品文档、真实代码、测试和运行证据建立或同步当前架构；未实现要求作为差异处理，不写入当前实现 | 项目设计初始化和最终同步读取同一份现状规则，不再生成实施前代码设计 | AC-02、AC-04 | 2、3 | 当前架构规则供项目设计初始化和最终同步共用 | 计划与现状混写会让最终门禁反复 | 规则中的代码位置和验证位置都要求真实可核对事实 |
| 10 | `src/workflow_loop/data/Template_Repository/code_design/code_design.md` | 代码架构文档模板 | 模板同时允许计划路径、计划符号和尚未实现内容，最终章节又要求全部是真实实现 | 删除计划内容入口，只保留当前代码位置、证据状态和已经确认的差异处理 | 新生成或最终同步的架构文档只有一套事实口径 | AC-02、AC-04 | 9 | 当前架构模板供项目设计初始化和最终同步共用 | 同一模板自相矛盾会让生成文档无法稳定通过 | 模板正文只描述真实实现，差异表不把计划冒充现状 |
| 11 | `src/workflow_loop/data/Standardized_Repository/code_design/revise_code_design.md` | 独立设计期代码修订规范 | 新路径不加载该文件，保留它只会形成第二套无执行入口的规范 | 删除文件；历史状态兼容只保留阶段名称和状态迁移代码，不保留可执行规范 | 安装包和当前活动材料仓库都不再包含孤立规范 | AC-01、AC-02、AC-03 | 2、3、9 | 迁移职责属于状态和 CLI，不属于规范文件 | 只删文件但仍有当前加载者会导致材料失败 | 现行阶段无引用，旧轮次先迁移后加载当前阶段材料 |
| 12 | `DESIGN.md` | 根目录第一版实现设计 | 与当前代码架构文档重复，且仍把五个已取消阶段和旧命令写成当前设计 | 删除文件；当前实现只由 `spec/代码架构设计.md` 说明 | 仓库不再有两套相互冲突的实现真相源 | AC-01、AC-02 | 2、3、10 | 当前代码架构归正式架构文档 | 删除前不修引用会产生坏链接 | 全部当前规则和架构都可从正式产品、架构或 ADR 文档获得 |
| 13 | `CONTEXT.md` | 根目录旧术语与约束合集 | 同时混入产品规则、实现规则、历史反例和旧版本事实，与产品文档、架构文档和 ADR 重复 | 把现行引用改到对应产品功能、代码架构或 ADR，然后删除文件；已发生的普通文字记录保留为事实 | 产品规则、当前实现和历史决定分别只有一个真相源 | AC-01、AC-02、AC-04 | 7、9、10、12 | 产品归 `spec/产品总说明.md` 和功能文档，实现归代码架构，取舍理由归 `docs/adr/` | 直接删除会破坏 39 个正式链接和发布测试 | 链接和测试全部迁移，仓库内无当前依赖 |
| 14 | `docs/workflow_loop_design_overview.drawio` | 旧流程可视化源文件 | 无当前文档入口，内含已取消阶段的流程图 | 删除文件，不用新名称保留旧图 | 维护者不会从孤立图源读到错误顺序 | AC-01、AC-02 | 12 | 当前流程由产品和架构文档说明 | 未确认内容可能丢失 | 文件无现行引用，其有效职责已被正式文档覆盖 |
| 15 | `README.md` | 仓库结构和详细文档入口 | 把 `DESIGN.md` 和 `CONTEXT.md` 列为当前权威文档 | 删除两个入口，只链接产品总说明、功能文档、代码架构、ADR 和追踪表 | 公开说明只导向当前事实 | AC-01、AC-02 | 12、13 | README 只作为安装与导航入口 | 测试仍强制旧链接时会失败 | 公开文档链接全部存在并职责单一 |
| 16 | `tests/test_public_project.py` | 公开项目文档契约 | 强制 README 列出两份已失效总文档 | 改为要求当前产品、架构、ADR 和追踪入口，并断言旧总文档不存在 | 公开项目测试阻止旧真相源回归 | AC-01、AC-02 | 15 | 公开资料契约由本测试维护 | 只删断言会降低导航覆盖 | 新入口存在，旧文件不存在 |
| 17 | `tests/test_release_workflow.py` | 版本规则证据集合 | 直接读取 `CONTEXT.md` 作为当前版本规则依据 | 改读产品总说明、安装功能文档和版本 ADR，输出不再声称旧文件是事实源 | 发布检查仍验证未占用新版本号规则 | AC-01、AC-04 | 13 | 产品规则与决策理由分开维护 | 证据键名变化会破坏历史快照断言 | 当前测试只读取存在的权威文档 |
| 26 | `src/workflow_loop/project.py` | 项目设计初始化判定注释 | 注释把已删 `CONTEXT.md` 当作规则来源 | 改成自足注释，直接说明不能用架构文档是否存在判定跳过 | 源码注释不依赖被删文档 | AC-01、AC-04 | 13 | 项目字段规则由项目模型代码和产品文档说明 | 只修注释不得改运行行为 | 代码差异只有注释 |
| 27 | `src/workflow_loop/installer.py` | 安装时 Agent 契约注释 | 注释把已删 `CONTEXT.md` 当作规则来源 | 改成自足注释，直接说明目标文件整份覆盖边界 | 源码注释不依赖被删文档 | AC-01、AC-04 | 13 | 安装行为由安装功能文档和实现说明 | 不得借注释修正改代码 | 代码差异只有注释 |
| 28 | `src/workflow_loop/acceptance_records.py` | `_automated_items_are_current()`（核对自动化测试当前性函数） | `record_is_current()`（判断验收记录是否当前有效的函数）已优先读取 `qa.test_tasks`（测试验证任务记录），但建立自动验收记录前的核对函数仍只读取旧 `test_execution.test_tasks`（测试执行阶段任务记录） | 两个入口共用同一条阶段选择规则：当前轮次优先读取 `qa`，只在迁移中的旧轮次没有 `qa` 状态时回退读取 `test_execution`；缺少两种状态时返回同时说明两个可接受来源的错误 | 新路径在主题验收时能把 `qa` 中当前有效的精确机器记录编号写入自动验收记录；旧轮次仍可读取已保留的测试执行记录 | AC-01、AC-03 | 1至6 | 主题验收的机器证据选择统一由本文件负责 | 两个函数各自选阶段会再次出现一处支持新路径、另一处仍读旧状态的分裂 | 状态只有 `qa` 时可建立并复核自动验收记录；状态只有 `test_execution` 时旧兼容行为不变 |
| 29 | `src/workflow_loop/artifact_validation.py` | `_validate_topic_acceptance_result()`（校验单个主题验收结果的函数） | 当前模板、主题验收规范和正式结果都使用“验收记录编号”，但门禁仍要求旧字段“程序记录”，并用旧字段与 `record.record_id`（状态中的验收记录编号）比较；因此符合当前模板的正确结果会被错误拒绝 | 把必填字段和编号一致性检查统一改为“验收记录编号”；缺失时报告缺少具体“验收记录编号”，编号不一致时报告“验收记录编号不一致”；保留实际结果、验收证据、机器测试记录、人工回答和确认时间的现有严格校验；不接受“程序记录”作为兼容别名 | 正确的当前模板文档可以通过主题验收门禁；缺失字段、错误编号或只写旧字段仍会被拒绝；状态结构和其他四个主题的有效记录不改变 | AC-01 | 28 | 当前主题验收结果的字段契约由模板、规范和本函数共同约束 | 若同时接受新旧字段，会继续产生两套当前写法；若误改其他字段，会扩大验收语义 | 当前字段且编号一致时校验通过；删除“验收记录编号”、写入错误编号或仅使用“程序记录”时分别得到稳定、可定位的失败信息 |

删除 `CONTEXT.md` 前，还要迁移九份正式过程文档中的 39 个链接：`spec/代码架构设计.md`、`需求交付追踪表.md`、三份历史验收计划和三份对应实施记录，再加 `README.md`。这些正式文档的链接修正不是业务代码修改：它们由整轮开工基线保护，不进入代码计划、真实代码差异和实施结果的三方文件集核对。链接只改到语义对应的当前产品功能文档；已发生的历史结论和代码结果不改写。

`spec/代码架构设计.md` 除删除已失效依据链接外，当前代码映射和最终验证事实仍在 `update_code_design`（最终代码设计同步）环节根据完成后代码、测试和验收结果填写，代码实施阶段不提前把计划写成已验证事实。
实施顺序固定为：先扩展状态兼容读取，再修改新路径和阶段策略，然后实现迁移与完成时机，最后调整失效映射和正式文档校验。迁移代码写状态前必须完成全部只读检查；任何一步发现无法可靠映射的旧事实，都保留原文件并把对应内部状态标为待重新核对，不先删除。

包内源资源完成后，另执行一次运行材料同步：将 `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` 原样写入当前项目的 `.workflow_loop/Standardized_Repository/global/workflow_lifecycle.md`。该副本是工作流过程材料，不属于业务代码修改计划，因此不纳入回退基线的代码文件集合；同步动作必须在代码结果记录前完成并逐字节核对。

#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| 读取三种工作意图的阶段路径 | `build_stage_path()` 的从零创建、修改产品、修复缺陷和项目初始化分支 | 三条路径都含统一主干，不含五个取消的用户阶段 |
| 旧状态 JSON 往返检查 | `state_from_dict()` 和状态保存 | 旧字段可读，新字段有安全默认值，保存后仍能恢复全部有效事实 |
| 迁移预览与重复执行检查 | 活动旧轮次的只读继续、首次写入继续和第二次继续 | 只读命令零写；首次迁移逐项列出保留与失效；第二次执行零变化 |
| 项目初始化标记时机检查 | 已有项目初始化和从零创建最终同步 | 已有项目在初始化确认后写入；从零创建直到最终同步确认前始终未写入 |
| 静态阶段名扫描 | `src/workflow_loop` 中面向用户的提示和新路径分支 | 新命令不再把五个旧阶段作为下一步，旧名字只留在迁移兼容位置 |
| 阶段材料可达性扫描 | 包内和活动副本中的模板、规范及 `StageStrategy.materials()`（阶段材料清单） | 每份保留资源至少有一个当前阶段加载者；唯一删除项没有当前加载者 |
| 主题验收机器证据来源局部检查 | 构造只有 `qa.test_tasks`（新测试验证任务状态）和只有 `test_execution.test_tasks`（旧测试执行任务状态）的两类已通过记录 | 两类状态都能返回当前有效的精确机器记录编号；不存在任务或记录不完整时继续拒绝建立验收记录 |
| 主题验收字段契约静态核对 | `_validate_topic_acceptance_result()` 中的必填字段、字段读取和错误信息 | 三处都使用“验收记录编号”，不再读取或要求旧字段“程序记录”；其他逐字段一致性检查保持不变 |
| `.venv/bin/python -m compileall -q src/workflow_loop/artifact_validation.py` | 主题验收门禁最小修复的 Python 语法 | 命令退出码为 `0`，没有语法错误输出；正式正反例测试留在后续 `qa`（测试验证）阶段执行 |

### 2.4 未决问题

暂无

## 3. 实施后记录
<a id="3-实施后记录"></a>

### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 类、函数或配置项 | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | `src/workflow_loop/state.py` | `WorkflowState`、`StageState` 和 `state_from_dict()` | 增加当前路径版本、旧阶段事实摘要、实施与 QA 内部步骤及旧状态安全默认值。 | 旧状态样本可以读取；新状态不生成五个旧用户阶段。 | 已完成 |
| 2 | 2、3 | `src/workflow_loop/path_composer.py`、`src/workflow_loop/stages/stages.py` | 三种研发路径常量、`QaStage`（测试验证阶段策略） | 把新轮次主干收敛为 `impl -> qa`，增加单一 QA 策略并保留旧阶段类供迁移识别。 | 三种意图与项目初始化分支得到预期顺序。 | 已完成 |
| 3 | 4 至 10 | `src/workflow_loop/cli.py`、`src/workflow_loop/verification.py`、`src/workflow_loop/artifact_validation.py` | 旧路径事务迁移、QA 内部恢复、追踪与结果兼容 | 实现只读迁移预览、一次事务提交、失败恢复和按真实变化源恢复 QA 内部步骤。 | 迁移幂等测试、写入失败恢复和新旧追踪格式检查通过。 | 已完成 |
| 4 | 11 至 17 | `src/workflow_loop/data/Standardized_Repository/`、`src/workflow_loop/data/Template_Repository/` | 生命周期、代码架构、实施、QA、穿刺与验收材料 | 删除独立设计期修订规范，统一当前流程、实施三段式、QA 五步和真实架构文档边界。 | 包内资源与当前 `.workflow_loop` 活动副本逐字节一致。 | 已完成 |
| 5 | 12 至 17 | `CONTEXT.md`、`DESIGN.md`、`docs/workflow_loop_design_overview.drawio`、`README.md` | 旧总文档、孤立流程图和公开导航 | 迁走仍有效规则后删除三个无独立职责文件，公开入口改到产品、架构、ADR 和追踪表。 | 全库受管 Markdown 链接扫描通过，分发物不含被删文件。 | 已完成 |
| 6 | 18 至 27 | `src/workflow_loop/role_doc.py`、`src/workflow_loop/project.py`、`src/workflow_loop/installer.py`、公开测试 | 当前角色说明、初始化时机和旧文档依赖 | 移除旧阶段角色与旧总文档引用，加入 QA 角色并校正从零项目最终同步后才初始化的说明。 | 全量开发检查 395 项通过。 | 已完成 |
| 7 | 28 | `src/workflow_loop/acceptance_records.py` | `_execution_stage()`（选择测试任务阶段函数）、`record_is_current()`（判断验收记录是否当前有效的函数）、`_automated_items_are_current()`（核对自动化测试当前性函数） | 增加统一的测试任务阶段选择函数，两个验收入口都优先读取 `qa`（测试验证）并兼容旧 `test_execution`（测试执行阶段）；缺失时的错误同时说明两种可接受状态。 | 现有验收记录局部检查为 `9 passed`，Python 语法检查和 `git diff --check`（差异格式检查）均通过。 | 已完成 |
| 8 | 29 | `src/workflow_loop/artifact_validation.py` | `_validate_topic_acceptance_result()`（校验单个主题验收结果的函数） | 将主题验收结果的必填字段和 `record.record_id`（状态中的验收记录编号）一致性检查从旧字段“程序记录”改为当前模板字段“验收记录编号”；同步修改编号不一致错误，不增加旧字段别名。 | Python 语法检查通过；静态核对确认必填和比对都读取“验收记录编号”，其余程序验收记录一致性校验未改变。 | 已完成 |

### 3.2 实施中问题与处理

实施规范和模板在本轮实现中发生变化，导致旧的材料阅读确认失效。程序按规则退回当前 `impl`（代码实施）阶段；AI 重新读取五份当前材料，并把本轮五份实施记录迁到最低实现设计、实施动作和代码结果结构。既有代码计划的需求、文件集合和用户结果没有改变。

主题验收时又发现一处新旧状态读取漏改：验收记录复核已经读取 `qa.test_tasks`（测试验证任务记录），但建立自动验收记录前的核对仍只读取旧 `test_execution.test_tasks`（测试执行阶段任务记录）。程序按用户确认只退回本主题的 `impl`（代码实施），保留其他四个独立主题；修复只收口机器证据的阶段选择，没有改验收条件、用户结果或其他业务逻辑。

重新进入主题验收后，按当前模板生成的结果仍被门禁拒绝。调查确认模板和主题验收规范都要求“验收记录编号”，但 `_validate_topic_acceptance_result()`（校验单个主题验收结果的函数）仍读取旧字段“程序记录”。程序按用户确认再次只退回本主题的 `impl`（代码实施）；本次修复只统一字段名和对应错误信息，未修改验收记录状态、编号算法、验收条件或其他主题结果。

### 3.3 未完成内容

暂无

### 3.4 代码结果

整体流程先按四种工作意图分流；完整研发任务的外部阶段已经收敛为 `impl`（代码实施）和 `qa`（测试验证）主干。从零创建的最低限度实现设计由代码计划承担，修改产品不再先做设计期代码修订。旧活动轮次在第一次可写命令中事务迁移，迁移版本达到 `2` 后重复继续不再改写状态；从零项目只在最终代码设计同步确认后写入项目设计已初始化标记。旧总文档、孤立流程图和独立设计期修订规范已经删除，当前产品规则、实现说明和历史决定分别回到产品文档、代码架构文档和 ADR。当前项目实际读取的规范与模板活动副本也已与包内新规则逐字节同步。主题验收门禁现在与当前模板统一使用“验收记录编号”，不再要求文档补写旧字段“程序记录”。

#### 3.4.1 实际代码修改

| 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件 |
|---|---|---|---|---|
| `src/workflow_loop/state.py` | `WorkflowState.stage_path_version`、`WorkflowState.legacy_stage_facts`、`StageState.internal_step`、`state_from_dict()` | 增加阶段路径版本、旧阶段事实摘要和用户阶段内部步骤；反序列化旧状态时，新字段缺失取版本 `0` 和空值，不把旧阶段事实猜成新流程已经完成。 | 新状态使用版本 `2` 表示 `impl`（代码实施）和 `qa`（测试验证）主干；旧 `state.json` 仍能读取，并保留迁移所需的旧事实。 | AC-03、AC-04 |
| `src/workflow_loop/path_composer.py` | `LEGACY_STAGE_NAMES`、`FROM_SCRATCH_PATH`、`PRODUCT_CHANGE_BASE`、`BUGFIX_BASE`、`build_stage_path()` | 将 `code_design`、`revise_code_design`、`test_plan`、`test_code`、`test_execution` 只保留为旧状态识别名；三种完整研发路径改为 `acceptance_plan -> impl -> qa -> topic_acceptance -> regression_test -> overall_acceptance -> update_code_design`，并保留各意图真实需要的前置阶段。 | 新启动轮次的 `stage_path` 不再生成五个取消的用户环节；从零创建不再提前生成初步代码设计阶段。 | AC-01、AC-02 |
| `src/workflow_loop/stages/stages.py` | `QaStage`、`TestCodeStage._validate_current_test_code()` | 新增单一 `QaStage`（测试验证阶段策略），内部复用测试计划、测试代码、任务登记和机器结果校验；测试代码校验可显式读取 `qa` 状态，旧三个测试阶段类继续存在供历史轮次读取。 | 新路径只暴露一个 `qa` 用户阶段，内部事实仍分开保存和校验；旧轮次不会因为类被删除而无法迁移。 | AC-01、AC-03 |
| `src/workflow_loop/cli.py` | `_legacy_stage_migration_preview()`、`_commit_stage_path_change()`、`ensure_stage_path_current()`、`apply_stage_completion_updates()` | 先只读生成旧路径迁移预览，再对状态、流水、追踪表和结果文件做一次事务提交；失败时恢复写入前字节。迁移保存版本和旧阶段摘要，旧测试发生在最终实施前时才清除对应确认；从零创建的项目设计初始化标记移动到 `update_code_design`（最终代码设计同步）确认后。 | 首次可写命令迁移旧轮次并明确列出保留、失效和继续位置；重复继续在版本和路径一致时不再改状态；从零项目在最终同步前保持未初始化。 | AC-03、AC-04 |
| `src/workflow_loop/verification.py` | `recovery_stage_action()`、`_qa_recovery_step()`、`_reset_qa_stage_for_invalidation()`、`apply_invalidation()` | 将恢复说明和失效应用接入 `qa` 内部的 `scope`（范围）、`test_code`（测试代码）、`execution`（执行）、`result`（结果）步骤；按新主干重置阶段，不再把三个旧测试阶段当作新路径依赖。 | 旧事实变化时，当前阶段回到最早真实失效的 `impl` 或 `qa` 内部步骤；仍有效的测试范围、代码或结果可以保留。 | AC-03 |
| `src/workflow_loop/artifact_validation.py` | `validate_acceptance_plan_documents()`、`validate_downstream_traceability()`、`validate_test_execution_results()`、`_validate_topic_acceptance_result()`（校验单个主题验收结果的函数） | 追踪表校验改为读取受支持的九列或十列表头并按列名核对状态；验收条件只要求六个可判断字段，不再强制重复的“条件与触发”和“预期结果”摘要；测试结果优先读取 `qa.test_tasks`（测试验证任务记录），旧轮次仍回退读取 `test_execution.test_tasks`；主题验收结果的必填和编号比对统一读取“验收记录编号”，不接受旧字段“程序记录”代替。 | 新阶段名称不会破坏验收计划、追踪表和测试结果文档校验；新计划没有重复字段，历史九列表、旧摘要和旧测试执行状态仍可读取；符合当前模板且编号与状态一致的验收结果可以通过，缺失或错误编号仍被拒绝。 | AC-01、AC-03 |
| `src/workflow_loop/acceptance_records.py` | `_execution_stage()`（选择测试任务阶段函数）、`record_is_current()`（判断验收记录是否当前有效的函数）、`_automated_items_are_current()`（核对自动化测试当前性函数） | 把当前测试任务的阶段选择收口为一个函数：优先使用 `qa`（测试验证），旧轮次没有 `qa` 时才使用 `test_execution`（测试执行阶段）。新建自动验收记录和复核已有验收记录都读取同一来源，并继续使用 `execution_task_has_current_success()`（判断测试任务当前成功的函数）核对完整机器事实。 | 新路径的精确机器记录编号可建立自动验收记录；旧轮次保留兼容；两种状态都缺失时返回可定位的错误而不伪造通过。 | AC-01、AC-03 |
| `src/workflow_loop/data/Standardized_Repository/acceptance/acceptance_plan.md` | `## 五、生成前确认` | 删除末尾重复且过期的研发顺序，只保留待生成文档链接规则。 | 验收计划规范不再维护第二份流程顺序，整体流程只由生命周期规范负责。 | AC-01、AC-03 |
| `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` | `## 零、整体流程顺序`、`## 一、后续环节发现问题时返回上游` | 规范先列四种工作意图的完整分流，再写前三种研发任务共用的代码实施、测试验证、主题验收、最终全量回归、整体验收、最终代码设计同步和正式收工顺序；QA 按真实变化源恢复内部步骤。 | 后续 `workflow discuss` 加载的全局材料描述整轮工作，不把研发主干冒充全部流程，也不再引导用户重复经过已取消阶段。 | AC-01、AC-02、AC-03、AC-04 |
| `src/workflow_loop/data/Standardized_Repository/code_design/code_design.md` | `# 代码架构文档编写规范`、`## 一、适用场景` | 删除从零创建时提前设计尚未实现代码的规则；当前规范只负责项目设计初始化时根据已有代码建立现状架构，以及最终同步时根据真实交付更新架构。 | 代码架构文档只描述真实存在并有代码、测试或运行证据的实现，不再形成实施前的计划代码真相源。 | AC-01、AC-02 |
| `src/workflow_loop/data/Template_Repository/code_design/code_design.md` | `### 2. 代码位置必须能定位`、`## 8. 产品设计与代码实现的差异`、`## 9. 最终同步结论` | 删除“初步设计可写计划路径、计划符号和计划状态”的模板字段；代码位置、差异和最终同步都只接受真实实现。 | 新生成的架构文档不会在实施前编造代码名称，最终同步仍保留真实差异和核对依据。 | AC-01、AC-02 |
| `src/workflow_loop/data/Standardized_Repository/code_design/revise_code_design.md` | 删除整个文件 | 删除没有当前阶段入口、且与现行代码架构编写规范重复的文件；旧状态迁移改读现行规范，不保留第二套可执行规则。 | 安装包不再包含孤立的设计期代码修订规范，修改产品的实现设计统一写入代码计划。 | AC-01、AC-02、AC-03 |
| `CONTEXT.md` | 删除整个文件 | 将仍然有效的产品规则迁到对应产品功能文档、实现规则迁到代码架构文档、历史取舍保留在 ADR（架构决策记录）后删除整份文件。 | 当前产品规则、实现说明和历史决定分别只有一个负责位置；现行代码、测试和公开入口不再读取旧合集。 | AC-01、AC-02、AC-04 |
| `DESIGN.md` | 删除整个文件 | 删除仍把旧阶段和旧命令写成当前实现的重复设计文档；当前实现说明统一由 `spec/代码架构设计.md` 承担。 | 仓库不再同时维护两份互相冲突的实现设计，维护者只从当前架构文档读取真实实现。 | AC-01、AC-02 |
| `docs/workflow_loop_design_overview.drawio` | 删除整个文件 | 删除没有当前文档入口、且仍绘制已取消阶段的旧图；没有用新文件名保留副本。 | 维护者不会从孤立图源读到错误流程；当前整体顺序由生命周期规范和产品文档说明。 | AC-01、AC-02 |
| `README.md` | `## 仓库结构`、`## 详细文档` | 删除 `CONTEXT.md` 和 `DESIGN.md` 的当前入口，改为链接产品总说明、功能文档、代码架构文档、ADR 和需求交付追踪表。 | 公开导航只指向仍存在且职责明确的当前文档，删除旧总文档后不产生公开坏链接。 | AC-01、AC-02 |
| `src/workflow_loop/role_doc.py` | `ROLE_DOC_MAP`（阶段角色说明表）、`COMMON_CONTEXT`（共同上下文） | 删除 `code_design`、`revise_code_design`、`test_plan`、`test_code` 和 `test_execution` 的当前角色说明，增加 `qa`（测试验证）角色，并把共同顺序改成代码实施三段式和单一 QA 主干。 | `workflow discuss` 只为当前阶段给出职责；穿刺资产保留、实施最低设计和测试连续执行的说明与真实路径一致。 | AC-01、AC-02、AC-03 |
| `src/workflow_loop/project.py` | `ProjectState.project_design_initialized`（项目设计已初始化字段）、`is_project_design_initialized()`（判断初始化函数）、`set_project_design_initialized()`（写入初始化标记函数）的注释 | 移除对已删 `CONTEXT.md` 的引用，并把从零创建的初始化时机说明改为最终 `update_code_design`（代码设计同步）确认后；运行逻辑没有借注释修改。 | 源码说明与真实初始化时机一致，不会引导维护者重新在初步代码设计后提前置真。 | AC-01、AC-04 |
| `src/workflow_loop/installer.py` | `AGENTS_MD_CONTENT`（代理契约固定正文）前的覆盖边界注释 | 删除旧总文档引用，直接说明用户确认安装范围后整份覆盖旧契约，失败恢复由安装事务负责。 | 安装行为不变，源码注释自身完整说明真实覆盖和恢复边界。 | AC-01、AC-04 |
| `tests/test_public_project.py` | `test_readme_has_the_confirmed_chinese_structure_and_entries()`（公开说明结构测试） | 把详细文档期望入口从两个已删旧总文档改为当前产品、架构和 ADR 入口，并明确断言旧文件不存在。 | README 再次引用或仓库重新出现 `CONTEXT.md`、`DESIGN.md` 时测试会失败。 | AC-01、AC-02 |
| `tests/test_release_workflow.py` | `test_current_release_identity_and_non_tag_publish_rules_are_consistent()`（当前发布身份测试） | 当前版本规则只从产品总说明和安装功能文档读取；旧版本 ADR 只验证它已被后续发布与更新功能取代，不再当作当前产品规则。 | 发布测试不依赖被删旧合集，也不会把历史决定重新升级成当前规范。 | AC-01、AC-02、AC-04 |

#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `.venv/bin/python -m compileall -q src` | 新增状态类型、阶段策略、迁移和失效代码的 Python 语法 | 命令退出码为 `0`，没有语法错误输出。 | 否 |
| 读取 `build_stage_path()` 的三种完整研发意图 | 从零创建、修改产品、修复缺陷的阶段名称和顺序 | 三条路径都使用 `impl -> qa` 主干，未再生成五个取消阶段；各意图原有的产品设计、复现、穿刺和项目设计初始化前置仍按条件保留。 | 否 |
| `cmp -s`（逐字节比较命令）核对包内生命周期规范与活动副本 | `src/workflow_loop/data/Standardized_Repository/global/workflow_lifecycle.md` 和 `.workflow_loop/Standardized_Repository/global/workflow_lifecycle.md` | 比较退出码为 `0`，当前项目实际读取的活动副本与包内源资源完全一致。 | 否 |
| `git diff --check` | 本轮全部文本差异的空白和补丁格式 | 命令退出码为 `0`，没有尾随空格或补丁格式错误。 | 否 |
| `.venv/bin/python -m pytest -q` | 当前仓库全部单元、命令、迁移、回退、门禁、穿刺、QA、文档和发布测试 | 395 项测试全部通过；没有失败、错误或跳过。 | 否 |
| `validate_managed_markdown_links()`（校验受管 Markdown 链接函数） | 当前正式文档的本地文件链接和显式定位 | 2736 个链接全部可导航；实施记录中的两处旧章节定位已直接改为现行定位，没有增加兼容锚点。 | 否 |
| `.venv/bin/python -m build --outdir /tmp/workflow_loop_dist_audit_20260811_6iZjV2`，随后核对源码包和 wheel（可安装包）内容 | Python 源码分发包和 wheel 内的规范、模板及旧文件排除结果 | 成功构建 `workflow_loop-0.3.1.tar.gz` 和 `workflow_loop-0.3.1-py3-none-any.whl`；两种分发物的规范和模板与源码逐字节一致，且都不含 `revise_code_design.md`、旧流程图、`CONTEXT.md` 或 `DESIGN.md`。 | 否 |
| `.venv/bin/python -m pytest -q tests/test_acceptance_records.py` | 主题验收记录的机器事实严格性、人工回答保留和旧 `test_execution`（测试执行阶段）兼容 | 开发反馈为 `9 passed in 0.04s`；现有旧状态兼容和严格记录校验没有回归。新 `qa`（测试验证）状态的专项回归测试在后续 QA 测试代码步骤增加并正式执行。 | 否 |
| `.venv/bin/python -m compileall -q src/workflow_loop/acceptance_records.py`；`git diff --check -- src/workflow_loop/acceptance_records.py impl/完整研发流程按最少用户环节推进并兼容旧轮次_实施记录.md` | 本次最小修复的 Python 语法和文本差异格式 | 两条命令退出码均为 `0`，没有语法错误、尾随空格或补丁格式错误。 | 否 |
| `.venv/bin/python -m compileall -q src/workflow_loop/artifact_validation.py` | 主题验收结果字段契约修复的 Python 语法 | 命令退出码为 `0`，没有语法错误输出。 | 否 |
| 读取 `_validate_topic_acceptance_result()` 并用 `rg`（文本搜索命令）核对字段 | 主题验收结果的必填字段、状态编号比较和错误信息 | 必填字段和 `record.record_id` 比较均使用“验收记录编号”；旧词“程序记录”只保留在描述状态记录一致性的通用错误中，不再作为文档字段读取。 | 否 |
| `git diff --check -- src/workflow_loop/artifact_validation.py impl/完整研发流程按最少用户环节推进并兼容旧轮次_实施记录.md acceptance/索引.md` | 本次生产代码、实施记录和现有结果索引修正的文本差异格式 | 命令退出码为 `0`，没有尾随空格或补丁格式错误。 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/完整研发流程按最少用户环节推进并兼容旧轮次_验收计划.md) | 本主题用户结果和 AC-01 至 AC-04 |
| 上游 | [代码架构设计](../spec/代码架构设计.md#513-当前研发路径与活动轮次迁移) | 当前路径、迁移、状态和初始化时机 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 当前工作流全部交付关系 |
| 下游 | `../qa/完整研发流程按最少用户环节推进并兼容旧轮次_测试计划.md`（待生成） | 测试验证内部形成测试计划 |
| 下游 | `../qa/完整研发流程按最少用户环节推进并兼容旧轮次_测试结果.md`（待生成） | 正式执行后生成机器结果 |
| 下游 | `../acceptance/完整研发流程按最少用户环节推进并兼容旧轮次_验收结果.md`（待生成） | 主题验收结果 |
