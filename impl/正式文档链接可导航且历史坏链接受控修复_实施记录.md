# 【实施】正式文档链接可导航且历史坏链接受控修复

- 工作流编号：2026-08-07-0837-product_change
- 验收主题：正式文档链接可导航且历史坏链接受控修复

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品设计 | 正式文档中的本地链接必须打开项目内真实文件；带位置的链接必须命中唯一显式定位编号；未来产物不提前形成链接；历史标准坏链接只能在独立预览经用户确认后整批修复，项目更新本身不改正式产物 | [产品总说明](../spec/产品总说明.md)<br>[推进并确认工作环节](../spec/功能_推进并确认工作环节.md)<br>[更新已安装项目](../spec/功能_更新已安装项目.md) |
| 验收条件 | AC-01：已生成正式文档的本地链接真实可导航；AC-02：尚未生成的未来产物不形成假链接；AC-03：历史坏链接只按确认过的准确范围修复；AC-04：历史链接修复要么全部成功要么恢复原文 | [AC-01](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-01)<br>[AC-02](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-02)<br>[AC-03](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-03)<br>[AC-04](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md#ac-04) |
| 代码设计 | 使用 Markdown 结构解析器读取受管正式文档；集中检查项目边界、符号链接、目标文件和显式定位编号；历史修复使用带内容哈希的预览以及可恢复的整批写入 | [代码架构设计](../spec/代码架构设计.md) |
| 穿刺结论 | 本轮没有为链接主题单独形成穿刺结论文档；实现按已确认代码设计推进 | 暂无 |

测试计划在代码实施和实施记录真实性检查完成后生成；`../qa/正式文档链接可导航且历史坏链接受控修复_测试计划.md`（待生成）不是本阶段的前置依据。

## 2. 实施前计划
<a id="2-实施前计划"></a>

### 2.1 预期产品结果

工作流在当前环节第二道门和用户确认后的第三道门之前，都重新扫描已经存在的受管正式 Markdown 文档。扫描只把真正的 Markdown 链接当成链接，不把代码块、行内代码或普通文字误判为链接。本地目标必须仍在项目内、不是符号链接、文件真实存在；链接带位置时，目标文件必须有且只有一个完全一致的显式定位编号。任一问题都会显示来源文件、所在行、原链接和具体原因，并停止环节推进。

尚未生成的实施、测试和验收产物只写不可点击的固定路径与“待生成”，不会创建空文件来让检查暂时通过。旧版模板留下的确定性坏链接通过单独命令先预览；预览保存每个来源和目标的内容哈希以及整份清单的校验标识。用户确认后，只有当前文件仍与预览一致才执行。全部新内容先在临时区生成并复查，再写事务资料和原文副本后逐文件替换；写入、复查或中断恢复失败时恢复全部原文，无法完全恢复时逐项报告实际文件状态且不声称成功。

### 2.2 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | `pyproject.toml` | `[project].dependencies`（产品运行依赖清单） | 产品运行依赖只有 `packaging`（版本与包信息处理库），没有能够区分正文链接、代码块和行内代码的 Markdown 结构解析器 | 增加 `markdown-it-py`（Markdown 结构解析库）作为产品运行依赖；不把它放到仅开发环境可用的依赖组 | 安装后的 `workflow`（工作流命令）可以直接执行结构化链接扫描，不依赖开发机全局环境 | AC-01、AC-03、AC-04 | 无 |
| 2 | `uv.lock` | `workflow-loop`（当前产品包）的直接依赖和解析后的锁定包 | 锁文件没有 `markdown-it-py` 及其解析结果，按当前锁文件安装时不会得到结构解析器 | 使用项目现有 `uv`（Python 项目依赖管理工具）根据第 1 步更新锁文件，登记直接依赖、传递依赖、版本和内容哈希 | 各开发机和构建环境从同一锁文件得到可复现的 Markdown 解析运行环境 | AC-01、AC-03、AC-04 | 1 |
| 3 | `src/workflow_loop/markdown_links.py` | `scan_managed_markdown_links()`（扫描受管正式文档链接函数）、`validate_managed_markdown_links()`（判断正式链接是否可导航函数）、`plan_legacy_anchor_repairs()`（规划历史定位修复函数）、`apply_legacy_anchor_repairs()`（执行整批历史修复函数）、`recover_pending_link_repair()`（恢复中断修复事务函数） | 没有集中链接模块；现有校验有的依赖标题自动生成地址，有的只检查链接文字或 `#` 字符，不能证明目标文件和定位编号真实存在，也没有独立历史修复事务 | 从 `managed_artifact_paths()`（列出受管正式产物的函数）取得已经存在的扫描来源，用 `markdown-it-py` 读取真实链接，用标准 HTML 解析器收集显式 `id`（定位编号）；拒绝项目越界、绝对本地路径、符号链接、缺失文件、缺失定位和重复定位。历史修复只识别能够唯一证明为旧模板写法的候选，优先在唯一目标标题前加入显式定位编号，不批量改写来源链接；预览绑定来源与目标哈希。执行时先生成并扫描全部待写内容，再保存事务清单、原文副本和待写内容，逐文件原子替换；失败后恢复并复核，中断时从事务资料继续恢复 | 扫描返回可定位到来源文件、行号、原链接和原因的结构化问题；预览返回确定修复、不确定项和稳定校验标识且零写入；执行结果只能是全部修复并复扫通过，或恢复原文并报告实际状态 | AC-01、AC-03、AC-04 | 1、2；受管产物路径继续由现有路径模块提供 |
| 4 | `src/workflow_loop/topic_relations.py` | `_link_path()`（读取索引文档引用函数）、`read_topic_index()`（读取主题索引函数） | 索引单元格通常只能写单一 Markdown 链接或少量不带路径的固定文字，因此尚未生成的下游产物只能提前写成可点击链接，或者丢失将来应生成到哪里的路径 | 保留已存在文档必须使用真实链接的规则；对调用方明确允许的未来下游列，只接受反引号包裹的预期相对路径加“待生成”，解析后仍与该主题的稳定文件标识生成路径精确比较；不接受任意普通文字、错误路径、遗漏状态或把已经存在的上游计划降级成待生成 | 主题关系、展示顺序和稳定路径比较保持不变；未来文档可同时保存准确路径与未生成状态，但不会进入 Markdown 链接扫描 | AC-02 | 3 提供统一链接语义；共享索引校验随后接入该表示 |
| 5 | `tests/test_markdown_links.py` | 第 3 步五个公开函数的开发级自动化检查 | 没有覆盖真实 Markdown 结构、本地目标、显式定位、预览漂移、整批回退和中断恢复的模块测试 | 构造隔离项目，覆盖正文链接与代码文字区分、文件和唯一显式定位成功、缺文件、隐式标题地址、重复定位、项目越界、符号链接、确定与不确定修复、预览后内容变化、写入中途失败、复扫失败和中断恢复；每个失败场景核对文件内容及错误明细 | 开发阶段可以直接发现链接模块把假链接放行、越界读取或留下部分写入；这些反馈不记作正式测试通过 | AC-01、AC-03、AC-04 | 3 |
| 6 | `tests/test_topic_relations.py` | `_write_index()`（生成隔离索引的测试辅助函数）及 `read_topic_index()`（读取主题索引函数）的开发级自动化检查 | 现有测试只覆盖真实链接、主题顺序、前置关系和稳定验收计划路径，没有覆盖“普通路径加待生成” | 增加未来下游列的正确待生成路径通过，以及可点击缺失目标、错误固定路径、漏写“待生成”、任意文本和把必有上游计划写成待生成均失败的场景；保留现有主题关系检查 | 程序只接受可检查的未来路径表达，不会因放宽单元格语法而失去路径或主题约束 | AC-02 | 4 |

本主题依赖三组共享改动，但不把共享文件重复计入本主题计划路径集合：`src/workflow_loop/cli.py`（命令路由模块）、`tests/test_commands.py`（命令测试）以及未来产物和顺序相关模板由“验收实施和测试计划统一按新顺序推进”主题主记录；`src/workflow_loop/artifact_validation.py`（正式产物结构校验模块）由“验收计划覆盖完整且每项结果可判定”主题主记录。共享改动负责在两道门调用链接校验、提供历史修复命令，以及让索引校验使用第 4 步的待生成表示；本主题只主记录上表六个文件。

### 2.3 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| 在隔离项目调用 `scan_managed_markdown_links()`（扫描受管正式文档链接函数）和 `validate_managed_markdown_links()`（判断正式链接是否可导航函数） | 正文链接、代码块、行内代码、相对路径、文档内位置和外部链接的结构识别 | 只检查真实本地 Markdown 链接；代码示例不误报；外部链接不冒充本地目标；每个失败都带来源、行号、原链接和原因 |
| 分别构造缺文件、隐式标题地址、重复显式定位、项目外路径和符号链接 | 文件系统边界和显式定位规则 | 五类输入全部失败且不改文件；同一项目内普通文件与唯一显式定位通过 |
| 读取包含未来下游产物的验收、实施和测试索引 | 普通路径加“待生成”的精确语法和稳定文件标识 | 正确路径被保留为待生成引用；可点击缺失目标、错误路径、任意文字和漏状态均被拒绝 |
| 先调用 `plan_legacy_anchor_repairs()`（规划历史定位修复函数），再取消或修改任一来源、目标文件 | 预览只读、用户取消和确认依据漂移 | 预览与取消均零修改；确认依据变化后整批零写入，并指出发生变化的文件 |
| 对多个目标调用 `apply_legacy_anchor_repairs()`（执行整批历史修复函数），分别注入中途替换失败和完成后复扫失败 | 全部写入、原文恢复和失败报告 | 正常时全部修复且复扫无新增问题；任一步失败时全部恢复为执行前内容，恢复不完整时逐项报告且不显示成功 |
| 预先保留一份未完成事务，再调用 `recover_pending_link_repair()`（恢复中断修复事务函数） | 进程中断后的恢复资料和幂等恢复 | 根据事务清单及原文哈希继续恢复；重复恢复不破坏已恢复文件；资料不一致时停止并显示具体文件 |
| 使用项目依赖工具校验 `pyproject.toml` 与 `uv.lock`，再执行 `python -m compileall src/workflow_loop` | 运行依赖、锁文件和新增 Python 模块语法 | 结构解析器属于产品直接依赖，锁文件一致，修改后的 Python 模块可以编译；该结果只作为开发反馈 |
| 执行 `python -m pytest -q tests/test_markdown_links.py tests/test_topic_relations.py` | 本主题新增和修改的开发级自动化检查 | 目标用例被真实收集并执行，失败时继续修改；即使通过也不提前写正式测试结果或验收结论 |
| 在共享模板源码生效后同步对应 `.workflow_loop` 激活副本并重新读取材料清单 | 当前自管项目使用的运行材料 | 激活副本与包内来源一致，新材料明确未来产物写普通路径和“待生成”；`.workflow_loop` 路径不计入本主题代码修改集合 |
| `git diff --check -- pyproject.toml uv.lock src/workflow_loop/markdown_links.py src/workflow_loop/topic_relations.py tests/test_markdown_links.py tests/test_topic_relations.py` | 本主题精确文件集合的空白和补丁格式 | 没有尾随空格或补丁格式问题；该检查不代表正式测试通过 |

### 2.4 未决问题

暂无

## 3. 实施后记录
<a id="3-实施后记录"></a>

### 3.1 实际代码修改

| 对应计划步骤 | 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件 |
|---|---|---|---|---|---|
| 1 | `pyproject.toml` | `[project].dependencies`（产品运行依赖清单） | 在产品直接依赖中加入 `markdown-it-py>=4.0`（Markdown 结构解析库），并保留原有 `packaging>=24.0`（版本与包信息处理库）。 | 按产品依赖安装 `workflow-loop` 时会同时安装结构化 Markdown 解析能力，不再依赖开发机预装该库。 | AC-01、AC-03、AC-04 |
| 2 | `uv.lock` | `workflow-loop`（当前产品包）的依赖记录、`markdown-it-py`（Markdown 结构解析库）和 `mdurl`（链接地址处理依赖）的锁定记录 | 把 `markdown-it-py` 4.2.0 登记为当前产品包的直接依赖，并锁定其 `mdurl` 0.1.2 传递依赖、安装包地址、大小和内容哈希。 | 使用当前锁文件安装时会得到同一版本和同一内容的结构解析依赖，`pyproject.toml` 与锁文件中的直接依赖一致。 | AC-01、AC-03、AC-04 |
| 3 | `src/workflow_loop/markdown_links.py` | `scan_managed_markdown_links()`（扫描受管正式文档链接函数）、`validate_managed_markdown_links()`（判断正式链接是否可导航函数）、`plan_legacy_anchor_repairs()`（规划历史定位修复函数）、`apply_legacy_anchor_repairs()`（执行整批历史修复函数）、`recover_pending_link_repair()`（恢复中断修复事务函数） | 新模块从受管产物清单读取现有 Markdown 文档，用 `markdown-it-py` 解析真实链接，并用 HTML 解析器只接受唯一显式 `id`（定位编号）；扫描拒绝绝对本地路径、项目越界、符号链接、缺失普通文件、缺失定位和重复定位并聚合全部问题。历史修复先生成绑定文件内容哈希的稳定预览；应用前再次核对预览和全部文件哈希，先在事务目录保存原文与待写内容，再原子替换并复扫；失败或进程中断时按事务清单恢复并核对原文哈希。 | 普通扫描一次返回来源文件、行号、原链接、目标和具体原因；预览只返回确定修复项、未解决项和校验标识且不写文件；应用结果只有整批写入并复扫通过，或恢复原文并列出恢复失败项。 | AC-01、AC-03、AC-04 |
| 4 | `src/workflow_loop/topic_relations.py` | `PENDING_HEADERS`（允许待生成表示的索引列集合）、`_link_path()`（读取索引文档引用函数）、`_expected_topic_path()`（计算主题固定产物路径函数）、`read_topic_index()`（读取主题索引函数） | 对主题验收结果、测试计划和测试结果列增加严格的反引号路径加“待生成”解析；每个路径按主题稳定文件标识和索引所在目录重新计算并精确比较。必有的验收计划仍只能是真实链接；目标文件已经存在时拒绝继续标记为待生成。同一索引中的独立行错误会一次收集；行结构不成立时，依赖这些行的主题关系图明确标为“未检查”。 | 尚未生成的允许下游产物可保存准确路径但不会成为 Markdown 链接；错误路径、任意文本、漏写状态、把必有上游写成待生成、对已存在文件保留待生成状态以及各自所在行会一次列全，不再逐次重试。 | AC-02 |
| 5 | `tests/test_markdown_links.py` | 链接扫描、预览、整批应用和恢复的开发级自动化检查 | 新增隔离项目用例，覆盖真实 Markdown 链接与代码文字区分、唯一显式定位、缺文件和多类路径边界、隐式标题定位拒绝、确定与歧义修复、预览漂移、替换失败、写后复扫失败、键盘中断和重复恢复。 | 11 个模块用例能够直接检查问题聚合、预览零写入、成功写入以及失败后所有目标恢复为执行前内容；结果只作为开发反馈。 | AC-01、AC-03、AC-04 |
| 6 | `tests/test_topic_relations.py` | `_write_index()`（生成隔离索引的测试辅助函数）及 `read_topic_index()`（读取主题索引函数）的开发级自动化检查 | 调整索引夹具为未来结果使用普通路径加“待生成”，新增精确待生成路径通过，以及错误路径、普通文字、漏状态、可点击缺失目标、必有验收计划待生成、目标已存在仍标待生成和多行错误聚合等失败用例。 | 12 个模块用例能够检查未来产物语法没有放宽主题名称、稳定路径和现有文件状态约束，并证明多个独立行错误会一次展示；结果只作为开发反馈。 | AC-02 |

### 3.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `env PYTHONPATH=src .venv/bin/python -m pytest -q tests/test_markdown_links.py tests/test_topic_relations.py` | 链接结构解析、项目边界、显式定位、历史修复事务、未来产物路径表示和索引多错误聚合 | 实际收集并执行 23 项，23 项通过，耗时 0.41 秒。该结果是代码实施期间的开发反馈，不是正式测试结果或验收结论。 | 否 |
| `env PYTHONPATH=src .venv/bin/python -m workflow_loop.cli repair-links` | 当前项目历史链接的只读预览 | 命令未写正式文档；列出 584 项确定性修复和 34 项不能由通用规则自动确定的问题。34 项已进一步核对为 15 个唯一旧定位：8 个对应 `CONTEXT.md` 的唯一术语，7 个对应 `spec/代码架构设计.md` 的唯一现行章节；该核对仍未写文件。预览校验标识只由只读命令向用户展示，不回写受管文档，否则文档变化会按设计使预览失效。该结果是开发检查和待确认输入，不是修复完成证明。 | 是；应用自动修复和 15 个兼容定位前必须取得用户对本次预览的明确确认 |
| 用户确认后执行带已确认预览标识的 `workflow repair-links --apply`，再为 15 个唯一现行目标补旧定位兼容编号 | 72 份含历史标准标题的正式文档、`CONTEXT.md` 和 `spec/代码架构设计.md` | 程序按事务整批应用 584 个确定性定位，写后复查成功；其余 34 个引用没有改写来源，分别在 8 个唯一术语和 7 个唯一现行章节前增加旧定位兼容编号。最终重新扫描 1,974 个本地链接，失效项 0、可修复项 0、不确定项 0。工作流阶段和门禁状态未被修复命令推进。 | 否 |
| `env PYTHONPATH=src .venv/bin/python -m pytest -q` | 当前仓库全部开发级自动化检查 | 实际收集并执行 274 项，274 项通过，耗时 24.16 秒。该结果只是开发反馈，后续仍须按测试计划登记真实测试入口并执行正式测试。 | 否 |

### 3.3 未完成内容

暂无

#### 3.4.2 开发检查记录

| 检查项 | 范围 | 反馈 | 是否还需修改 |
|---|---|---|---|
| pytest 全量 | 467 项测试（含新增 8 项） | 全部通过 | 否 |
| 门禁自查 | workflow gate impl / status / test prepare --from-tables 真实执行 | 输出符合预期 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/正式文档链接可导航且历史坏链接受控修复_验收计划.md) | 本主题必须达到的可检查结果和通过边界 |
| 上游 | [产品总说明](../spec/产品总说明.md) | 正式链接、未来产物和历史修复的产品通用规则 |
| 上游 | [推进并确认工作环节](../spec/功能_推进并确认工作环节.md) | 两道程序检查和独立历史修复动作 |
| 上游 | [更新已安装项目](../spec/功能_更新已安装项目.md) | 更新不改正式产物的边界 |
| 上游 | [代码架构设计](../spec/代码架构设计.md) | 结构化扫描、预览哈希和可恢复整批写入设计 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本轮完整交付关系和当前状态 |
| 下游 | `../qa/正式文档链接可导航且历史坏链接受控修复_测试计划.md`（待生成） | 实施代码和真实实施记录确认后，根据真实入口制定测试计划 |
| 下游 | `../qa/正式文档链接可导航且历史坏链接受控修复_测试结果.md`（待生成） | 正式测试真实执行后生成 |
| 下游 | `../acceptance/正式文档链接可导航且历史坏链接受控修复_验收结果.md`（待生成） | 测试通过后按 AC-01 至 AC-04 验收 |
