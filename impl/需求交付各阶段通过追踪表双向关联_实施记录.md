# 【实施】需求交付各阶段通过追踪表双向关联

- 工作流编号：2026-07-20-0637-from_scratch
- 验收主题：需求交付各阶段通过追踪表双向关联

## 1. 实施依据

| 依据类型 | 具体内容 | 文档位置 |
|---|---|---|
| 产品规则 | 每条验收条件在全局追踪表中单独占一行；局部文档写自己的上下游；不同工作流的记录按工作流编号分开保存。 | [用户确认的追踪规则](../CONTEXT.md#traceability-matrix-需求交付追踪表) |
| 产品设计 | AC-01 至 AC-03，分别要求逐条追踪、局部链接可导航和历史记录不互相覆盖。 | [本主题验收计划](../acceptance/需求交付各阶段通过追踪表双向关联_验收计划.md) |
| 测试计划 | TC-01 检查逐条更新，TC-02 检查链接导航，TC-03 检查历史保留和幂等更新。 | [本主题测试计划](../qa/需求交付各阶段通过追踪表双向关联_测试计划.md) |
| 代码设计 | `traceability.py` 只更新当前阶段负责的列；索引关系由验收索引继承到测试和实施索引。 | [代码架构设计](../spec/代码架构设计.md#53-三道门禁与阶段推进) |
| 追踪记录 | 当前工作流已有九列表格，并为三个主题的每条验收条件保留待制定、待执行或待更新状态。 | [需求交付追踪表](../需求交付追踪表.md#2026-07-20-0637-from_scratch) |
| 穿刺结论 | 暂无。本工作流已由用户确认跳过穿刺，本主题不依赖技术穿刺结果。 | 暂无 |

## 2. 实施前计划

### 2.1 预期产品结果

用户从任意验收计划、测试计划、实施文档或结果文档进入时，都能打开本阶段的上游依据、下游文档和全局 `需求交付追踪表.md`。维护者查看追踪表时，可以按验收条件看到测试项、实施计划、实际代码、测试结果、验收结果和最终代码设计；新的工作流追加到独立章节，不覆盖旧工作流。

### 2.2 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改的具体逻辑 | 数据、状态或输出变化 | 对应验收条件和测试项 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | `src/workflow_loop/traceability.py` | `validate_structure()`（校验追踪表结构）、`_update_stage_rows()`（更新当前主题行）、`update_for_stage()`（按阶段写入追踪列）、`_test_plan_links()`（读取测试项链接） | 追踪表定义九列；阶段确认时只更新当前工作流主题行的对应列；测试计划中的 TC 链接会写入测试项列；实施阶段写入实施前计划和实施后记录入口。 | 复核并补齐每条验收条件一行、当前工作流隔离、各阶段只写自己的列、重复更新不新增重复链接；确保实施前计划和实施后记录使用同一主题文件的明确锚点。 | `需求交付追踪表.md` 当前工作流章节从初始状态逐列变成链接或结果；旧工作流章节不变；每条验收条件保留独立追踪关系。 | AC-01、AC-03；TC-01、TC-03 | 已生成当前工作流的 `需求交付追踪表.md` 和验收/测试计划。 |
| 2 | `src/workflow_loop/topic_relations.py` | `read_topic_index()`（读取主题索引）、`relation_signature()`（生成可比较的主题关系） | 从验收、测试和实施索引读取展示顺序、主题和前置主题，用稳定关系表示比较上下游索引。 | 继续以 `acceptance/索引.md` 为主题关系来源；测试和实施索引只继承展示顺序和前置主题，不允许某个下游阶段自行改顺序或增加依赖。 | 验收、测试、实施三个索引的关系签名一致；关系不一致时门禁指出具体索引。 | AC-03；TC-03 | 已确认 `acceptance/索引.md` 的三主题顺序和前置主题。 |
| 3 | `src/workflow_loop/artifact_validation.py` | `validate_acceptance_index()`（校验验收索引）、`validate_test_plan_documents()`（校验测试计划）、`validate_inherited_topic_index()`（校验继承关系） | 验收计划、测试计划和实施索引分别检查主题、链接、顺序和前置主题；当前工作流追踪表检查九列和状态。 | 把上下游一致性作为固定门禁：验收索引是唯一关系来源；测试索引必须链接验收计划和测试计划；实施索引必须链接验收计划、测试计划和实施文档；每个主题只能出现一次。 | 关系错误、缺链接、缺主题或空状态时门禁失败并指出文件；通过时确认下游索引与上游关系一致。 | AC-01、AC-02、AC-03；TC-01、TC-02、TC-03 | 先保证验收和测试索引结构完整。 |
| 4 | `src/workflow_loop/stages/stages.py` | `AcceptancePlanStage.code_validate()`、`TestPlanStage.code_validate()`、`ImplStage._validate_topic_documents()`（各阶段文档和索引门禁） | 验收计划确认主题，测试计划继承验收主题，实施阶段继承测试索引并要求实施文档；各阶段门禁会读取追踪表。 | 保持阶段只能沿上下游关系推进，禁止通过修改下游索引绕过上游主题关系；实施讨论完成时检查当前三个主题均有实施文档。 | 门禁输出缺少主题、索引不一致或追踪表不完整的具体原因；状态文件不在错误阶段推进。 | AC-01、AC-02；TC-01、TC-02 | 先完成各阶段文档，再执行阶段门禁。 |
| 5 | `src/workflow_loop/cli.py` | `apply_stage_completion_updates()`（阶段确认后的追踪更新）、`cmd_gate()`（门禁和阶段推进） | `test_plan`、`impl`、主题执行、回归和整体验收确认时调用追踪表更新；更新失败不会推进阶段。 | 复核更新时机和幂等性：先写当前阶段负责的追踪列，写入成功后才推进状态；任何写入失败都保留当前阶段并提示修复固定记录。 | `需求交付追踪表.md` 追加当前阶段证据链接；`.workflow_loop/state.json` 只有在固定记录更新成功后才写用户确认和下一阶段。 | AC-02、AC-03；TC-01、TC-03 | 先完成追踪表模块和阶段校验，再接入命令确认流程。 |

### 2.3 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| `python3 -m pytest tests/test_traceability.py -q` | 追踪表按行更新、当前工作流隔离、阶段列写入和重复更新。 | 每条验收条件都有对应行；旧工作流不变；重复执行不会重复追加链接。 |
| `python3 -m pytest tests/test_stages.py tests/test_commands.py -q` | 验收、测试、实施索引关系和门禁推进。 | 主题关系不一致、链接缺失或固定记录失败时门禁拒绝推进。 |
| `python3 -m pytest tests/test_verification.py -q` | 验收计划哈希与追踪表后续更新的边界。 | 追踪表后续更新不让已确认的验收计划哈希失效；真正修改验收计划时才退回后续阶段。 |
| `./scripts/test_all.sh` | 全部现有测试。 | 全量测试通过，且不破坏其他阶段的追踪行为。 |
| `git diff --check` | 文档和代码差异中的空白错误。 | 无空白错误。 |

### 2.4 未决问题

暂无

## 3. 实施后记录

### 3.1 实际代码修改

| 对应计划步骤 | 文件 | 类、函数或配置项 | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 对应验收条件和测试项 |
|---|---|---|---|---|---|
| 1 | `src/workflow_loop/traceability.py` | `update_for_stage()`（按阶段更新追踪表）、`_update_stage_rows()`（更新当前主题行）、`reset_after_upstream_invalidation()`（清理失效下游列） | 每条验收条件独立占一行；阶段确认时只更新本阶段负责的列；验收计划或测试计划失效时，只清理当前工作流的下游链接和状态，不修改旧工作流章节。 | `需求交付追踪表.md` 保留工作流历史；当前工作流按阶段补入测试项、实施计划、实施记录、测试结果、验收结果和代码设计入口；上游失效后对应下游列恢复为初始状态。 | AC-01、AC-03；TC-01、TC-03 |
| 2 | `src/workflow_loop/topic_relations.py` | `read_topic_index()`（读取主题索引）、`relation_signature()`（比较主题关系） | 从 `acceptance/索引.md` 读取展示顺序和前置主题，生成稳定的主题关系；测试和实施索引通过同一关系签名进行继承校验。 | `qa/索引.md` 和 `impl/索引.md` 不能自行改变主题顺序或增加依赖；关系不一致时门禁指出具体索引。 | AC-03；TC-03 |
| 3 | `src/workflow_loop/artifact_validation.py` | `_validate_topic_index_rows()`（校验索引行）、`validate_inherited_topic_index()`（校验下游继承）、`validate_downstream_traceability()`（校验下游追踪） | 校验主题集合、唯一性、展示顺序、前置主题、链接路径和依赖循环；验收索引作为唯一关系来源，测试和实施索引必须继承。 | 缺少主题、重复主题、错误链接、错误顺序或循环依赖时门禁失败；通过后各阶段索引可以互相导航。 | AC-01、AC-02、AC-03；TC-01、TC-02、TC-03 |
| 4 | `src/workflow_loop/stages/stages.py` | `TestPlanStage.code_validate()`、`ImplStage._validate_topic_documents()`、`TestExecutionStage.code_validate()`、`TopicAcceptanceStage.code_validate()`、`RegressionTestStage.code_validate()`、`OverallAcceptanceStage.code_validate()` | 各阶段门禁读取并校验继承后的索引和追踪表；实施阶段要求所有主题有实施文档；测试、主题验收、回归和整体验收继续检查下游追踪关系。 | 阶段只能沿验收索引确定的主题关系推进；追踪表或索引不完整时不推进。 | AC-01、AC-02、AC-03；TC-01、TC-02、TC-03 |
| 5 | `src/workflow_loop/cli.py`、`src/workflow_loop/verification.py` | `apply_stage_completion_updates()`（阶段确认后的追踪更新）、`check_invalidation()`（检查上游变化） | 阶段确认成功后才更新当前阶段负责的追踪列；验收计划或测试计划变化时清零后续门禁和代码/产物基线，并调用追踪表清理当前工作流的失效下游记录。 | 追踪表、阶段状态、验证哈希和 Journal 的变化保持同一阶段顺序；追踪表后续补链不会单独使验收计划哈希失效。 | AC-01、AC-03；TC-01、TC-03 |

### 3.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| `PYTHONPATH=src .venv/bin/python -m pytest tests/test_traceability.py tests/test_verification.py -q` | 追踪表按行更新、工作流隔离、幂等更新、上游失效后的下游清理和哈希边界。 | 命令退出码为 0；相关测试执行完成。 | 否 |
| `PYTHONPATH=src .venv/bin/python -m pytest tests/test_stages.py tests/test_commands.py -q` | 验收、测试、实施索引继承和阶段门禁。 | 命令退出码为 0；相关测试执行完成。 | 否 |
| `./scripts/test_all.sh` | 全部现有单元测试和集成测试。 | 命令退出码为 0；终端统计 `126 passed in 10.00s`。此记录只作为实施期间开发检查，不作为正式测试结果。 | 否 |
| `PYTHONPATH=src .venv/bin/python -m compileall -q src tests` | Python 源码和测试源码语法。 | 无输出，退出码为 0。 | 否 |

### 3.3 未完成内容

暂无

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/需求交付各阶段通过追踪表双向关联_验收计划.md) | 定义逐条追踪和上下游导航的验收条件。 |
| 上游 | [测试计划](../qa/需求交付各阶段通过追踪表双向关联_测试计划.md) | 定义追踪表更新、导航和历史保留的测试范围。 |
| 上游 | [代码架构设计](../spec/代码架构设计.md) | 说明追踪表、索引关系和门禁的代码职责。 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看每条验收条件在整个工作流中的位置。 |
| 下游 | [主题测试结果](../qa/需求交付各阶段通过追踪表双向关联_测试结果.md) | 实施完成后记录实际测试结果。 |
| 下游 | [主题验收结果](../acceptance/需求交付各阶段通过追踪表双向关联_验收结果.md) | 测试通过后记录实际验收结果。 |
