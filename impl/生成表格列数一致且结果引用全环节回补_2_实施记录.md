# 【实施】生成表格列数一致且结果引用全环节回补

- 工作流编号：2026-08-30-1153-bugfix
- 验收主题：生成表格列数一致且结果引用全环节回补

## 1. 实施依据

| 依据类型 | 依据编号 | 具体内容 | 文档位置 |
|---|---|---|---|
| 产品设计 | AC-01 | 覆盖表分隔行列数与表头一致，标准 GFM 渲染为表格 | acceptance/生成表格列数一致且结果引用全环节回补_2_验收计划.md#ac-01 |
| 产品设计 | AC-02 | 结果文档生成后同主题五类环节文档的结果引用全部回补 | acceptance/生成表格列数一致且结果引用全环节回补_2_验收计划.md#ac-02 |

<a id="2-实施前计划"></a>
## 2. 实施前计划（代码计划）


### 2.1 预期产品结果

- 覆盖表在标准 GFM 渲染器渲染为真实表格；结果文档生成后同主题验收计划、实施记录、测试计划、结果与验收结果文档的结果引用全部回补为真实链接。

### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|

### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改内容 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | src/workflow_loop/records.py | _generate_document_v2 test_plan 分支 | 分隔行写成“\|---\|”加“---\|”乘列数，比表头多一列 | 分隔行改为“\|”加“---\|”乘列数，与表头列数一致 | 覆盖表分隔行列数等于表头列数，标准 GFM 渲染为表格 | AC-01 | 无 |
| 2 | src/workflow_loop/records.py | _document_reference_map | 结果文档的回补登记按环节手工枚举，已漏 test_plan | 结果文档的回补登记固定覆盖 acceptance_plan、impl_record、test_plan、test_result、acceptance_result 五类环节文档 | 结果文档生成后同主题全部环节文档的结果引用被回补 | AC-02 | 无 |

#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| uv run python -m pytest tests/test_records.py -q | 分隔行列数与回补覆盖 | 新增断言通过且既有用例零失败 |
| uv run python -m pytest tests/ -q | 全项目回归 | 全部用例通过、退出码 0 |

### 2.4 未决问题

- 暂无：两处修法与验证均明确。

<a id="3-实施后记录"></a>
## 3. 实施后记录（代码实施与代码结果）

### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 代码位置（最终文件） | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | src/workflow_loop/records.py | L1646-L2759 | 分隔行改为与表头列数一致（移除 off-by-one） | pytest 分隔行列数断言通过 | 已完成 |
| 2 | 2 | src/workflow_loop/records.py | L1646-L2759 | 结果文档回补登记固定覆盖五类环节文档 | pytest 回补映射断言通过 | 已完成 |

### 3.2 实施中问题与处理

- 首轮填表脚本把代码修改计划行写成了空字典，被讨论完成校验当场拦下；改用 schema 列名重新生成计划行后一次通过，无代码偏离。

### 3.3 未完成内容

状态：无

### 3.4 代码结果

#### 3.4.1 实际代码修改

| 文件 | 代码位置（最终文件） | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| src/workflow_loop/records.py | L1646-L2759 | test_plan 渲染分支分隔行改为按表头列数生成（消除 off-by-one）；_document_reference_map 的结果文档回补登记固定覆盖 acceptance_plan、impl_record、test_plan、test_result、acceptance_result 五类环节文档 | 生成的覆盖表分隔行列数与表头一致、在标准 GFM 渲染为表格；结果文档生成后同主题全部环节文档的结果引用回补为链接 | 分隔行 off-by-one 使覆盖表从未渲染成功；回补按环节手工枚举逐个漏登记，均为用户报告缺陷 | AC-01、AC-02 | tests/test_records.py::test_test_plan_columns_match_coverage_table、test_table_mode_items_carry_plan_fields_and_skip_manual_rows；pytest 全量 490 passed |

#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| uv run python -m pytest tests/ -q | 全项目回归 | 490 项全部通过（含新增断言），退出码 0 | 否 |

## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/生成表格列数一致且结果引用全环节回补_2_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [生成表格列数一致且结果引用全环节回补 测试计划](../qa/生成表格列数一致且结果引用全环节回补_2_测试计划.md) | 代码结果确认后，在测试验证开始时确认范围和通过标准 |
| 下游 | [生成表格列数一致且结果引用全环节回补 测试结果](../qa/生成表格列数一致且结果引用全环节回补_2_测试结果.md) | 同一测试验证阶段连续完成测试代码、登记、执行和结果 |
| 下游 | [生成表格列数一致且结果引用全环节回补 验收结果](../acceptance/生成表格列数一致且结果引用全环节回补_2_验收结果.md) | 正式测试后执行主题验收 |
