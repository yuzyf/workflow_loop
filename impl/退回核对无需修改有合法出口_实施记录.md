# 【实施】退回核对无需修改有合法出口

- 工作流编号：2026-09-09-0901-product_change
- 验收主题：退回核对无需修改有合法出口

<a id="1-实施依据"></a>
## 1. 实施依据

| 依据类型 | 依据编号 | 具体内容 | 文档位置 |
|---|---|---|---|
| 产品设计 | JU-01 | 工作记录表支持核对结论无需修改声明：声明存在、核对依据有实质内容、产物相对基线确实无变化时，第二道门放行。 | [推进并确认工作环节 R47](../spec/功能_推进并确认工作环节.md#r-47) |
| 产品设计 | JU-02 | 每份主题级工作记录表增加可选声明行，核对对象和核对依据两项必填；生成文档渲染为核对记录一节正式留痕。 | [工作记录表与正式文档生成 R30](../spec/功能_工作记录表与正式文档生成.md#r-30) |
| 验收条件 | JU-03 | 声明加依据过门、缺依据拒绝、产物实际变化不走声明、生成文档渲染核对记录。 | [验收计划 AC-01 至 AC-04](../acceptance/退回核对无需修改有合法出口_验收计划.md#ac-01) |
| 现有代码设计 | JU-04 | changed_stage_paths 空变化清单在表模式下按表判断产出；spec 门 2 的 product_change 分支要求产品总说明必须变化，无声明出口。 | [代码架构设计](../spec/代码架构设计.md) |

<a id="2-实施前计划"></a>
<a id="2-实施前计划代码计划"></a>
## 2. 实施前计划（代码计划）


<a id="21-预期产品结果"></a>
### 2.1 预期产品结果

- 退回后核对确认无需修改时，填声明加核对依据即可通过第二道门；声明缺依据被拒绝；产物实际变化时按普通流程走；声明在生成文档留痕。

<a id="22-最低实现设计"></a>
### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 数据、状态与副作用 | 主题级表 schema 新增可选行清单核对结论（核对对象、核对依据两列）；行存在时表内容参与门禁校验：依据列按实质内容最小信息量检查。 | 复用现有行清单机制和 R19 实质校验，声明缺依据自动走内容问题拒绝，不新增校验体系。 | AC-01、AC-02、AC-04 |
| 模块与职责 | changed_stage_paths 返回声明状态；spec 门 2 product_change 分支在产物无变化且声明有效时放行；产物有变化时按普通流程走，声明不参与。 | 出口逻辑收在门禁判定处，声明只是放行依据之一，不影响真实变化的正常校验路径。 | AC-01、AC-03 |

<a id="23-代码修改计划"></a>
### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改内容 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | src/workflow_loop/records.py | 主题级表 schema 与生成器 | 主题级表（acceptance_plan、impl_record、test_plan、test_result、acceptance_result）没有核对结论声明栏位；生成文档没有核对记录章节。 | 主题级表 schema 加可选行清单核对结论（核对对象、核对依据）；生成器按声明渲染核对记录一节（无声明不渲染）。 | 表支持声明行；有声明时生成文档出现核对记录章节。 | AC-04 | 无 |
| 2 | src/workflow_loop/artifact_validation.py | changed_stage_paths | 比较基线返回变化清单；表模式空变化按表判断产出返回通过；没有声明的概念。 | 返回值扩展声明状态：变化为空且当前主题表有有效声明（依据有实质内容）时标记声明放行；变化非空时忽略声明。 | 空变化加有效声明返回声明放行状态；空变化无声明维持现状。 | AC-01、AC-02 | 1 |
| 3 | src/workflow_loop/stages/stages.py | SpecStage.code_validate | product_change 分支在产品总说明未变化时固定报错必须更新，无出口。 | 分支改为：产品总说明变化→照常；未变化但声明有效→放行；未变化且无声明或声明无效→保留现有报错并附声明指引。 | 退回核对无需修改场景能通过门 2；缺声明时报错带指引。 | AC-01、AC-02、AC-03 | 2 |

<a id="开发检查计划"></a>
#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| TMPDIR=/tmp/wf-impl3 .venv/bin/python -m pytest tests/test_records.py tests/test_artifact_validation.py -q | 声明行 schema、生成器渲染和 changed_stage_paths 声明放行 | 新增声明测试通过（有效声明放行、缺依据拒绝、产物变化不走声明）。 |

<a id="24-未决问题"></a>
### 2.4 未决问题

- 暂无

<a id="3-实施后记录"></a>
<a id="3-实施后记录代码实施与代码结果"></a>
## 3. 实施后记录（代码实施与代码结果）

<a id="31-实施动作记录"></a>
### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 代码位置（最终文件） | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | src/workflow_loop/records.py | L100-L1462 | 五张主题级表加核对结论声明行清单；最小信息量；可选缺键放行；no_change_declaration 与 declaration_rationale_has_substance；_declaration_section 渲染。 | py_compile 通过 | 已完成 |
| 2 | 2 | src/workflow_loop/artifact_validation.py | L645-L745 | changed_stage_paths 接入声明放行（_valid_no_change_declaration），表模式文档模式都适用。 | py_compile 通过 | 已完成 |
| 3 | 3 | src/workflow_loop/stages/stages.py | L232-L248 | SpecStage 门 2 product_change 分支按声明放行，缺声明报错附指引。 | py_compile 通过 | 已完成 |

<a id="32-实施中问题与处理"></a>
### 3.2 实施中问题与处理

- 实施中发现 _declaration_section 初版引用了 _generate_document_v2 的内嵌函数 _inline 导致 NameError，已改为局部定义 _one_line；可选行清单缺键在旧版本表上报格式错，已按 required_at_gate=False 缺键视为未声明放行。
- QA 测试执行前发现设计缺口：spec 环节只有轮次级表（product_features/topic_relations）没有主题级表，声明无处可填；已修复——product_features 表 schema 增加核对结论行清单，_valid_no_change_declaration 按环节查找（主题级表按主题查、spec 环节查轮次级表），test_declaration_with_rationale_releases_gate 修复后通过。

<a id="33-未完成内容"></a>
### 3.3 未完成内容

状态：无

<a id="34-代码结果"></a>
### 3.4 代码结果

<a id="341-实际代码修改"></a>
#### 3.4.1 实际代码修改

| 文件 | 代码位置（最终文件） | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| src/workflow_loop/artifact_validation.py | L648-L760 | changed_stage_paths 空变化分支接入声明放行：_valid_no_change_declaration 按环节查找声明——主题级表（impl_record、acceptance_plan、test_plan 等）按主题逐个查；只有轮次级表的环节（spec 的 product_features）查轮次级表——退回核对发生在哪个环节声明就填在那个环节的表里；有效时返回通过并标注核对结论无需修改；变化非空时声明不参与。 | 退回核对确认无需修改时第二道门的产物变化检查放行；缺声明维持原报错。 | 推进 R47：核对过不用改是合法完成状态，与没干活区分。 | AC-01、AC-02、AC-03 | TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_records.py tests/test_commands.py -q --basetemp=/tmp/pytest-td：56 passed |
| src/workflow_loop/records.py | L103-L3098 | 五张主题级表（impl_record/test_plan/acceptance_plan/test_result/acceptance_result）schema 增加可选行清单核对结论（核对对象、核对依据两列，required_at_gate=False）；轮次级表 product_features 同步增加核对结论行清单（spec 环节退回核对时声明填在它里面——spec 无主题级表）；核对对象/核对依据纳入自由描述最小信息量（8/12 字符）；可选行清单缺键视为未声明不报格式错；新增 no_change_declaration（读声明行）和 declaration_rationale_has_substance（占位词同源校验）；_declaration_section 把声明渲染为生成文档的核对记录一节（无声明不渲染）。 | 表支持声明行填写和校验；有声明的生成文档出现核对记录章节正式留痕；旧版本表升级不因缺新栏位报错。 | 工作记录表 R30：声明在表内表达、门禁按 R47 口径放行、生成文档渲染留痕。 | AC-02、AC-04 | TMPDIR=/tmp/wf-qa-check .venv/bin/python -m pytest tests/test_gate_no_change_declaration.py -q --basetemp=/tmp/pytest-qag：4 passed（含 QA 驱动修复后的 product_features 声明场景） |
| src/workflow_loop/stages/stages.py | L235-L244 | SpecStage 门 2 的 product_change 分支接入声明放行：changed_detail 含核对结论无需修改标注时不报产品总说明必须更新；其余情况保留原报错并附声明填写指引。 | product_change 轮次退回核对无需修改能过 spec 门 2；缺声明报错带指引。 | 推进 R47 在 spec 环节的落地（与返回上游 R25 呼应）。 | AC-01、AC-02 | TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_records.py tests/test_commands.py -q --basetemp=/tmp/pytest-td：56 passed |

<a id="342-开发检查记录"></a>
#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_records.py tests/test_commands.py -q --basetemp=/tmp/pytest-td | 声明行 schema、渲染和门禁放行 | 56 passed，退出码 0 | 否 |

<a id="4-上下游文档"></a>
## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/退回核对无需修改有合法出口_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [退回核对无需修改有合法出口 测试计划](../qa/退回核对无需修改有合法出口_测试计划.md) | 代码结果确认后，在测试验证开始时确认范围和通过标准 |
| 下游 | [退回核对无需修改有合法出口 测试结果](../qa/退回核对无需修改有合法出口_测试结果.md) | 同一测试验证阶段连续完成测试代码、登记、执行和结果 |
| 下游 | [退回核对无需修改有合法出口 验收结果](../acceptance/退回核对无需修改有合法出口_验收结果.md) | 正式测试后执行主题验收 |
