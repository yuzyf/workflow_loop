# 【实施】临时产物不再卡实施门禁

- 工作流编号：2026-09-09-0901-product_change
- 验收主题：临时产物不再卡实施门禁

<a id="1-实施依据"></a>
## 1. 实施依据

| 依据类型 | 依据编号 | 具体内容 | 文档位置 |
|---|---|---|---|
| 产品设计 | JU-01 | 实际改动的文件集合只认代码文件、测试文件、脚本、项目配置文件和本轮明确登记的路径，与实施观察快照使用同一套判断标准。 | [实施并保护项目修改 R34](../spec/功能_实施并保护项目修改.md#r-34) |
| 产品设计 | JU-02 | 讨论期临时产物统一放在轮次草稿目录，该目录不参与任何门禁检查、快照和实际改动统计。 | [实施并保护项目修改 R35](../spec/功能_实施并保护项目修改.md#r-35) |
| 验收条件 | JU-03 | 非代码临时文件不算实际改动；草稿目录豁免；后缀扩展登记；放错位置提示不阻塞。 | [验收计划 AC-01 至 AC-04](../acceptance/临时产物不再卡实施门禁_验收计划.md#ac-01) |
| 现有代码设计 | JU-04 | Git 工作区清单当前用黑名单过滤（排除流程目录和 .md），与观察快照的白名单规则不同源。 | [代码架构设计](../spec/代码架构设计.md) |

<a id="2-实施前计划"></a>
<a id="2-实施前计划代码计划"></a>
## 2. 实施前计划（代码计划）


<a id="21-预期产品结果"></a>
### 2.1 预期产品结果

- 讨论期生成的原型图、草图等临时文件不再被实施门禁算成实际改动；临时产物有专门草稿目录且完全不参与检查；非代码产物项目可按项目级配置登记后缀扩展。

<a id="22-最低实现设计"></a>
### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 模块与职责 | 改动清单判定逻辑收口到 verification.is_implementation_related_path 一处，新增项目级扩展后缀参数；Git 清单过滤改为调用同一判定，不再自持黑名单。 | 两处过滤同源后，白名单口径只在一处维护，新增格式免疫自动生效，符合 R34 与观察快照同源的要求。 | AC-01、AC-03 |
| 数据、状态与副作用 | ProjectState 新增 extra_code_suffixes 字段（字符串列表，默认空），写入 project.json；判定函数读取后把后缀并入代码后缀集合。 | 项目级配置跨轮次保存，纯 HTML 等非代码产物项目登记后即生效，不动全局常量。 | AC-03 |
| 错误与边界 | 疑似临时产物提示：Git 清单里未被白名单命中的未提交文件，按目录前缀和后缀识别为疑似临时产物，输出一句存放约定提示，不进入改动清单也不阻塞。 | R35 要求提示不阻塞；识别只用于提示文案，不影响判定结果。 | AC-04 |

<a id="23-代码修改计划"></a>
### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改内容 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | src/workflow_loop/project.py | ProjectState.extra_code_suffixes | ProjectState 没有 extra_code_suffixes 字段，代码后缀集合只来自 verification.CODE_SUFFIXES 常量。 | 新增 extra_code_suffixes 字段（list[str]，默认空）并纳入 MANAGED_PROJECT_FIELDS，加载保存随 project.json。 | project.json 保存项目级扩展后缀；未登记时行为不变。 | AC-03 | 无 |
| 2 | src/workflow_loop/verification.py | is_implementation_related_path | 只按模块内 CODE_SUFFIXES、CONFIG_NAMES、CONFIG_SUFFIXES 和测试路径判定，无项目级扩展入口。 | 新增可选 extra_suffixes 参数，读 project.json 的 extra_code_suffixes 并入后缀集合；新增疑似临时产物判定辅助（未提交、非白名单、非流程目录）。 | 登记 .html 后缀的项目，.html 文件通过判定进入实际改动清单。 | AC-03、AC-04 | 1 |
| 3 | src/workflow_loop/rollback.py | list_actual_working_tree_changes | Git 变化路径按黑名单过滤：排除六个流程目录和 .md 文件，其余全部算实际改动，HTML 原型图等非代码文件被误算。 | 黑名单过滤替换为调用 verification 的白名单判定（含项目级扩展后缀）；.workflow_loop 前缀保持排除（草稿目录天然在内）；对疑似临时产物收集提示信息随返回值带出。 | 原型图不再出现在实际改动清单；草稿目录文件不出现；疑似临时产物在输出中附一句提示。 | AC-01、AC-02、AC-04 | 2 |

<a id="开发检查计划"></a>
#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| TMPDIR=/tmp/wf-impl1 .venv/bin/python -m pytest tests/test_rollback.py -q | 实际改动清单过滤行为与既有回退测试 | 既有测试通过；新增白名单测试通过，原型图路径不在改动清单。 |
| TMPDIR=/tmp/wf-impl1 .venv/bin/python -m pytest tests/test_project_state.py -q | ProjectState 新字段保存加载 | extra_code_suffixes 随 project.json 保存和读回，默认空。 |

<a id="24-未决问题"></a>
### 2.4 未决问题

- 暂无

<a id="3-实施后记录"></a>
<a id="3-实施后记录代码实施与代码结果"></a>
## 3. 实施后记录（代码实施与代码结果）

<a id="31-实施动作记录"></a>
### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 代码位置（最终文件） | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | src/workflow_loop/project.py | L37-L273 | ProjectState 新增 extra_code_suffixes 字段并接入受管字段清单、加载校验和快照恢复。 | py_compile 通过 | 已完成 |
| 2 | 2 | src/workflow_loop/verification.py | L1445-L1514 | 白名单判定加 extra_suffixes 参数；新增 project_extra_code_suffixes、is_scratch_path、looks_like_temporary_artifact。 | py_compile 通过 | 已完成 |
| 3 | 3 | src/workflow_loop/rollback.py | L1470-L1496 | list_actual_working_tree_changes 黑名单过滤替换为白名单判定，加疑似临时产物提示。 | test_rollback 41 项全过 | 已完成 |

<a id="32-实施中问题与处理"></a>
### 3.2 实施中问题与处理

- 连带修复：ProjectState 新增字段使旧作废清单快照校验失败（snapshot 缺 extra_code_suffixes），已在 _validate_managed_fields 兼容旧快照（缺字段按空列表）、restore_managed_fields 用 fields.get 带默认值。

<a id="33-未完成内容"></a>
### 3.3 未完成内容

状态：无

<a id="34-代码结果"></a>
### 3.4 代码结果

<a id="341-实际代码修改"></a>
#### 3.4.1 实际代码修改

| 文件 | 代码位置（最终文件） | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| src/workflow_loop/project.py | L37-L273 | ProjectState 新增 extra_code_suffixes 字段（项目级扩展代码后缀，默认空列表），纳入 MANAGED_PROJECT_FIELDS、load_project 读取（小写规范化）和 _validate_managed_fields（以点开头的字符串数组，旧快照缺该字段按空列表兼容）；snapshot/restore_managed_fields 带上该字段。 | project.json 保存项目级扩展后缀；纯 HTML 等非代码产物项目登记后 .html 文件被实际改动判定识别。 | 非代码产物项目的页面文件不属于常规代码后缀，需要项目级登记入口让白名单扩展（R34）。 | AC-03 | TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_rollback.py -q --basetemp=/tmp/pytest-t4：41 passed（含项目字段快照恢复） |
| src/workflow_loop/rollback.py | L1470-L3805 | list_actual_working_tree_changes 从黑名单过滤改为白名单：调用 verification.is_implementation_related_path（含项目级 extra_code_suffixes 和测试入口）筛选 Git 变化路径，.workflow_loop 等流程目录保持排除（草稿目录 .workflow_loop/scratch/ 天然在内）；未被白名单命中且疑似临时产物（HTML/图片等后缀）的路径不进改动清单，随返回 detail 附存放约定提示。 | 原型图不再出现在实际改动清单和门禁报错；疑似临时产物输出一句提示（指向 .workflow_loop/scratch/<轮次编号>/）不阻塞。 | Git 清单过滤与观察快照同源（R34/R35）：白名单只认代码、测试、脚本、配置和登记路径。 | AC-01、AC-02、AC-04 | TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_rollback.py -q --basetemp=/tmp/pytest-t4：41 passed |
| src/workflow_loop/verification.py | L1445-L1514 | is_implementation_related_path 新增 extra_suffixes 参数（项目级扩展后缀并入判定）；新增 project_extra_code_suffixes（读 project.json，异常返回空集）、is_scratch_path（轮次草稿目录判定）、looks_like_temporary_artifact（疑似临时产物判定：非草稿目录、非流程目录、非白名单、命中提示后缀表）。 | 登记 .html 后缀的项目判定 .html 为实施相关；草稿目录路径可判别；原型图类文件可识别为疑似临时产物供提示。 | 白名单判定收口到一处并支持项目级扩展（R34），提示识别独立于判定结果（R35）。 | AC-02、AC-03、AC-04 | TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_rollback.py -q --basetemp=/tmp/pytest-t4：41 passed |

<a id="342-开发检查记录"></a>
#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| TMPDIR=/tmp/wf-impl-check .venv/bin/python -m pytest tests/test_rollback.py -q --basetemp=/tmp/pytest-t4 | 白名单过滤与既有回退行为 | 41 passed，退出码 0 | 否 |

<a id="4-上下游文档"></a>
## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/临时产物不再卡实施门禁_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [临时产物不再卡实施门禁 测试计划](../qa/临时产物不再卡实施门禁_测试计划.md) | 代码结果确认后，在测试验证开始时确认范围和通过标准 |
| 下游 | [临时产物不再卡实施门禁 测试结果](../qa/临时产物不再卡实施门禁_测试结果.md) | 同一测试验证阶段连续完成测试代码、登记、执行和结果 |
| 下游 | [临时产物不再卡实施门禁 验收结果](../acceptance/临时产物不再卡实施门禁_验收结果.md) | 正式测试后执行主题验收 |
