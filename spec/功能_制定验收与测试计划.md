# 【功能】制定验收与测试计划

## 1. 背景

产品设计确认了“要做什么”，但实施前还需要先说清“什么结果才算完成”，实施后还需要根据真实代码说清“怎样取得可信证据”。如果验收条件、实际实施和测试范围各自解释需求，开发完成后就可能出现代码做了很多、却无法判断是否真正满足用户需求的情况。

因此，本功能分两个时点完成：实施前先把当前需求拆成用户可以独立验收的主题，并为所有本轮行为写清前提、触发、可观察结果和通过或不通过边界；实施完成后，在单一 `qa`（测试验证）环节内读取真实代码和实施记录，为每条验收条件确定真实入口、准备条件、执行动作、观察位置和证据。用户确认测试范围后，AI 在同一环节继续完成测试代码、正式执行和结果整理，不再为测试计划单独增加一次阶段确认。计划不冒充已经测试或验收。

## 2. 场景

| 场景 | 使用者 | 发生条件 | 想完成的事情 | 预期结果 |
|---|---|---|---|---|
| 为从零创建制定计划 | 项目负责人、验收人、项目维护者 | 初始产品设计和必要穿刺已经确认 | 先把全部初始需求拆成可验收结果，实施完成后在测试验证环节设计测试覆盖 | 每项初始需求依次追踪到验收条件、真实实施和测试项 |
| 为产品修改制定计划 | 项目负责人、验收人、项目维护者 | 本轮产品修改和必要穿刺已经确认 | 先明确本轮变化及直接受影响行为的完成边界，实施后在测试验证环节确定测试方法 | 当前需求得到完整验证，无关旧功能留给最终全量回归 |
| 为缺陷修复制定计划 | 项目维护者、验收人 | 缺陷现象、期望行为、根因和修复后主题已经确认 | 先把恢复原有产品行为转成验收条件，修复实施后再确定测试范围 | 原复现条件、正确结果和直接受影响行为都得到覆盖 |
| 复用项目现有测试入口 | 项目维护者 | 项目已经有稳定的全量测试命令或脚本 | 把真实入口登记为最终全量回归依据 | 后续程序使用同一入口，不重复生成脚本 |
| 为项目补充统一测试入口 | 项目维护者 | 项目有可运行测试，但没有单一、稳定的全量测试入口 | 根据项目技术栈补充入口并保存回退基线 | 后续最终全量回归能够运行项目约定的全部单元测试 |
| 测试环境尚未准备 | 项目维护者 | 项目运行时、依赖、设备或测试工具链缺失 | 说明需要准备的环境并等待用户决定 | 未准备好时停留在测试验证环节，不静默安装或假装可测试 |

## 3. 功能边界

<a id="31-支持范围"></a>
### 3.1 支持范围

- 根据已确认需求确定本轮全部验收主题、主题关系、验收范围和完成条件。
- 为每个主题生成 `acceptance/<主题文件标识>_验收计划.md`，并用 `acceptance/索引.md` 首次确认展示顺序和前置主题。
- 在项目根的 `需求交付追踪表.md` 中连接需求来源、验收主题、验收条件、穿刺结论与可复用内容和后续产物。
- 实施完成后读取验收计划、真实代码和实施记录，根据每条验收条件设计一个或多个测试项、真实入口、准备条件、执行动作、观察结果、证据位置和针对性回归范围。
- 为每个主题生成 `qa/<主题文件标识>_测试计划.md`，并用 `qa/索引.md` 继承已确认的主题关系。
- 测试计划作为测试验证环节的执行前依据，与后续测试结果分开保存；结果链接、执行状态和验收状态不属于测试计划内容。
- 测试验证的输入是现状架构基线（如有）、已经确认的代码计划和代码结果、真实产品代码、现有测试及配置；不要求尚未生成的最终代码设计作为前置输入。
- 调查当前项目的测试工具链、依赖环境和统一测试入口；优先复用稳定入口，没有入口时才补充项目专属脚本。
- 在 `.workflow_loop/project.json` 中按操作系统保存统一测试入口的命令参数数组。

### 3.2 不支持范围

- 不在验收计划或测试计划中新增、删除或改写用户尚未确认的产品行为。
- 不把验收计划写成测试步骤，也不把测试计划写成产品需求的重复说明。
- 验收计划不修改产品代码。测试验证环节的计划部分也不修改产品代码；计划检查通过后，AI 可以在同一环节继续复用或修改测试代码、执行正式测试并生成结果。
- 不执行修改前全量测试，不保存修改前测试通过基线，也不因现有测试失败自动阻止实施。
- 不提供一条适用于所有项目的固定全量测试脚本；不同技术栈使用各自项目的真实工具链。
- 不由测试计划改名、拆分、合并验收主题或重新制定主题依赖。

<a id="4-规则"></a>
## 4. 规则

| 编号 | 条件 | 规则 |
|---|---|---|
| <a id="r-1"></a>R1 | 确定验收依据 | 从零创建使用本轮确认的全部产品设计；修改产品使用本轮修改记录和修改后的设计；修复缺陷使用用户报告、缺陷复现记录和现有产品设计中的期望行为。 |
| <a id="r-2"></a>R2 | 确定验收主题 | 从零创建和修改产品在验收计划环节由用户确认；修复缺陷只能使用复现环节已经确认的主题，不能重新改名、拆分或合并。 |
| <a id="r-3"></a>R3 | 命名验收主题 | 使用中文直白说明验收对象和完成结果；主题文字本身就是唯一标识，在整个项目历史中永久唯一，不使用开发任务名称或另加主题编号。 |
| <a id="r-4"></a>R4 | 保存主题文件 | 显示名称保持完整中文，文件路径使用已经登记的稳定、安全中文文件标识；验收、测试、实施和结果文档沿用同一标识。 |
| <a id="r-5"></a>R5 | 确定主题关系 | `acceptance/索引.md` 首次保存展示顺序、验收主题和前置主题；关系不能形成循环。`qa/索引.md` 和后续实施索引只能继承，不能重排。 |
| <a id="r-6"></a>R6 | 确定验收范围 | 只覆盖本轮新增、修改、删除的产品行为，被变化直接影响的已有行为，以及用户明确要求保持不变的行为；无关旧功能交给最终全量回归。 |
| <a id="r-7"></a>R7 | 写验收条件 | 本轮包含、改变、删除或要求保持不变的每个产品行为都必须写清前提、触发动作、可观察或可核实的结果、通过边界和不通过边界，并链接产品设计依据；不能只写“正确”“正常”或“符合预期”。 |
| <a id="r-8"></a>R8 | 编号验收条件 | 每个主题内使用 `AC-01`、`AC-02` 等稳定编号；`AC` 是“验收条件”的程序标识，只用于主题内引用，不是新的主题编号。 |
| <a id="r-9"></a>R9 | 区分验收条件和测试设计 | 验收条件说明产品最终必须怎样工作，不规定测试环境、数据、命令、代码或检查步骤。 |
| <a id="r-10"></a>R10 | 保证需求覆盖 | 本轮每项产品设计修改或缺陷来源至少被一个主题覆盖；一项来源可以由多个主题共同覆盖，一个主题也可以承接多条共同产生同一用户结果的来源。 |
| <a id="r-11"></a>R11 | 建立测试项 | 只有实施完成并形成真实代码和实施记录后，才在测试验证环节为每条验收条件建立至少一个测试项；一条验收条件可以有多个测试项，但每个测试项只绑定一条主要验收条件。 |
| <a id="r-12"></a>R12 | 编号测试项 | 每个主题内使用 `TC-01`、`TC-02` 等编号；`TC` 是“测试项”的程序标识。追踪表必须同时显示编号、直白名称和具体链接，不能只写编号。 |
| <a id="r-13"></a>R13 | 选择测试方式 | 根据真实产品入口和判断需要选择自动化测试、人工验收或二者结合；能自动判断的内容不能为了省事改成纯人工。 |
| <a id="r-14"></a>R14 | 设计测试范围 | 除验收条件覆盖外，单独记录本次变化直接影响的已有行为、准备条件、执行动作、观察位置、通过或不通过判定和需要保留的证据；不机械凑“正常、异常、边界”分类。 |
| <a id="r-15"></a>R15 | 安排测试项依赖 | 同一主题内只记录直接前置测试项并检查循环；跨主题等待关系由验收主题关系表达，不能让测试项跨主题直接互相依赖。 |
| <a id="r-16"></a>R16 | 调查真实项目 | 测试计划必须读取相关验收计划、实施后的真实代码、实施记录、现有测试和当前测试配置；不能只依据实施前设计或文件名猜测试方式。 |
| <a id="r-17"></a>R17 | 测试验证完成前写明可执行细节 | 测试计划检查完成前必须写明真实产品入口、测试入口、准备条件、测试数据、执行动作、观察或证据位置、预期结果和不通过表现；不允许用“实施后确认”或其它待定文字通过测试计划。 |
| <a id="r-18"></a>R18 | 确认项目统一测试入口 | 入口属于被管理项目，不属于工作流安装包；已有稳定入口时复用，没有入口时才根据项目真实技术栈新建脚本。 |
| <a id="r-19"></a>R19 | 保存统一测试入口配置 | `test_entry`，中文含义为“项目统一测试入口”，按 `windows`、`linux`、`darwin` 和 `default` 保存参数数组；当前操作系统配置优先，没有时才使用默认配置。 |
| <a id="r-20"></a>R20 | 入口需要管道、重定向或多条命令 | 把这些动作写入项目脚本，再把脚本作为单一命令登记；不能把等待命令行解释器拆分的整段字符串写入配置。 |
| <a id="r-21"></a>R21 | 测试环境缺失 | 清楚说明所需运行时、依赖和安装动作，由用户决定是否准备；不能静默安装。用户不准备时，本轮停留在测试验证环节。 |
| <a id="r-22"></a>R22 | 新建或修改统一测试入口脚本 | 修改前保存原文件内容或“原本不存在”记录，并保存原 `test_entry` 配置；没有回退基线时不能写脚本或通过门禁。 |
| <a id="r-23"></a>R23 | 统一测试入口的结果约定 | 入口必须运行项目约定的全部单元测试，退出码为零表示通过，非零表示失败或无法执行；测试验证的计划步骤只确认入口，最终全量回归才使用该入口执行整项目测试。 |
| <a id="r-24"></a>R24 | 更新需求交付追踪表 | 追踪表按工作流编号追加独立章节，不覆盖旧轮次；每条验收条件单独占一行。验收计划建立本轮来源、验收条件和实际支撑这些条件的穿刺结论与可复用内容；实施环节补代码计划与代码结果，测试验证再补“测试项”链接；测试结果和验收结果等尚未发生的列保持“待执行”。一项穿刺支撑多条验收条件时，在对应多行重复记录。各环节只更新自己负责的列；每份主题文档保留可直接打开的上游依据、已经形成的下游文档和全局追踪表链接。 |
| <a id="r-25"></a>R25 | 发现计划依据有问题 | 从零创建或修改产品时，产品结果未定义就返回产品设计；修复缺陷时若必须新增或改变产品结果，则作废当前修复轮次，再开始修改产品轮次。验收条件无法判断时返回验收计划；测试验证发现真实实现不满足验收条件时返回实施；必须运行才能确认且会改变产品或实施决定的技术事实未知时返回穿刺。 |
| <a id="r-26"></a>R26 | 测试计划内容绑定 | 测试计划的有效性只绑定测试范围、测试项、入口、准备、动作、观察位置、证据和通过标准。后续结果链接、执行状态、时间、机器记录编号和验收状态变化不算测试计划变化。 |
| <a id="r-27"></a>R27 | 用户确认测试范围 | 用户在测试验证环节开始时确认一次测试范围和通过标准。此后 AI 可以连续完成测试代码、正式执行和结果整理，不再要求用户分别确认测试计划和测试代码；安装依赖、使用生产凭据或修改外部数据等额外风险动作除外。 |

## 5. 使用过程

| 步骤 | 执行者 | 操作或行为 | 产品响应或结果 |
|---|---|---|---|
| 1 | 程序 | 指出验收计划环节必须读取的产品设计、缺陷记录、产物模板、工作规范和相关路径 | 智能体获得本轮验收依据 |
| 2 | 智能体 | 调查本轮需求、产品变化、相关代码、现有测试和已有证据，提出完整主题清单及每个主题覆盖的来源 | 用户先看到整轮准备验收哪些结果 |
| 3 | 智能体、用户 | 先确认主题完整性、拆分和前置关系，再逐个主题讨论验收范围、验收条件和完成判定 | 主题关系和“什么算完成”得到明确确认 |
| 4 | 智能体 | 生成或更新需求交付追踪表、验收索引和各主题验收计划；把实际支撑验收条件的穿刺结论、保留路径、用途和运行方法写入追踪表对应行 | 本轮每条来源、验收条件和可复用穿刺资产都有稳定入口 |
| 5 | 程序、用户 | 程序检查固定结构和覆盖；用户阅读结果并确认验收计划 | 当前验收主题、关系和完成标准成为代码实施依据，流程先进入实施环节 |
| 6 | 程序 | 实施完成后指出测试验证环节必须读取的模板、规范、验收计划、真实代码、实施记录和项目测试材料 | 智能体知道需要调查的真实测试条件 |
| 7 | 智能体 | 先对全部主题做覆盖审查，调查真实产品入口、已有测试工具链、运行环境和统一测试入口 | 用户看到整体覆盖、针对性回归和环境缺口 |
| 8 | 智能体、用户 | 智能体展示全部主题的测试入口、准备条件、执行动作、观察或证据位置、预期结果、测试方式和测试项依赖；用户一次确认测试范围和通过标准 | 每条验收条件都有当前实现上可执行、可检查的验证方法，后续测试动作无需逐段重新确认 |
| 9 | 智能体 | 复用已有统一入口，或在先保存回退基线后补充项目专属入口；随后生成测试索引和各主题测试计划 | 后续测试代码和最终全量回归有明确依据 |
| 10 | 程序、智能体 | 程序在测试验证环节内部检查每条验收条件的测试覆盖、主题一致性、入口配置和追踪链接；智能体检查范围和证据设计 | 空泛测试项、漏测和计划冒充结果会被阻止 |
| 11 | 智能体、程序 | 计划检查通过后，智能体在同一测试验证环节继续复用或修改测试代码、登记并执行正式测试 | 不增加测试计划到测试代码之间的用户确认或阶段切换；此时仍没有产生未执行的通过结论 |

## 6. 异常情况

| 异常情况 | 发生条件 | 产品处理 | 用户看到的结果 |
|---|---|---|---|
| 产品结果没有定义 | 计划无法说明某个条件下产品应该返回什么结果 | 从零创建或修改产品返回产品设计；修复缺陷需要新增或改变产品结果时作废本轮并开始修改产品轮次 | 验收计划不会替产品设计补规则，也不会让修复缺陷非法返回本轮不存在的环节 |
| 验收条件无法判断 | 条件没有明确触发、观察结果或通过标准 | 返回验收计划重新编写并确认 | 不可验证的条件不能进入测试验证 |
| 主题过大、重复或遗漏 | 一个主题混入多个独立结果，或者本轮来源没有主题覆盖 | 返回主题清单重新拆分、合并或补充，并同步索引和追踪表 | 后续计划不会沿用错误主题关系 |
| 修复缺陷的主题被改名 | 测试或验收计划准备改变复现环节已经确认的主题 | 拒绝修改并返回缺陷复现环节处理 | 缺陷记录、验收、测试和实施保持一一对应 |
| 测试项没有覆盖验收条件 | 某条验收条件没有测试项或只有空泛名称 | 补充明确验证方向和测试项后再检查 | 每条验收条件都能找到准备怎样证明 |
| 测试项跨主题依赖 | 某个测试项直接引用另一主题的测试项 | 改用已经确认的主题前置关系，主题内只保留自己的测试项依赖 | 不会出现两套相互冲突的等待规则 |
| 技术事实仍未知 | 真实接口、文件、设备、平台或性能行为会改变测试设计，但没有实际证据 | 返回本轮穿刺环节取得真实证据 | 测试计划不会在关键事实未知时编造方案 |
| 项目没有统一测试入口 | 已有测试只能通过多条零散命令执行 | 根据项目技术栈补充单一入口，并在修改前保存回退基线 | 最终全量回归有稳定、可重复的项目命令 |
| 测试工具链缺失 | 所需运行时、依赖、设备或构建环境不可用 | 说明准备方法并等待用户决定；用户不准备时停留在测试验证 | 不会静默安装环境或带着无法运行的计划继续执行 |
| 入口配置使用命令字符串 | 配置依赖命令行解释器拆分、管道或重定向 | 改为参数数组；复杂动作放入项目脚本 | Windows、macOS 和 Linux 都能按原参数执行 |
| 入口脚本没有修改前基线 | 已经准备新建或修改脚本，但原内容和原配置尚未保存 | 禁止修改和通过门禁，先建立准确回退记录 | 整轮作废时不会因缺少原内容而无法恢复 |
| 计划写成已经通过 | 验收计划或测试计划出现未执行的通过、失败或验收结论 | 删除虚假结果，保留计划和待执行状态 | 用户不会把计划误认为真实测试证据 |

## 7. 修改记录

| 日期 | 工作流编号 | 用户需求 | 修改类型 | 修改内容 | 追踪入口 |
|---|---|---|---|---|---|
| 2026-07-30 | 2026-07-30-0740-product_change | 完善计划、门禁和项目专属全量测试入口 | 新增 | 定义验收主题、验收条件、测试项、追踪关系、项目专属统一测试入口和环境阻塞的完整计划规则。 | [需求交付追踪表](../需求交付追踪表.md#2026-07-30-0740-product_change) |
| 2026-08-07 | 2026-08-07-0837-product_change | 调整验收、实施和测试计划顺序并提高测试可执行性 | 修改 | 验收计划覆盖所有行为并写明可判断边界；实施完成后再依据真实代码和实施记录制定无待定项的测试计划。 | 待验收计划生成 |
| 2026-08-10 | 2026-08-10-0817-product_change | 把测试计划并入单一测试验证环节并登记穿刺复用资产 | 修改 | 验收计划仍在实施前独立确认；实施后，测试计划成为测试验证环节的第一部分。用户一次确认范围和通过标准后，AI 连续完成测试代码、正式执行和结果整理；计划只绑定自身内容，不绑定后续状态和结果链接；追踪表同时记录实际支撑验收条件的穿刺结论和可复用路径。 | 本轮追踪待验收计划建立 |
