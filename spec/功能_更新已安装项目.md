# 【功能】更新已安装项目

## 1. 背景

Workflow Loop 发布新正式版本后，已经安装到其它项目的全局命令、模板、规范和项目版本标记不会自动变化。用户需要进入目标项目，用一条命令把电脑上的全局 `workflow` 命令和当前项目更新到同一个更高版本，同时保留项目的业务代码、正式产物和工作流运行记录。

本功能为旧项目提供明确的更新入口。更新默认使用 PyPI（Python 公共软件包仓库）的最新正式版本，也允许用户指定更高版本。旧版本尚未提供更新命令时，用户通过对应操作系统的正式更新脚本完成第一次升级。更新本身始终保留项目正式产物；旧模板产生的历史标准坏链接由单独的预览、确认和修复动作处理。

## 2. 场景

| 场景 | 使用者 | 发生条件 | 想完成的事情 | 预期结果 |
|---|---|---|---|---|
| 更新到最新正式版本 | 项目维护者 | 当前项目已经完整安装旧版 Workflow Loop | 用一条命令更新电脑和当前项目 | 全局命令与当前项目达到 PyPI 最新正式版本 |
| 更新到指定更高版本 | 项目维护者 | 用户暂时不准备使用比目标版本更高的版本 | 指定一个高于当前版本的正式版本 | 全局命令与当前项目达到指定版本 |
| 从没有更新命令的旧版升级 | 项目维护者 | 当前版本没有 `workflow update` | 使用当前操作系统对应的一条更新脚本 | 脚本先更新全局命令，再更新当前项目 |
| 工作流进行中更新 | 项目维护者 | 当前项目存在进行中的 Workflow Run（工作流轮次） | 不结束当前轮次直接更新 | 运行状态和历史保留；变化的材料在后续推进前重新读取和确认 |
| 补齐部分版本差异 | 项目维护者 | 全局命令和当前项目只有一侧已达到目标版本 | 只更新仍然落后的部分 | 两侧达到同一目标版本 |

## 3. 功能边界

### 3.1 支持范围

- 从目标项目根目录更新当前项目，不向上查找项目根。
- `workflow update` 默认更新到 PyPI 的最新正式版本。
- `workflow update --version <目标版本>` 更新到用户指定的更高正式版本。
- 一条更新操作同时处理电脑全局命令和当前项目；已经达到目标版本的一侧不重复修改。
- 直接覆盖 `AGENTS.md`、`.workflow_loop/Template_Repository/`、`.workflow_loop/Standardized_Repository/` 和 `.workflow_loop/project.json` 中的安装版本。
- 保留 `.workflow_loop/state.json`、`.workflow_loop/journal.jsonl`、`.workflow_loop/rollback/`、业务代码和 `spec/`、`acceptance/`、`qa/`、`impl/`、`bug/` 等正式产物；其中的历史链接也不由更新动作改写。
- 允许有进行中工作流轮次的项目更新；材料变化后，原讨论结论按现有材料变化规则重新确认。
- 支持从任意受支持且骨架完整的旧版本直接更新到目标版本，不要求逐个安装中间版本。
- 每个正式版本提供 `update.sh`（macOS 和 Linux 更新脚本）和 `update.ps1`（Windows 更新脚本），供旧版本完成第一次升级。

### 3.2 不支持范围

- 不通过更新命令降级到低于当前项目版本的版本。
- 不自动安装预发布版、测试版或没有在 PyPI 正式发布的版本。
- 不自动更新同一电脑上的其它项目。
- 不修复 `.workflow_loop/` 缺失、结构残缺或版本标记无法识别的安装。
- 不保存或恢复被覆盖的旧版 `AGENTS.md`、模板和规范。
- 不承诺全局命令和项目文件作为一个整体回滚；任一步失败时保留已经完成的修改并报告两侧版本。
- 不修改项目业务代码、正式产物、工作流状态、历史记录或回退资料；不把历史链接修复隐藏在更新过程中。

<a id="4-规则"></a>
## 4. 规则

| 编号 | 条件 | 规则 |
|---|---|---|
| <a id="r-1"></a>R1 | 用户准备更新项目 | 用户必须先进入目标项目根目录；当前目录不是完整安装的项目时停止。 |
| <a id="r-2"></a>R2 | 未指定目标版本 | 以 PyPI 当前最新正式版本为目标，不选择预发布版本。 |
| <a id="r-3"></a>R3 | 用户指定目标版本 | 目标版本必须已经在 PyPI 正式发布，并且高于或等于当前项目版本。 |
| <a id="r-4"></a>R4 | 目标版本低于当前版本 | 拒绝更新，不修改全局命令或项目文件。 |
| <a id="r-5"></a>R5 | 目标版本等于当前版本 | 全局命令和项目都相同时不修改任何文件；只有一侧落后时只更新落后的一侧。 |
| <a id="r-6"></a>R6 | 判断发布一致性 | PyPI 包版本和对应 GitHub Release 的更新脚本版本必须一致；缺失或不一致时停止。 |
| <a id="r-7"></a>R7 | 更新前预检 | 在写入前检查项目根、项目骨架、当前全局版本、当前项目版本、目标版本和网络来源。 |
| <a id="r-8"></a>R8 | 请求更新确认 | 确认前显示项目绝对路径、当前两侧版本、目标版本、覆盖文件和保留文件；只询问一次。 |
| <a id="r-9"></a>R9 | 用户取消 | 不更新全局命令，不覆盖项目文件。 |
| <a id="r-10"></a>R10 | 覆盖项目管理文件 | 用户确认后直接用目标版本覆盖 `AGENTS.md`、模板仓库和规范仓库；不检查或保留项目定制内容。 |
| <a id="r-11"></a>R11 | 保留运行资料 | `project.json` 只更新安装版本，保留设计初始化、主题历史、全量测试入口和其它项目级状态；不删除或重建当前轮次状态、追加历史、回退资料和正式产物，也不自动修改正式产物中的历史链接。 |
| <a id="r-12"></a>R12 | 当前轮次正在进行 | 更新仍然允许执行；新模板或规范改变材料内容时，后续工作流推进必须重新读取材料并重新确认受影响结论。 |
| <a id="r-13"></a>R13 | 旧版本没有更新命令 | 用户从目标项目根目录运行对应操作系统的更新脚本；脚本完成全局命令更新后继续更新当前项目。 |
| <a id="r-14"></a>R14 | 支持跨版本更新 | 只要旧项目骨架完整且版本受支持，就允许直接更新到目标版本。 |
| <a id="r-15"></a>R15 | 项目安装残缺 | 停止更新并列出残缺项；不把残缺项目当成首次安装，也不猜测修复。 |
| <a id="r-16"></a>R16 | 更新中途失败 | 不回滚已经完成的修改；明确显示失败步骤、当前全局命令版本和当前项目版本，并允许重新执行。 |
| <a id="r-17"></a>R17 | 更新完成 | 只有全局命令和当前项目都达到目标版本时才显示更新成功。 |
| <a id="r-18"></a>R18 | 发布新正式版本 | GitHub Release 必须同时提供 `update.sh` 和 `update.ps1`，README 必须提供两类系统的一条命令更新入口。 |
| <a id="r-19"></a>R19 | 更新后发现历史标准坏链接 | 更新结果仍然有效，正式产物保持不变。需要修复时，另行启动历史链接修复动作：先预览拟修改范围，用户确认后才修改能确定为旧模板生成的标准链接。 |

## 5. 使用过程

| 步骤 | 执行者 | 操作或行为 | 产品响应或结果 |
|---|---|---|---|
| 1 | 用户 | 进入需要更新的项目根目录 | 当前目录成为唯一更新目标 |
| 2 | 用户 | 执行 `workflow update`、带目标版本的更新命令，或旧版本对应的操作系统更新脚本 | 程序开始只读预检，不立即覆盖文件 |
| 3 | 程序 | 检查项目骨架、全局命令、当前项目版本、目标正式版本和发布一致性 | 得到实际需要更新的电脑侧和项目侧范围 |
| 4 | 程序 | 显示项目绝对路径、当前版本、目标版本、覆盖文件和保留内容 | 用户能够判断更新目标与影响范围 |
| 5 | 用户 | 确认或取消 | 取消时零修改；确认时开始直接更新 |
| 6 | 程序 | 更新或复用电脑全局命令 | 全局命令达到目标版本，或失败并报告当前版本 |
| 7 | 程序 | 覆盖项目管理文件并更新项目版本标记 | 当前项目达到目标版本，运行状态和正式产物保持不变 |
| 8 | 程序 | 核对全局命令和项目版本 | 两侧一致时显示成功，并说明正式产物未被修改；不一致时显示失败位置和可重试结果 |
| 9 | AI、用户 | 当前轮次继续推进时重新读取发生变化的材料并确认受影响结论 | 进行中的轮次不会沿用已经过期的材料结论 |

## 6. 异常情况

| 异常情况 | 发生条件 | 产品处理 | 用户看到的结果 |
|---|---|---|---|
| 不在项目根目录 | 当前目录不是完整安装项目的根目录 | 停止更新 | 显示当前绝对路径和正确执行要求 |
| 无法取得最新版本 | 网络不可用、PyPI 无响应或返回内容不能确认 | 在写入前停止 | 全局命令和项目文件不修改 |
| 发布版本不一致 | PyPI 版本和 GitHub Release 脚本版本不同 | 停止更新 | 显示两个来源的版本差异 |
| 请求降级 | 指定版本低于当前项目版本 | 拒绝执行 | 显示当前版本和不允许降级的原因 |
| 已经是目标版本 | 全局命令和当前项目都等于目标版本 | 不覆盖任何文件 | 显示已经是目标版本 |
| 项目骨架残缺 | 模板、规范、项目标记或必要目录缺失 | 停止更新，不自动修复 | 列出残缺内容并提示重新安装前先处理现有残留 |
| 全局更新成功但项目更新失败 | 全局命令已经达到目标版本，项目覆盖失败 | 不回滚全局命令，报告两侧真实版本 | 用户知道可以修复权限或文件问题后重新执行 |
| 项目更新成功但最终核对失败 | 项目文件已覆盖，但版本或身份核对不一致 | 不显示成功，不恢复旧文件 | 显示当前全局和项目版本及失败原因 |
| 进行中轮次材料变化 | 更新覆盖了当前或后续环节使用的模板或规范 | 保留轮次状态，在后续推进时使旧材料确认失效 | AI 被要求重新读取材料并与用户重新确认受影响结论 |
| 更新后仍有历史标准坏链接 | 旧版正式产物使用了无法定位的标准链接 | 不回滚更新，也不在更新中改文档；需要时单独预览、确认和修复 | 更新结果与文档修复范围分开，用户可以先审查拟修改内容 |

## 7. 修改记录

| 日期 | 工作流编号 | 修改类型 | 修改内容 | 追踪入口 |
|---|---|---|---|---|
| 2026-08-04 | 2026-08-04-0950-product_change | 新增 | 增加全局命令与当前项目的一条命令更新、最新或指定更高正式版本、直接覆盖且无备份、进行中轮次更新、跨版本升级、重复更新和旧版本脚本入口。 | 本轮追踪待验收计划建立 |
| 2026-08-07 | 2026-08-07-0837-product_change | 修改 | 明确项目更新本身不修改任何正式产物；历史标准坏链接需通过单独的预览、用户确认和修复动作处理。 | 待验收计划生成 |
