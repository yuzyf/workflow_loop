# Agent 契约

本项目由 workflow_loop 管理。

## workflow 入口

- 用户只需要提出需求，不需要知道或手动执行任何 `workflow` 命令。
- 用户提出需求后，由 AI 先调 `workflow start` 检查状态，再调查现状并推荐 `from_scratch`
  （从零做）、`product_change`（改产品）、`bugfix`（修缺陷）或 `light_task`（无需开发任务）之一。
- 进入任何任务路线前都必须让用户确认；路线启动后严格按每条命令 stdout 打印的"下一步"执行。
- 日常 `workflow` 命令全部由 AI 执行。AI 每次执行或转述命令时，必须先用直白中文说明：
  这条命令要解决什么、会做什么和不会做什么、执行成功后进入哪一步、用户现在是否需要
  确认或操作；不能只把 stdout 中的英文命令原样转发给用户。

## 无需开发任务

- `light_task` 是一类不需要开发的任务，不限于改文档、生成文档、Git 提交或发布版本。
- 只有确认不修改正式产品规则、产品代码、测试代码，以及影响运行、构建、测试、部署或依赖
  的配置时，才使用 `light_task`；否则推荐三种完整研发路线之一。
- 即使只改或生成文档，AI 也要先用第一性原理调查需求、穷尽需要达成共识的问题，给出建议，
  每次只问一个问题并等待用户回答。用户明确确认讨论完毕后，才记录讨论完成并执行任务。
- 执行 commit（本地 Git 提交）、push（推送远端）、发布、删除等难撤销操作前，AI 必须说明
  准确操作并单独取得用户确认。遇到“提交代码”，必须问清是只 commit，还是 commit 后再 push。
- 执行结束后，AI 按约定方法核对并展示真实结果；用户确认结果后才正式收工。
- 失败或作废时保留已经发生的本地和外部状态，不自动回滚，并写清已完成、未执行和失败内容。
- 执行中发现需要开发时，结束当前简单轮次，说明原因并推荐完整路线；用户确认后再开始新轮次。

## 工作记录表

- 环节材料列出工作记录表时，AI 在表中填写固定事实（数据按栏目填，叙述一段一条），
  不手写正式文档的固定标题和表格；正式文档由程序按表生成，改内容只改表。

## 表达要求

AI 回复用户和编写正式文档时：

- 输出前先弄清实际问题、已知事实、限制和目标。
- 能用直白话就不用抽象词；必须使用专业词时，马上说明它具体指什么。
- 写清谁在什么情况下做什么，以及会得到什么结果。
- 删除空泛、重复，或者没有增加事实、决定、行动和理由的话。
