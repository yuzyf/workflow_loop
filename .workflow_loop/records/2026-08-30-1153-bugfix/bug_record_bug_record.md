# 【缺陷】生成表格列数一致且结果引用全环节回补

- 工作流编号：2026-08-30-1153-bugfix

## 缺陷信息

| 缺陷编号 | 现象 | 复现步骤 | 实际结果 | 期望结果 | 根因 |
|---|---|---|---|---|---|
| BUG-01 | 表模式生成的测试计划“验收条件覆盖”表在所有 Markdown 预览里都显示为竖线文字流，不渲染成表格 | 1. 打开任意表模式生成的测试计划文档第 1 章；2. 数表头行与分隔行的列数：表头 13 列、分隔行 14 列；3. Markdown 规范要求两侧列数一致才构成表格 | 表头 13 列、分隔行 14 列，列数不等，整张表不构成 Markdown 表格，六份表模式测试计划全部如此 | 分隔行列数与表头一致（13 列），覆盖表在标准 GFM 渲染器里渲染为真实表格 | 根因说明：test_plan 渲染分支的分隔行写成“\|---\|”再加“---\|”乘列数，比表头多出一列；根因位置：src/workflow_loop/records.py 的 _generate_document_v2 test_plan 分支；根因证据：六份表模式测试计划实测表头 13/分隔行 14，而 _render_rows 生成的其它表分隔行列数与表头一致 |
| BUG-02 | 测试结果文档生成后，测试计划文档“上下游文档”表中的结果引用仍停留（待生成），同类问题此前已在验收计划、实施记录上各修过一次 | 1. qa 门禁生成 qa/生成文档列序与结果引用符合模板规则_2_测试结果.md；2. 打开 qa/生成文档列序与结果引用符合模板规则_2_测试计划.md 第 5 节 | 结果文档已存在，测试计划文档第 32 行仍写“（待生成）”；此前验收计划与实施记录的同类引用各需单独修一次 | 下游文档生成后，同主题全部上游文档中指向它的引用都自动回补为链接，不再按环节逐个补登记 | 根因说明：回补映射按环节手工枚举引用方，每漏登记一个环节就漏一处回补；根因位置：src/workflow_loop/records.py 的 _document_reference_map；根因证据：映射中测试结果文档的登记只有 acceptance_plan 与 impl_record（后补），test_plan 一直缺失 |

## 缺陷说明

- 两个表模式生成缺陷：覆盖表分隔行列数错位导致表格从未渲染成功；回补映射按环节手工枚举导致结果引用回补逐环节漏登记。

## 真实复现条件

- 运行环境：本仓库工作区（macOS，workflow_loop 已安装，表版本 2）
- 真实输入：上一修复轮与 bugfix 轮真实生成的 qa/、impl/ 文档；证据：qa/ 下六份测试计划覆盖表实测表头 13 列、分隔行 14 列；qa/生成文档列序与结果引用符合模板规则_2_测试计划.md 第 32 行（待生成）而同名结果文档已存在

## 根因证据

- BUG-01：六份表模式测试计划实测表头 13 列、分隔行 14 列；_render_rows 生成的对照表列数一致，说明仅 test_plan 分支手拼分隔行有 off-by-one。BUG-02：_document_reference_map 中测试结果文档的登记清单缺 test_plan，结果文档生成后测试计划文档未被回补。

## 修复仍存在的不确定性

- 无：两处根因位置与修法明确。

## 修复与验收结果

- 本节由实施、测试与主题验收阶段按实际结果追加。