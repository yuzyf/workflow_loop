# 门禁失败一次展示完整可处理原因且快照范围准确测试计划

- 工作流编号：2026-08-07-0837-product_change
- 上游验收计划：[门禁失败一次展示完整可处理原因且快照范围准确验收计划](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md)

## 1. 验收条件覆盖

| 验收条件链接 | 测试项 | 前置测试项 | 测试方式 | 产品入口 | 代码入口 | 测试入口 | 准备数据 | 执行动作 | 观察位置 | 预期结果 | 不通过表现 | 证据要求 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| [AC-01：一次列出全部独立错误并标明未检查依赖](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md#ac-01) | <a id="tc-01"></a>[TC-01 同一门禁一次报告全部错误和未检查项](#tc-01) | 无 | 自动化测试 | `workflow gate impl`（实施阶段第二道门） | `src/workflow_loop/diagnostics.py::format_validation_report` | `tests/test_current_workflow_contracts.py::test_gate_reports_all_independent_errors_and_unchecked_dependencies` | 构造两个独立文档错误、一个状态错误和一个依赖前置错误才能执行的带副作用检查，并记录副作用调用次数 | 原样调用门禁校验入口生成完整报告 | 报告中的错误数、未检查数、稳定编号、每项六类事实和副作用调用次数 | 三个独立错误各出现一次；依赖检查标为未检查并注明依赖；副作用未执行；状态停留当前阶段 | 提前返回、遗漏错误、未检查被猜成错误或通过、依赖动作被执行、同一错误重复出现 | pytest JUnit XML 报告、完整诊断文本和副作用调用次数零 |
| [AC-02：每项错误能定位到具体内容并给出唯一下一命令](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md#ac-02) | <a id="tc-02"></a>[TC-02 错误定位到原值且只给一条可执行命令](#tc-02) | TC-01 | 自动化测试 | `workflow gate update_code_design`（最终代码设计程序检查） | `src/workflow_loop/artifact_validation.py::validate_final_architecture` | `tests/test_current_workflow_contracts.py::test_gate_error_points_to_exact_source_and_one_next_command` | 在隔离架构文档同一映射表放入两个不存在的代码位置，并准备正确文件路径作为对照 | 执行最终代码设计门禁并读取格式化诊断 | 来源文件、章节、表格行列、原值、预期格式、证据、影响、修改动作和末尾命令 | 两个错误同次准确定位；末尾恰好一条完整命令，并写明执行者、自动动作、成功条件和后续阶段 | 只说“没有真实文件”、缺原值或行列、需要查看源码才能修改、出现多条命令或命令不可直接执行 | pytest JUnit XML 报告、两项错误原值、唯一下一命令和报告哈希 |
| [AC-03：重复失败不逐次暴露错误，最终回归给出唯一执行方式](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md#ac-03) | <a id="tc-03"></a>[TC-03 重复诊断稳定且最终回归只有真实入口](#tc-03) | TC-02 | 自动化测试 | 重复执行失败门禁；最终回归执行 `workflow gate regression_test` | `src/workflow_loop/stages/stages.py::RegressionTestStage` | `tests/test_current_workflow_contracts.py::test_repeated_failure_is_stable_and_regression_has_one_command` | 保存一份多错误状态并保持文件字节不变；另登记可观察调用次数的统一全量测试入口 | 连续两次生成同一失败报告，随后检查最终回归讨论指令并执行最终回归门禁入口 | 两次报告全文和哈希、下一命令、统一入口调用次数及回归机器记录 | 两次错误清单、顺序和哈希相同；失败报告要求先修改；最终回归唯一命令为 `workflow gate regression_test` 且入口只执行一次 | 第二次才出现原有错误、提示无变化时重试、给出不存在的 `workflow regression`、误用主题测试或未执行统一入口 | pytest JUnit XML 报告、两次报告哈希、统一入口调用次数和最终回归记录 |
| [AC-04：只对登记范围做快照并准确列出文件差异](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md#ac-04) | <a id="tc-04"></a>[TC-04 窄快照忽略生成物并列出四类文件变化](#tc-04) | 无 | 自动化测试 | 实施或测试失效检查使用已登记核心路径 | `src/workflow_loop/snapshots.py::collect_snapshot` | `tests/test_current_workflow_contracts.py::test_snapshots_include_only_registered_core_paths_and_exact_diffs` | 在隔离项目登记四个核心文件，同时创建 `.next`、`node_modules`、构建目录、缓存和运行文件；随后分别新增、修改、删除和把登记文件改成目录 | 采集前后快照并比较差异，再只改变范围外目录并重复比较；另传入非法登记目录和缺失基线 | 快照文件事实、四类差异、非法路径错误和缺基线未检查结果 | 只读取登记普通文件；四个登记变化分别标为 added、modified、deleted、type_changed；范围外变化零差异；非法目录拒绝；缺基线标未检查 | 递归扫描项目、把安装或构建输出当核心变化、只返回总哈希、变化分类错误、非法目录通过或缺基线猜测原因 | pytest JUnit XML 报告、登记路径清单、逐文件前后事实和四类差异集合 |

## 2. 针对性回归范围

- 回归各阶段门禁仍保留原有成功输出和唯一推进命令。
- 回归实施、测试代码和最终回归的副作用只在全部前置通过后执行。

## 3. 测试条件要求

- 使用临时项目和可计数的受控入口，不执行当前活动工作流的真实回归作为准备动作。
- 正式执行使用 `pytest-junitxml`，报告必须精确匹配四个非参数化测试目标。

## 4. 未决测试条件

暂无

## 5. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/门禁失败一次展示完整可处理原因且快照范围准确_验收计划.md) | 定义完整诊断、唯一命令和窄快照结果 |
| 上游 | [实施记录](../impl/门禁失败一次展示完整可处理原因且快照范围准确_实施记录.md) | 诊断与快照真实代码入口 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看本主题交付状态 |
| 下游 | `./门禁失败一次展示完整可处理原因且快照范围准确_测试结果.md`（待生成） | 正式执行后记录结构化报告事实 |
