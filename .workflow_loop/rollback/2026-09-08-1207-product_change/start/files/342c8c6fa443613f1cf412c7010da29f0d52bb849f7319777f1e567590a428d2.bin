# 【实施】门禁失败输出支持自修复

- 工作流编号：2026-09-08-0228-product_change
- 验收主题：门禁失败输出支持自修复

<a id="1-实施依据"></a>
## 1. 实施依据

| 依据类型 | 依据编号 | 具体内容 | 文档位置 |
|---|---|---|---|
| 产品设计 | JU-01 | 退回成功输出必须写明时序：先重新加载材料并与用户讨论、通过第一道门之后再修改产物文件；先改文件会被记进产物基线 | [返回上游重新处理 R25](../spec/功能_返回上游重新处理.md#r-25) |
| 产品设计 | JU-02 | 基线类失败带基线拍摄时间、基线哈希、当前哈希、对比结论；表文档不一致类失败写明差异章节块、凭据指纹与当前指纹，按事实区分修复路径 | [推进并确认工作环节 R46](../spec/功能_推进并确认工作环节.md#r-46) |
| 验收条件 | JU-03 | AC-01 退回输出写明时序；AC-02 基线类失败带对比数据；AC-03 表文档不一致区分修复路径 | [AC-01](../acceptance/门禁失败输出支持自修复_验收计划.md#ac-01) |
| 现有代码设计 | JU-04 | 退回成功输出在 cli.py cmd_return；阶段产出校验报错在 stages.py（修改产品时产品总说明必须更新并记录本轮变化）；文档与表不一致报错在 records.py _sync_product_features（产品总说明的功能清单与工作记录表不一致）；结构化诊断框架 diagnostics.py ValidationReport 已支持七字段 | [代码架构设计 6.3](../spec/代码架构设计.md#63-功能推进并确认工作环节)、[6.12](../spec/代码架构设计.md#612-功能返回上游重新处理) |
| 穿刺结论 | JU-05 | 本轮未执行穿刺，无可复用资产 | 暂无 |

<a id="2-实施前计划"></a>
<a id="2-实施前计划代码计划"></a>
## 2. 实施前计划（代码计划）


<a id="21-预期产品结果"></a>
### 2.1 预期产品结果

- AI 犯操作时序或手改程序管辖区这类错误时，从退回输出和门禁报错直接读到原因与修正方法，不需要翻源码定位。

<a id="22-最低实现设计"></a>
### 2.2 最低实现设计

| 设计项 | 已确认做法 | 选择理由 | 对应验收条件 |
|---|---|---|---|
| 模块与职责 | 三处独立修改，不新建模块：①cmd_return 的成功输出追加时序提示段；②stages.py 修改产品分支报错改为携带基线事实（基线拍摄时间、基线哈希、当前哈希，从 stage_state.artifact_baseline_captured_at 和 artifact_baseline_hashes 与当前 compute_file_hashes 对比取得）；③records.py 两处表文档不一致报错（_sync_product_features 与通用 _document_was_edited 路径）扩展为携带差异章节块、凭据指纹、当前指纹，并按表是否已含新内容区分两种修复路径文案 | 三处都是既有输出点的文案与事实增强，诊断结构 ValidationReport 已有位置承载额外事实，分散在原位置改动最小、调用方全部自然受益 | AC-01、AC-02、AC-03 |
| 接口与调用顺序 | ②③的报错文案组装各自封装为所在模块的辅助函数（如 _baseline_conflict_facts、_table_document_conflict_facts），返回结构化字段串；调用处只把原断言字符串换成函数返回值，不改变失败判定条件本身 | 判定条件与事实展示分离；将来其他环节复用同一文案口径时不复制字符串 | AC-02、AC-03 |
| 数据、状态与副作用 | 全部为只读增强：基线对比读 stage_state 已存字段；指纹对比读已存生成凭据与当前文件；退回提示为纯输出。不新增状态字段，不改变任何判定通过与否的条件 | 本主题只改输出信息量，任何判定语义变化都会超出已确认范围 | AC-01、AC-02、AC-03 |
| 错误与边界 | 基线哈希缺失（旧状态无基线）时报错写明无法提供对比数据的原因而不是崩；表文档不一致但差异章节无法定位时，退回为现有笼统文案加无法定位原因；文案均按全局失败输出规范六要素组织 | 旧状态兼容是既有约束；六要素是项目自身规范要求 | AC-02、AC-03 |

<a id="23-代码修改计划"></a>
### 2.3 代码修改计划

| 顺序 | 文件 | 类、函数或配置项 | 当前逻辑 | 计划修改内容 | 数据、状态或输出变化 | 对应验收条件 | 前置步骤 |
|---|---|---|---|---|---|---|---|
| 1 | src/workflow_loop/cli.py | cmd_return 的成功输出段 | 退回成功输出说明已退回哪里、清除了什么、当前要做和下一步，不含产物修改时序 | 输出追加时序提示：先重新加载材料并讨论、通过第一道门之后再修改产物文件，先改文件会被记进产物基线、第二道门看不见变化 | 退回成功输出多一段时序说明 | AC-01 | 无 |
| 2 | src/workflow_loop/stages/stages.py | SpecStage 阶段产出校验中修改产品分支（修改产品时产品总说明必须更新并记录本轮变化 报错处） | 只报一句断言，不带基线事实 | 报错改为携带产物基线拍摄时间、产品总说明基线哈希、当前哈希、对比结论；文件在基线前已被修改时写明时序修正方法 | 报错一次输出全部对比数据 | AC-02 | 无 |
| 3 | src/workflow_loop/records.py | _sync_product_features 与 _document_was_edited 调用处的表文档不一致报错 | 统一报文档被直接修改、请把改动写回工作记录表，不区分表是否已更新 | 报错携带差异章节块、程序凭据指纹、当前指纹；按表内容是否已等于本次预期生成结果区分：表未更新提示写回表，表已更新仅文档手改提示恢复文档由程序按表重写 | 两种情况各自输出正确修复路径 | AC-03 | 无 |
| 4 | tests/test_gate_self_heal_output.py | 新增测试文件 | 暂无现有逻辑（新文件） | 覆盖：退回输出含时序段；基线类失败报错含四项对比数据且同状态重复执行输出一致；表未更新与表已更新两种场景各自修复路径文案正确 | 新增测试全部通过 | AC-01、AC-02、AC-03 | 3 |

<a id="开发检查计划"></a>
#### 开发检查计划

| 检查命令或方法 | 检查范围 | 预期观察结果 |
|---|---|---|
| uv run python -m pytest tests/test_gate_self_heal_output.py -q | 三处输出增强全部行为 | 全部测试通过，退出码 0 |
| uv run python -m pytest tests/test_commands.py tests/test_return_rework_flow.py tests/test_gate_rework_stability.py -q | 退回、门禁、表同步既有行为无回归 | 既有测试全部通过 |

<a id="24-未决问题"></a>
### 2.4 未决问题

- 暂无

<a id="3-实施后记录"></a>
<a id="3-实施后记录代码实施与代码结果"></a>
## 3. 实施后记录（代码实施与代码结果）

<a id="31-实施动作记录"></a>
### 3.1 实施动作记录

| 实施顺序 | 对应计划步骤 | 文件 | 代码位置（最终文件） | 实际执行的动作 | 当步反馈 | 状态 |
|---|---|---|---|---|---|---|
| 1 | 1 | src/workflow_loop/cli.py | L3842-L3849 | cmd_return 成功输出增加产物修改时序段（目标为 spec/acceptance_plan/reproduce 时输出）：先过第一道门再改产物文件、先改会被记进基线、无需修改不要求制造变化 | tests/test_gate_self_heal_output.py 时序提示测试通过 | 已完成 |
| 2 | 2 | src/workflow_loop/stages/stages.py | L73-L109 | 新增 _baseline_conflict_facts（基线时间、基线哈希、当前哈希、对比结论四项；内容一致时报修改被记进基线与时序修正方法）；SpecStage 的修改产品报错追加该事实段 | test_gate_self_heal_output.py 两项基线事实测试通过 | 已完成 |
| 3 | 2 | src/workflow_loop/stages/stages.py | L235-L238 | （续前一行）新增 _baseline_conflict_facts（基线时间、基线哈希、当前哈希、对比结论四项；内容一致时报修改被记进基线与时序修正方法）；SpecStage 的修改产品报错追加该事实段 | test_gate_self_heal_output.py 两项基线事实测试通过 | 已完成 |
| 4 | 3 | src/workflow_loop/records.py | L2342-L2396 | 新增 _table_document_conflict_facts（差异章节块、凭据指纹、当前指纹；按预期正文摘要与凭据对比判定表是否更新，区分写回表与恢复文档两种修复路径，无凭据时保守提示）；四处表文档不一致报错全部替换 | test_gate_self_heal_output.py 三项冲突路径测试通过；既有断言更新后全过 | 已完成 |
| 5 | 3 | src/workflow_loop/records.py | L2126-L2133 | （续前一行）新增 _table_document_conflict_facts（差异章节块、凭据指纹、当前指纹；按预期正文摘要与凭据对比判定表是否更新，区分写回表与恢复文档两种修复路径，无凭据时保守提示）；四处表文档不一致报错全部替换 | test_gate_self_heal_output.py 三项冲突路径测试通过；既有断言更新后全过 | 已完成 |
| 6 | 3 | src/workflow_loop/records.py | L2756-L2763 | （续前一行）新增 _table_document_conflict_facts（差异章节块、凭据指纹、当前指纹；按预期正文摘要与凭据对比判定表是否更新，区分写回表与恢复文档两种修复路径，无凭据时保守提示）；四处表文档不一致报错全部替换 | test_gate_self_heal_output.py 三项冲突路径测试通过；既有断言更新后全过 | 已完成 |
| 7 | 3 | src/workflow_loop/records.py | L3134-L3141 | （续前一行）新增 _table_document_conflict_facts（差异章节块、凭据指纹、当前指纹；按预期正文摘要与凭据对比判定表是否更新，区分写回表与恢复文档两种修复路径，无凭据时保守提示）；四处表文档不一致报错全部替换 | test_gate_self_heal_output.py 三项冲突路径测试通过；既有断言更新后全过 | 已完成 |
| 8 | 3 | src/workflow_loop/records.py | L3434-L3441 | （续前一行）新增 _table_document_conflict_facts（差异章节块、凭据指纹、当前指纹；按预期正文摘要与凭据对比判定表是否更新，区分写回表与恢复文档两种修复路径，无凭据时保守提示）；四处表文档不一致报错全部替换 | test_gate_self_heal_output.py 三项冲突路径测试通过；既有断言更新后全过 | 已完成 |
| 9 | 4 | tests/test_gate_self_heal_output.py | L1-L224 | 新建测试：退回输出时序提示存在性、基线四项事实齐全、无基线时保守提示、表已更新走恢复文档路径、表未更新走写回表路径、无凭据保守提示 | 6 passed in 0.27s | 已完成 |

<a id="32-实施中问题与处理"></a>
### 3.2 实施中问题与处理

- 实施中修正了一处初版设计缺陷：表是否更新的判定初版写成了自比较恒真，改为按当前表生成预期正文摘要与上次凭据对比，配套两项路径测试。

<a id="33-未完成内容"></a>
### 3.3 未完成内容

状态：无

<a id="34-代码结果"></a>
### 3.4 代码结果

<a id="341-实际代码修改"></a>
#### 3.4.1 实际代码修改

| 文件 | 代码位置（最终文件） | 实际修改的代码逻辑 | 数据、状态或输出的实际变化 | 修改理由 | 对应验收条件 | 测试证据 |
|---|---|---|---|---|---|---|
| src/workflow_loop/cli.py | L3842-L3849 | cmd_return 成功输出在目标为产物环节（spec/acceptance_plan/reproduce）时追加【产物修改时序】段：先重新加载材料讨论、通过第一道门后再改产物文件；先改文件会被记进基线、第二道门看不见变化；核对无需修改不要求制造无意义修改 | 退回成功输出含完整时序说明与原因 | 返回上游 R25 时序提示（今天 spec 重走踩坑的直接修复） | AC-01 | tests/test_gate_self_heal_output.py::test_return_output_contains_sequence_hint |
| src/workflow_loop/stages/stages.py | L73-L238 | _baseline_conflict_facts：从 stage_state.artifact_baseline_captured_at 和 artifact_baseline_hashes 取基线时间与哈希，compute_file_hashes 取当前哈希；两者一致时输出修改被记进基线结论与先过第一道门的修正方法，不一致时输出确认时序提示；无基线时输出无法提供对比数据与原因。SpecStage 修改产品分支报错追加该段 | 基线类失败报错带四项对比数据，不读源码即可定位 | 推进 R46 基线类失败带机器事实 | AC-02 | test_gate_self_heal_output.py 两项基线事实测试通过 |
| src/workflow_loop/records.py | L2126-L3441 | _table_document_conflict_facts：current_hash 取当前文件、previous_hash 取生成凭据 body_hash；表是否更新=按当前表生成的预期正文摘要是否不同于凭据；表已更新输出恢复文档由程序按表重写（不要再改表），表未更新输出写回工作记录表，无凭据输出保守双查提示。四处调用点（通用 sync、单表 sync、product_features、bug_record）全部替换 | 表文档不一致报错带差异章节块与两个指纹，两种情况各自输出正确修复路径 | 推进 R46 表文档不一致区分修复路径（今天第 7 章手改踩坑的修复路径误导修复） | AC-03 | test_gate_self_heal_output.py 三项冲突路径测试通过 |

<a id="342-开发检查记录"></a>
#### 3.4.2 开发检查记录

| 检查命令或方法 | 检查范围 | 实际反馈 | 是否需要继续修改 |
|---|---|---|---|
| .venv/bin/python -m pytest tests/test_gate_self_heal_output.py -q --basetemp=/tmp/pytest-basetemp | 三处输出增强全部行为 | 6 passed in 0.27s，退出码 0 | 否 |
| .venv/bin/python -m pytest tests/test_commands.py tests/test_return_rework_flow.py tests/test_gate_rework_stability.py -q --basetemp=/tmp/pytest-basetemp | 退回、门禁、表同步既有行为无回归 | 全部通过，退出码 0 | 否 |

<a id="4-上下游文档"></a>
## 4. 上下游文档

| 关系 | 文档 | 说明 |
|---|---|---|
| 上游 | [验收计划](../acceptance/门禁失败输出支持自修复_验收计划.md) | 本主题要达到的用户结果和验收条件 |
| 全局 | [需求交付追踪表](../需求交付追踪表.md) | 查看完整交付链路 |
| 下游 | [门禁失败输出支持自修复 测试计划](../qa/门禁失败输出支持自修复_测试计划.md) | 代码结果确认后，在测试验证开始时确认范围和通过标准 |
| 下游 | [门禁失败输出支持自修复 测试结果](../qa/门禁失败输出支持自修复_测试结果.md) | 同一测试验证阶段连续完成测试代码、登记、执行和结果 |
| 下游 | [门禁失败输出支持自修复 验收结果](../acceptance/门禁失败输出支持自修复_验收结果.md) | 正式测试后执行主题验收 |
