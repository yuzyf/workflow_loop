# 【穿刺】穿刺清单

- 工作流编号：2026-08-04-0950-product_change

## SP-001 Windows 辅助进程替换或删除正在运行的全局命令

- 真实场景：Windows 用户从项目根目录执行 `workflow update`，或者执行 `workflow uninstall --global`；当前 `workflow.exe` 需要先退出，再由 PowerShell 辅助进程更新或删除 uv 管理的全局工具。
- 要验证的不确定性：Windows PowerShell 5.1 和 PowerShell 7 启动的辅助进程，能否可靠等待父 `workflow.exe` 退出，然后用真实的 `uv tool install --force` 完成更新，或用 `uv tool uninstall` 完成全局卸载，并把结果保留给调用方检查。
- 验证结果用于决定什么：决定 Windows 更新和全局卸载脚本是否采用“当前命令启动辅助进程后退出，辅助进程等待并继续”的设计；失败时必须改用独立下载脚本或其他进程交接方式。
- 结论文档：[Windows 辅助进程替换或删除正在运行的全局命令](./穿刺_Windows_辅助进程替换或删除正在运行的全局命令.md)
- 穿刺状态：限制已确认
- 是否阻塞后续：否
- 产品设计影响：无需修改
- 代码设计影响：需要修改
- 后续处理阶段：impl
